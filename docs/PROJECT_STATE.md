# Paco — Project State

> Snapshot of where the project stands. Update this when major milestones ship.
> Last updated: end of session that shipped v0.8.9 (M5.6 — **Color theming**, a cross-cutting foundation (not a per-screen cluster): 4 user-selectable palettes — **Sépia (default)** · Ardoise (petrol) · Indigo (violet) · **Nuit (dark)** — on a root `data-theme` attribute. **Phase 0 unlock:** `@theme inline` (scaffold default) baked token hexes into utilities so `[data-theme]` couldn't override them; split so **fonts stay `inline`** (they var-chain to the next/font `<html>` vars) and **colors/radius/shadow go non-inline** → utilities emit `var(--color-*)`, Sépia byte-identical. **`[data-theme]` blocks** (`:root[data-theme=…]`) set the 13 mapped roles (handover `PALETTES`; `surface→card`/`border→line`/`sepia→muted`/`accent*→amber-*`/`success→ok`/`danger→err`) + 9 **designer-specced legibility tokens** (`ok-bg`, `sage-ink/border`, `err-bg`, `terra-ink/border`, on-accent `ivory`, `disabled-ink`, `scrim`); the 6 accent-family tints (`amber-mid/tint/pale`, `tint`, `tinted-border`, `hair`) are shared **`color-mix`** derivations tracking each theme's accent (Sépia keeps literals = parity). **Frozen semantics:** success(sage)/danger(terra) keep their role every theme, never re-themed to accent — Nuit **inverts** them to bright dark-ground values (`#8ACCB6`/`#EC8E6B`); the **rating rungs track the accent** (amber-family tokens) per "accent replaces amber". **Nuit:** on-accent flips dark via `--color-ivory #2A2218`; shadows darken via `--tw-shadow-color`; deeper scrim. **Phase 1 holdouts** de-hardcoded (Button §03 hexes→tokens, new `--color-disabled-ink`/`--color-scrim`, `text-white`→`text-ivory`, Discovery washes/stamps→`color-mix`); 4 dead rating tokens backlogged. **Control + persistence:** ACTIVE "Thème de couleur" swatch picker (`ThemePicker`) → flips `<html data-theme>` + cookie + PATCH; `profiles.theme` column; `lib/theme.ts` (tested); SSR **cookie-mirror** on `<html>` (FOUC-free, `suppressHydrationWarning`). Suite 446→451; tsc+lint clean; smoke-tested live-switch + Nuit. **Accepted caveats:** `color-mix` legacy fallback (modern-mobile target), new-device one-frame flash, tunable tint %. Tag the docs-close commit). Prior: v0.8.8 (M5.5i — **Profil / Préférences / Compte cluster**: a NEW settings/account surface at `/account` built from the `profil-screens.jsx` mockups onto the locked v2 board (only new token `--color-terra-border`; reusable `components/form/` = the auth foundation login/signup will import). Three live `profiles` controls each closing write→persist→read via `PATCH /api/profile`: **Cartes par session** (stepper 10–50/def 20 → `cards_per_session`, read by BOTH `/review` cap consumers); **Lecture auto à la révélation** (→ `autoplay_audio`, gates a NEW reveal-time autoplay in `ResultReveal`); **Vitesse de lecture** (Lent/Normal/Rapide → `playback_speed` → tested `playbackRateFor` → `playbackRate` 0.833/1.0/1.111 over the baked-0.9 MP3, **Normal=today's default**, `preservesPitch` true; Web-Speech fallback out of scope). Audio settings flow app-wide via a server-seeded `SettingsProvider`. **Stats strip "révisions" sourced from `Σ review_cards.reps`, NOT `count(review_logs)`** (deliberate — the logger is broken; `reps` is deterministic/monotonic/already-populated/log-independent), summed on the same `getDictionaryState` fetch as `memorizedCount` (collection scope manual∪promoted). **Compte:** email · change-password (`signInWithPassword` reauth → `updateUser`, wrong-current = field error; validity from tested `password-policy`) · type-to-confirm "SUPPRIMER" delete (privileged `auth.admin.deleteUser` cascade; copy uses TOTAL words) · logout; **legal kept LIVE** (two re-skinned NavRows, NOT demoted to BIENTÔT). BIENTÔT inert rows for unbuilt features. Tested helpers `playback-speed`/`session-cap`/`password-policy`; suite 446; tsc+lint clean. **SURFACED, separate commits:** the `review_logs` insert bug (`word_id`→`card_id`, empty since inception — that's *why* révisions reads `reps`) + `getDictionaryState`'s swallowed words-fetch error; **no-backfill gap** (~532 historical reviews unrecoverable; Home time-estimate self-heals post-fix); **parked:** new-cards-per-day budgeting. Tag the docs-close commit after smoke-test). Prior: v0.8.7 (M5.5h — **Conjugaison drill repaint** (design-pass, screen-by-screen): the shipped `/drill` flow (setup → prompt → result → bilan) re-skinned onto the v2 board, **presentation-only** (grading verdict tiers / scheduling / deck-only sourcing / <5-verb unlock / `profiles` prefs / queries all untouched) **+ one new tested helper `tenseLabel`** — the SINGLE Spanish tense-name source for every drill surface (chips · both focus headers · cue parenthetical · result teaching line · bilan), returning the FULL names. **DELIBERATE (Omar's): drill tense names stay Spanish** — the on-screen `ConjugationGrid` headers are inherently Spanish so French chips would split one screen's vocabulary, Spanish reinforces the learning frame, and it's consistent with the M5.5g dictionary row-gloss call; `Pretérito perfecto simple` → **`Pretérito indefinido`** applied app-wide incl. the shared `ConjugationGrid.labelEs` (so /add + /review match; `resolveGridTense` still parses either as input). `¡Casi!` neutralized to ink (board §6); `Voir la conjugaison` **blanks the asked cell** (réviser Indice-2 reuse, the other five forms shown); teaching line keeps its French frame but Spanish names; canonical `Button` on every CTA; shared `AnswerBlank` **caret-gated-on-focus + enlarged tap area**; **Enter advances** the result screen. **DECLINED:** chip horizontal-scroll + edge-fade (chose `flex-wrap`, forward-compat for the M5.3d compound tenses). **DEFERRED:** the Accueil « entraînement » card (rides the Home cluster pass). Suite 428; tsc + lint clean; smoke-tested clean, tagged on the close commit). Prior: v0.8.6 (M5.5g — **Dictionnaire cluster** design-pass: the Dictionnaire surface (nav pill · Home card · locked screen · populated A–Z index · unlock celebration) re-skinned onto the v2 board, **all M5.2b logic preserved** (collation/jump rail/sticky flag/audio/routing), plus two behavioural changes. **(1) Unlock reveal relocated to review-end:** normal bilan first → tapping **either** bilan exit intercalates the **`UnlockTakeover`** once → "Plus tard" resumes the tapped exit / "Ouvrir" → `/dictionary`; the Home/app-load `<UnlockSync/>` stays a **safety-net fallback**. **Flip-on-show:** `evaluateDictionaryUnlock()` is now **read-only**, the flag is flipped by **`markDictionaryUnlocked()`** on the celebration's client mount — so the flag is an honest "ceremony shown" once-guard (quitting before tapping can't lose it; the safety-net stays valid), still no new column, still client-mount-triggered Server Actions (not RSC render). **(2) Milestone number = live memorized count** at the crossing (≥10, same source as the dictionary/home count, never hardcoded). Shared `UnlockTakeover` (review-end + the re-skinned safety-net interstitial); `Display` union **+`'milestone'`**; `Confetti` re-skinned to the sanctioned warm drift + `lx-*` entrance keyframes under reduced-motion; populated index re-skin + jump-rail **magnify bubble** + **`AudioButton variant="circle"`** (reuse); **row gloss stays Spanish (`definition.es`)** as a *recorded* decision (an index of already-memorized words — Spanish reinforces; FR at the fiche — NOT the old "Spanish-first override" framing); locked screen / Home card / locked nav pill re-skinned (the 5 always-on pills + inactive-border untouched = deferred nav-IA); dictionary icon = lucide `BookA` everywhere (nav pill + Home card; the handover raster was tried then dropped — read darker/inconsistent vs the vector pill icons; no `dict-book-*.png` ships). Suite 424→428; tag held for the device smoke-test. Prior: v0.8.5 (M5.5f — **Écriture context/hint model + QCM instruction**: the écriture card's always-shown context (redundant with the FR sentence translation) is removed for both card types and replaced by a **penalized 3-tier Indice** — non-verb: first-letter / ES definition / scramble; verb: form `v.·tense·person` / blanked `ConjugationGrid` / scramble (degrade-safe via `verbGridCoords`; scramble = tested `lib/scramble.ts`). A **rating cap by hints used** (`HINT_CAP=[4,3,2,1]`, 0→Facile…3→À revoir) **replaces** the old `hintUsed` penalty (`computeRating` now takes `hintLevel`; écriture-only; no double-count). The `ConjugationGrid` is **reused three ways** (Indice 2 blanked + the two verb verdicts highlighted; `blankTarget`/`infinitive` opt-in props leave /add + drill identical). A **QCM instruction eyebrow** ("Choisissez la bonne réponse") pairs with écriture's. **Countdown untouched.** Suite 416→424; tag held for the device smoke-test. Prior: v0.8.4 (M5.5e — **Review cluster**, the fourth per-screen design-pass slice: `/review` (FSRS QCM + écriture) re-skinned onto the v2 board — **three fixes** (écriture lemma-reveal now conditional: verb chip `infinitif·tense·person` in **Spanish** via the new tested `lib/review-cue.ts`, non-verb `posAbbrev·FR gloss` never the lemma; **¡Casi! → neutral ink** incl. the verb-form face, **verdict-layer only — `computeRating`/FSRS untouched**; **rating → amber single-hue gradation, no dots**, new `--color-amber-pale`) + **one new behavior: the auto-advance countdown** (neutral JS-driven faint bar 0→100%, tunable `AUTO_ADVANCE_MS=10000`, suggested-rating-on-complete, one-tap-advance on the suggestion / different-pill-cancels / pause=stop, no captions, honest reduced-motion numeric) + the re-skinned **① entry card relocated to HOME** (crème+ hero w/ `Display kind="count"`; `/review` auto-starts, Durmiendo passive; the standalone landing was removed) + a **device crash fix** (`lib/bilingual.ts` `flatBilingual` normalizes bilingual fields in the shared `mapReviewRow`, so a malformed nested `{es,fr}` never renders raw) + **Display union extended** to route ALL verdict faces (¡Eso es!/¡Casi!/¡Uy!/¡Buen trabajo!) through the gate + ⑥ "Sus du 1er coup" (replaces Réussite %). Suite 402→411; tag held for the device smoke-test. Prior: v0.8.3 (M5.5d — **Discovery cluster**, the third per-screen design-pass slice: `/discover` re-skinned onto the v2 board — ① theme grid + §04 selected tint + the **"Pour toi" non-functional placeholder shell** (adjacency + A2–B1 featured cards w/ "Bientôt" chip → warm Paco "pas encore disponible"; features stay deferred = M5.1b + content-gate); ② generation **reuses the /add loading choreography** via the new shared `LoadingChecklist` (extracted from `LoadingIdiom`; `IdiomCard` moved to shared; theme phase labels + longer dwell; reveal gated on max(dataReady, floor)); ③④ the fling-commit deck re-skinned with the unified inline **`posAbbrev`** (speaker omitted per the brief's card enumeration); ⑤ the **swipe-color role fix** (Je connais → sage, À apprendre → amber — M5.1 had them inverted; `SwipeCard` gains board stamp placement + wash + lift); ⑥ end states → Feliz bilan (honest tally, no streak) + Durmiendo exhausted. No FSRS/generation change; suite 402; tag held for the device smoke-test. Prior: v0.8.2 (M5.5c — **Add-flow cluster**, the second per-screen design-pass slice: the whole `/add` journey re-skinned onto the v2 board across the six add-flow boards — entry + autocomplete + did-you-mean re-skin, ② loading with the **specced-but-never-firing active-phase animation finally WIRED** (board keyframes in `globals.css`, honest reduced-motion fallback; fixes the backlog bug), ③ ¡Listo! → Fraunces via the **first real `Display` consumer**, rotating curated input placeholder (drops "mariposa"), ⑤ "Forme conjuguée" interstitial re-skin (deterministic grid, never LLM), and **two newly-built screens whose behaviors already shipped: ④ compact "déjà dans ton deck" duplicate** (replaces the full-fiche-for-duplicate; reset-schedule moves to the detail ⋮) **and ⑥ save success — single (confirmed "dans ton deck") + multi (honest in-progress "N mots en route", background bulk-add unchanged).** `WordRow` extended to optional-link (reuse) for ⑥-multi; additive plumbing (`save` returns id, `enrich` returns card `state`/`stability` for ④'s row). No taxonomy/FSRS/conjugator change; suite 402; tag held for the device animation smoke-test. Prior: v0.8.1 (M5.5b — **Words cluster**, the first per-screen design-pass slice: `/words` + detail + delete/undo/Relearn re-skinned onto the v2 board and §06 status pills built — **presentation-only over the unchanged 6-band `lib/word-status.ts` taxonomy**. New `getMasteryGauge` 4-dot projection `{0,1,2,4}` (3rd dot never filled by design); StatusPill render-layer label-map (Relearning folds into À réviser; En cours/En apprentissage merge); `MasteryGauge` replaces `FamiliarityMeter`; filter pills→`SELECTION_ACTIVE`, sort→amber underline; flush one-rectangle `SwipeRow` + dark undo toast w/ countdown; detail ⋮ overflow menu (Relearn/Supprimer, bottom bar removed) + labeled "‹ Mes mots" back + two-way FR reveals; three mascot empty states (**no-results=Pensando**, intentional divergence from the board's Durmiendo; passive=Durmiendo; first-word=Animando); token reconciliation (ok-bg→#DCEAE5/err-bg→#F4E1D8/tint→#F6E7CC + sage-ink/sage-border/tinted-border/terra-ink/amber-tint/amber-mid/shadow-menu); `SELECTION_PERSISTENT` softened to tinted-border. **Pre-tag refinements, final per `BRIEF-words-adjustments.md` (same branch):** gauge re-bucketed to fill all 4 dots (`GAUGE_MID_STABILITY_DAYS=15`; New→0/<7→1/<15→2/<30→3/≥30→4), **horizontal in the right column centered under the pill, leading dot kept**; "N révision(s)" **removed from rows, kept on detail only**; **POS unified inline+abbreviated+gendered** across /words detail + /add via shared `posAbbrev` (spelled-out eyebrow removed; `posEyebrow` now discovery-deck-only), FORME conditional on both; bidirectional sort (re-click flips direction + amber caret; Date newest / Familiarité least-mastered / Alpha A→Z defaults). Suite 383→402. Prior: v0.8.0 (M5.5a — design-system foundation, the FIRST slice of the PRE-BETA design pass; **substrate only, NOT a screen redesign**). Additive color tokens to board §01 (`--color-amber-deep #9A5A1C`, `--color-amber-light #F2DCB8`, `--color-ivory #FFFBF3`, `--shadow-amber`/`--shadow-amber-sm`; `--color-surface-alt` value-aligned `#EDE0C4`→`#EFE2C6` = canonical "crème+"; confirmed `--color-line`=bordure / `--color-muted`=sépia already board-exact). Fraunces (italic 700) loaded as a raw `<html>` var only (NOT in `@theme` → no `font-fraunces` utility); the SOLE application path is the new `Display` primitive whose closed `kind` union ('listo'|'casi'|'count') is the structurally-enforced allowlist — no usages ship yet. Canonical `Button` (4 variants × 3 states, 14px block / 999 pill radius rule) + the two brown-`bg-ink` primaries unified to amber NOW (`DrillResult` "Suivant →", `LoadingIdiom` "Voir la fiche" — retiring the v0.5.1 ink-CTA pilot); all other already-compliant CTAs defer to their clusters. Selection treatments centralized in `selection.ts` (`SELECTION_ACTIVE` momentary / `SELECTION_PERSISTENT` persistent); the one global instance wired now = the nav active tab. Nav §05 paint: active pill routed through `SELECTION_ACTIVE` (gains `shadow-amber-sm`, `text-white`→`text-ivory`); IA/behavior unchanged; one-line avatar swap-point comment left for the asset-gated 32px Animando crop. OUT (cluster-deferred): ¡Casi! screen, StatusPill §06 recolor/merge/3-dot gauge, per-screen button/selection migration, nav IA restructure, per-screen Paco placements. Prior: v0.7.1 (M5.4b — `/words` per-word actions: swipe-to-reveal delete + a layout-level deferred-delete/undo-toast primitive (set-shaped for the M5.4c bulk slice; survives detail→list nav) + a single-word `DELETE /api/words/[id]` route (RLS + FK-cascade verified) + Relearn on the detail page (`reset-schedule` = reschedule-only, so no confirm modal). Bulk/multi-select is M5.4c, out of scope). Prior: v0.7.0 (M5.4a — `/words` list scale: forgiving free-text search (lowercase + NFD diacritic-strip, ñ→n folds, matches the Spanish word + both glosses via the pure tested `lib/word-search.ts`) + load-on-scroll progressive append (IntersectionObserver, initial 40 / +30, cap resets on filter/search/sort change) — all client-side over the already-fetched list; NO schema / NO API route / NO server pagination). Prior: v0.6.7 (M5.3c — the verb-conjugation drill, Paco's first game mode: a self-contained practice flow at `/drill` (Home `DrillCard` → Setup → 10 prompts, prompt→result manual-advance → Recap), gated on ≥5 trusted deck verbs; all logic in the pure tested `lib/drill.ts` (pool/unlock gate, prompt builder, the single swappable `triggerFrame` seam, strict grading via the existing `classifyVerbBlank`, deterministic teaching line via `analyze`/`paradigm`); reuses the review écriture input `AnswerBlank` + desktop `AccentBar` + the `ResultReveal` Paco-mood family + the v0.6.6 `ConjugationGrid` (new `buildConjugationGridForTense` builder) for the "Voir la conjugaison" hint sheet; drill prefs (tenses + person scope) persisted to `profiles`; NO FSRS impact (never writes review_logs/review_cards). Reflexives + untrusted verbs excluded from the pool. Paco PNGs compressed (~1.5 MB → ~110 KB each). Prior: v0.6.6 (M5.3b conjugation-table half — the frame-5 lemma-interstitial conjugation grid: the `/add` interstitial for an inflected verb form now shows the 2×3 paradigm of the typed form's tense with the typed cell in amber, gated on `canDisplayParadigm` so it never shows a guessed paradigm; tested pure helper `lib/conjugation-grid.ts` + reusable `ConjugationGrid` component; in-deck review-status line via optional `lemma_reps`/`lemma_due` on enrich; the Astuce-de-Paco /review hint stays a separate slice). Prior: v0.6.5 (two review/add hygiene bugs from v0.6.4 smoke-testing: result-screen example now threads the exercise's `picked.example` via a tested `resultHintExample` helper; reflexive add corrects a malformed proclitic — `"ti acuestas"`→`"te acuestas"` — via the deterministic `correctProcliticReflexive`). Prior: v0.6.4 (conjugator hardening — standalone slice ahead of M5.3b: systemic imperative-table-bypass fix for all 16 irregulars, stem-change expansion, `canDisplayParadigm` display guard + self-verifying golden-fixture harness). Prior: v0.6.2 + v0.6.3. v0.6.3 (écriture-polish — accent-homograph masking denylist, clitic-aware reflexive masking → coherent cloze-MCQ + lemma backfill, reps example rotation, desktop accent-row, prefer-coherent example selection). Prior: v0.6.2 (post-M5.3a review-format coherence — token-aware spellcheck gate for multi-word adds, verb cloze-MCQ gated on stored-in-form coherence via `chooseQcmCue`, écriture answer-revealing word header removed). Prior: v0.6.1 (M5.3a — conjugation substrate + fill-in-blank matching fix + in-question hint). Prior: v0.6.0 (review rework close — slice 3 bilan + QCM result-state alignment). Prior: v0.5.6 (review rework slices 1–2 — focus-mode + write-in result card + rating restyle + MCQ reveal), v0.5.5 (M5.2b — Dictionnaire personnel), v0.5.4 (M5.2 — Top pill-tab nav), v0.5.3 (M5.1 — Discovery mode), v0.5.2 (M5.0c — Home + word-list editorial: `/words` list, editorial Home top with effort estimate, end-of-session recap, shared status/familiarity renderers, list-row redesign), v0.5.1 (M5.0b — add-flow loading polish), v0.5.0 (M5.0a — honest status taxonomy), v0.4.3 (M4.3 — Google Cloud TTS), v0.4.2, v0.4.1, v0.4.0.

## What Paco is

A Spanish vocabulary learning app for French speakers (A2-B1 level). User adds Spanish words; the app generates bilingual definitions, contextual examples, confusable distractors, and lemma metadata via Anthropic. Words enter an FSRS spaced-repetition schedule. Daily review sessions mix fill-in-blank and multiple-choice formats with auto-rated answers the user can override.

The product is mobile-first, French UI, designed for the user (Omar) to actually use daily — not a demo or portfolio piece.

## Current state

- **Live URL:** https://spanish-vocab-lyart.vercel.app (Vercel hobby tier, free)
- **Repo:** https://github.com/OmarGhb/spanish-vocab (name unchanged — only product naming/visual is "Paco")
- **Latest tag:** v0.8.9 (M5.6 — **Color theming**, a cross-cutting foundation: 4 palettes (Sépia default · Ardoise · Indigo · Nuit/dark) via CSS custom properties on a root `data-theme`. Phase 0 = the `@theme inline`→non-inline split (fonts stay inline for their next/font var-chain; colors emit `var(--color-*)`), Sépia byte-identical. `[data-theme]` blocks set 13 mapped roles + 9 designer-specced legibility tokens + 6 accent-family `color-mix` tints; success/danger keep their role (Nuit inverts to bright dark-ground values, rating rungs track the accent); Nuit shadows darken via `--tw-shadow-color`, on-accent flips via `--color-ivory`. ACTIVE "Thème de couleur" swatch picker; persistence via `profiles.theme` + an SSR cookie-mirror on `<html>` (FOUC-free). Suite 451; smoke-tested live-switch + Nuit). Prior: v0.8.8 (M5.5i — **Profil / Préférences / Compte** surface at `/account`: three live `profiles` controls (Cartes par session 10–50 read by both `/review` cap consumers · Lecture auto gating a new `ResultReveal` autoplay · Vitesse Lent/Normal/Rapide → `playbackRate` 0.833/1.0/1.111 over the baked-0.9 cache, Normal=today); stats strip with révisions sourced from **`Σ review_cards.reps`** (NOT `count(review_logs)` — the logger is broken); Compte (email, change-password reauth flow, type-to-confirm delete with total-word copy, logout) + live legal; reusable `components/form/` auth foundation; surfaced the `review_logs` insert bug (`word_id`→`card_id`, separate commit) + the no-backfill gap; suite 446; **tag the docs-close commit after smoke-test**). Prior: v0.8.7 (M5.5h — Conjugaison drill repaint: the `/drill` flow re-skinned onto the v2 board, presentation-only + the one new tested `tenseLabel` helper (single Spanish tense-name source for chips/headers/cue/teaching-line/bilan); DELIBERATE Spanish tense names routed through `tenseLabel` (grid headers inherently Spanish · reinforces the learning frame · consistent with the M5.5g row-gloss call) with `Pretérito perfecto simple`→`Pretérito indefinido` app-wide incl. shared `ConjugationGrid.labelEs`; `¡Casi!`→ink (§6); `Voir la conjugaison` blanks the asked cell; canonical `Button`; shared `AnswerBlank` caret-on-focus + bigger tap area; Enter advances the result; DECLINED chip horizontal-scroll/edge-fade (flex-wrap, forward-compat); DEFERRED the Accueil entraînement card (Home pass); suite 428; smoke-tested clean). Prior: v0.8.6 — pending the device smoke-test (M5.5g — Dictionnaire cluster design-pass: nav pill · Home card · locked screen · A–Z index · unlock celebration re-skinned onto the v2 board, M5.2b logic preserved; unlock reveal relocated to review-end with flip-on-show once-guard + Home safety-net; live milestone count; shared `UnlockTakeover`; `Display` +`'milestone'`; row gloss stays Spanish as a recorded decision; **tag after the device smoke-test**). **v0.8.5 (M5.5f) is committed (`c43127c`) but not yet tagged — tag it once the device re-test is confirmed clean, then this.** Prior: v0.8.5 (M5.5f — Écriture context/hint model + QCM instruction: always-shown context removed both types; 3-tier penalized Indice (non-verb first-letter/ES-definition/scramble · verb form/blanked-table/scramble, degrade-safe; `lib/scramble.ts`); rating cap by hints (`HINT_CAP`, `computeRating` `hintLevel` replaces `hintUsed`, écriture-only, no double-count); `ConjugationGrid` three-way reuse (`blankTarget`/`infinitive` props); QCM eyebrow; countdown untouched; suite 424; **tag after the device smoke-test**). **v0.8.4 (M5.5e) is committed (`ddf548c`) but not yet tagged — tag it once the device re-test is confirmed clean, then this.** Prior: v0.8.4 (M5.5e — Review cluster design-pass: `/review` re-skin; Fix#1 conditional écriture cue (verb `infinitif·tense·person` Spanish via `lib/review-cue.ts`, non-verb `posAbbrev·gloss`, never the lemma); Fix#2 ¡Casi! neutral ink incl. verb-form face (verdict-only — `computeRating` untouched); Fix#3 rating amber-gradation no-dots (`--color-amber-pale`); NEW auto-advance countdown (`AUTO_ADVANCE_MS`, JS-driven neutral bar, one-tap-advance/pause=stop/no captions, reduced-motion numeric); the re-skinned ① entry card **relocated to Home** (`Display` count; `/review` auto-starts; Durmiendo passive); crash fix (`lib/bilingual.ts` normalizes bilingual fields in shared `mapReviewRow`); `Display` union extended for all verdict faces; ⑥ "Sus du 1er coup"; suite 416; **tag after the device re-test**). Prior: v0.8.3 (M5.5d — Discovery cluster design-pass: `/discover` re-skin; ① grid + §04 selected tint + the "Pour toi" non-functional placeholder shell (adjacency=M5.1b + A2–B1=content-gate, deferred); ② generation reuses the /add choreography via the shared `LoadingChecklist` (extracted; `IdiomCard` shared); ③④ deck re-skin + inline `posAbbrev` (speaker omitted per brief); ⑤ swipe-color role fix (Je connais=sage / À apprendre=amber) + `SwipeCard` stamp/wash/lift; ⑥ Feliz bilan + Durmiendo exhausted; no FSRS/generation change; **tag the final commit after the device smoke-test**). Prior: v0.8.2 (M5.5c — Add-flow cluster design-pass: `/add` re-skinned across the six boards; the loading active-phase animation finally wired (board keyframes + honest reduced-motion fallback); ¡Listo! → Fraunces (first `Display` consumer); rotating curated input placeholder; ④ compact duplicate screen (replaces full-fiche; reset-schedule → detail ⋮) + ⑥ single (confirmed) / multi (honest in-progress) success screens; `WordRow` optional-link reuse; additive plumbing — `save` returns id, `enrich` returns card state/stability; no taxonomy/FSRS change; **tag the final commit after the device smoke-test**). Prior: v0.8.1 (M5.5b — Words cluster design-pass: §06 StatusPill render-layer label-map (Relearning→À réviser, En cours/En apprentissage merge) + `getMasteryGauge` 4-dot gauge `{0,1,2,4}` + `MasteryGauge` (replaces `FamiliarityMeter`) + WordRow re-anatomy + `/words` filter/sort/search re-skin + 3 mascot empty states + flush one-rectangle `SwipeRow` + dark undo toast + detail ⋮ overflow menu + labeled back + two-way FR reveals; presentation-only over the unchanged taxonomy; token reconciliation; tag the docs-close commit after smoke-test). Prior: v0.8.0 (M5.5a — design-system foundation: additive board-§01 tokens + Fraunces-via-`Display` allowlist + canonical `Button` + `selection.ts` treatments + nav §05 paint; brown-`bg-ink` primaries unified to amber; tag the docs-close commit after smoke-test). Prior: v0.7.1 (M5.4b — `/words` per-word actions: swipe-to-reveal delete + deferred-delete/undo primitive + single-word DELETE route + detail-page Delete/Relearn; tag the docs-close commit after smoke-test). Prior: v0.7.0 (M5.4a — `/words` list scale: client-side free-text search + load-on-scroll progressive append; pure tested `lib/word-search.ts`; tag the docs-close commit after smoke-test). Prior: v0.6.7 (M5.3c — verb-conjugation drill, Paco's first game mode; `/drill` flow + `lib/drill.ts` tested helpers + `profiles` drill prefs + `buildConjugationGridForTense`; tag the docs-close commit). Prior: v0.6.6 (M5.3b conjugation-table half — frame-5 lemma-interstitial conjugation grid; display-guarded, reusable component + tested helper). Prior: v0.6.5 (two review/add hygiene bugs — result-screen example threading + reflexive-clitic correction). Prior: v0.6.4 (conjugator hardening — standalone slice ahead of M5.3b). Prior: v0.6.3 (écriture-polish — accent-homograph masking denylist, clitic-aware reflexive masking + lemma backfill, reps example rotation, desktop accent-row, prefer-coherent example selection; tag the docs-close commit). Prior: v0.6.2 (post-M5.3a review-format coherence — token-aware spellcheck gate, verb cloze-MCQ stored-in-form gate via `chooseQcmCue`, écriture word-header removal). Prior: v0.6.1 (M5.3a — conjugation substrate + fill-in-blank matching fix). Prior: v0.6.0 (review rework close) — slice 3 bilan redesign + Encore-continues-session + QCM result-state aligned to the écriture card (tag the milestone-close commit). Prior: v0.5.6 (review rework slices 1–2 — focus-mode + write-in/MCQ result cards + rating restyle) @ 633aae0, v0.5.5 (M5.2b — Dictionnaire personnel)
- **Recent migration:** `supabase/migrations/20260621000000_theme_pref.sql` (M5.6 — `profiles.theme`) — applied for smoke-test; **must be applied to live Supabase on deploy** (the `(app)` layout selects it). Prior: `20260612000000_settings_prefs.sql` (M5.5i — cards_per_session / autoplay_audio / playback_speed), also apply-on-deploy; `20260529000000_discovery_words.sql` (M5.1), applied to live.
- **Real usage:** Omar uses it daily; deck at ~72 words and growing
- **Active dev environment:** Mac, `/Users/gahbicheomar/spanish-vocab`, VS Code + integrated terminal, SSH key auth to GitHub

## Tech stack

- **Frontend:** Next.js 16 (App Router) + React 19 + TypeScript strict + Tailwind 4
- **Database/Auth:** Supabase (Postgres + Auth + RLS-protected); region eu-west-3 or eu-central-1
- **AI:** Anthropic API (`claude-sonnet-4-6` for enrichment — generates definition + lemma + form_annotation + examples + distractors in a single call)
- **Spaced repetition:** ts-fsrs library
- **Spellcheck:** `dictionary-es` + `nspell` (~656k accent-preserving inflected forms, loaded server-side at module init), `fastest-levenshtein` for fuzzy matching
- **Audio:** Google Cloud TTS Neural2 (`es-ES-Neural2-F`, MP3 @ 0.9× rate), generated at enrichment time, cached in Supabase Storage (`word-audio` public bucket). Browser SpeechSynthesis retained as fallback for words with no `audio_urls`. `@google-cloud/text-to-speech` marked `serverExternalPackages` in next.config (Turbopack breaks the bundled gRPC client — see M4.3 notes).
- **Testing:** vitest (^4) — added in M5.0a; first test runner in the repo. Scoped to pure-logic modules (no Next.js / browser APIs). `npm test` = `vitest run`.
- **Deploy:** Vercel auto-deploy on push to main; v0.x.x git tags as version markers

## Schema (Supabase Postgres, all RLS-protected)

- `words` (id, user_id, word, definition JSONB `{es, fr, pos?, gender?}`, examples JSONB `[{es, fr}]` (default `'[]'`), distractors JSONB (default `'[]'`), lemma TEXT nullable, form_annotation TEXT nullable, **audio_urls JSONB nullable `{es_ES: url}`**, **origin TEXT NOT NULL DEFAULT 'manual'** (`'manual' | 'discovery'`), **discovery_status TEXT** (`'pending' | 'kept' | 'promoted' | 'known'`; CHECK `origin='discovery' OR discovery_status IS NULL`), **discovery_topic TEXT**, **discovery_claimed_at TIMESTAMPTZ**, created_at)
- Index `words_discovery_idx (user_id, discovery_topic, discovery_status) WHERE origin='discovery'`
- `review_cards` — FSRS state per word: due, stability, difficulty, elapsed_days, scheduled_days, reps, lapses, state, last_review, learning_steps. **`UNIQUE (word_id)`** (M5.1 duplicate-card backstop — note the PostgREST embed-cardinality consequence in SESSION_PROTOCOL)
- `review_logs` — full review history per card: **`card_id` (uuid, NOT NULL → review_cards)**, user_id, rating, reviewed_at, scheduled_days, time_ms, hint_used. ⚠️ **Empty across all users since inception** — `/api/review` inserts `word_id` (a non-existent column) instead of `card_id`, so every insert fails 42703 silently (fix is a separate commit; see "From M5.5i"). Derived review counts read `Σ review_cards.reps` instead.
- `add_events` — instrumentation for the lemma flow (5 event types, see M3.3 notes)
- `profiles` (M5.2b) — **per-user settings table**: `user_id` PK → `auth.users` (CASCADE), `dictionary_unlocked BOOLEAN NOT NULL DEFAULT false`, **`drill_tenses JSONB NOT NULL DEFAULT '["presente","preterito","imperfecto","futuro"]'`** + **`drill_person_scope TEXT NOT NULL DEFAULT 'singular' CHECK (singular|all)`** (M5.3c drill prefs), **`cards_per_session INT NOT NULL DEFAULT 20`** + **`autoplay_audio BOOLEAN NOT NULL DEFAULT true`** + **`playback_speed TEXT NOT NULL DEFAULT 'normal' CHECK (lent|normal|rapide)`** (M5.5i Profil settings, migration `20260612000000_settings_prefs.sql`), **`theme TEXT NOT NULL DEFAULT 'sepia' CHECK (sepia|ardoise|indigo|nuit)`** (M5.6 color theme, migration `20260621000000_theme_pref.sql`), `created_at`. RLS own-row (select/insert/update) covers all columns. Holds the sticky dictionary-unlock flag + the drill's last-used tense/person + the Profil live-control prefs + the color theme.
- All FK use `ON DELETE CASCADE` so account deletion removes everything cleanly

## Architecture decisions worth remembering

### From M2-M3 (still relevant)

- **Spanish-first definitions** (M2.5): definition stored as `{es, fr, pos?}` JSONB. Spanish primary in UI. French behind a "Voir en français" reveal that counts as a hint in MCQ.
- **Two-step add flow** (M3.1): enrichment is reviewed before saving. `/api/words/enrich` checks the user's deck first; if word exists, returns stored data (no Anthropic call). `/api/words/save` actually inserts. `/api/words/reset-schedule` for due-now duplicates.
- **Wordlist-driven spellcheck over LLM** (M3.2): bundled Spanish dictionary loaded once at module init. No LLM in the spellcheck path.
- **Lemma normalization via Anthropic** (M3.3): the inverse of the M3.2 lesson. When the LLM call is already happening, layering on a separate dataset is overkill. Lemma + form_annotation come back as fields on the existing enrichment response.
- **Form annotation, not duplicate definitions** (M3.3): when a user types "comieron," Anthropic generates the definition for the lemma (*comer*), and a separate `form_annotation` field labels the conjugation (`"Comer — 3ª pers. plural, pretérito perfecto simple"`).
- **Schema cache reload in migrations** (M3.3): every migration that adds a column ends with `NOTIFY pgrst, 'reload schema';` to prevent PGRST204 errors after migrating. Was critical for M4.3's `audio_urls` column.
- **Save-route error logging** (M3.3): API routes `console.error` underlying Supabase errors instead of swallowing them under generic 500s.
- **Server-only admin client** (`lib/supabase/admin.ts`): hard `import 'server-only'` guard. Used only by account deletion. Backfill scripts create their own service-role client inline.
- **AbortController plumbing**: navigation away during enrichment cancels the request all the way through to Anthropic SDK.
- **Fire-and-forget event logging** (M3.3 phase B): the five lemma events log via `void fetch().catch(console.error)` — no await, no UI feedback.
- **Bulk distractor add** (M2.4): per-word parallel Anthropic calls via `Promise.all`. Background processing. Toast feedback.

### From M4 (v0.4.0)

- **`/words/[id]` detail view**: server component for shell + data fetch with RLS-protected `maybeSingle()` + `notFound()` on null (no existence leak). Client component (`WordDetailContent`) for interactive bits.
- **`AudioButton`**: `useSyncExternalStore` for SSR-safe support detection. Two-tier voice fallback. `speechSynthesis.cancel()` before every `speak()` for iOS Safari. Rate 0.9. **Superseded in M4.3 by cloud TTS; this is now the fallback-only branch.**
- **Status pill mapping** (M4 original — superseded by M5.0a's `lib/word-status.ts`): state-based mapping with the "Mémorisé too broad" flaw that M5.0a fixed.
- **Stats line** uses calendar-day delta via midnight normalization, not 24h delta.
- **M3.3 placeholder rewire**: `lemma_word_id` threaded through to the "Ouvrir «lemma»" button → `/words/[id]`.

### From M4.1 (v0.4.1)

- **Definition backfill via fetch-all + filter-in-JS**, not PostgREST `.or()`. At 51 words, client-side filtering is trivial and unambiguous. **Lesson:** when a script-side filter needs a complex SQL WHERE involving JSON shape checks, fetch-and-filter client-side is more reliable than translating to PostgREST `.or()`.
- **The pre-M2.5 weak data shape** was `{"es": "", "fr": {"es": "<actual Spanish text>"}}` — a nested object instead of a flat bilingual string. All 47 broken rows shared this. Repaired via full re-enrichment.
- **Script env-var loading**: standalone tsx scripts don't auto-load `.env.local`. Prefer `import { loadEnvConfig } from '@next/env'; loadEnvConfig(process.cwd())` at script top.
- **Shared enrichment logic** already in `lib/anthropic.ts` (`getWordData`).
- **DÉFINITION hide-when-empty guard** in `WordDetailContent.tsx`: `{(defEs || defFr) && (...)}`.
- **Styled 404 page**: `app/not-found.tsx`, sad Paco PNG at `/paco-sad.png`.

### From M4.2 (v0.4.2)

- **`<StickyActions>` shared primitive**: `position: fixed` at `bottom-16` (above NavBar), `z-[39]`, cream backdrop, iOS safe-area via `paddingBottom: calc(1rem + env(safe-area-inset-bottom))`. Consumers add `pb-36`. **(Superseded by M5.2: `bottom-0` / `z-30` / `pb-20` now there's no bottom NavBar — see From M5.2.)**
- **Pattern decision**: sticky bottom for long-scrolling primary-action surfaces; natural flow for short surfaces.
- **LoadingIdiom reorder**: Paco status card + copy FIRST, idiom card BELOW.

### From M4.3 (v0.4.3)

- **TTS pipeline**: `lib/tts.ts` (server-only). `es-ES-Neural2-F`, MP3, `speakingRate: 0.9`. Deterministic Storage filename, `upsert: true` (idempotent). Duplicate adds share one object.
- **Parallel TTS at enrich**: cache-miss path runs Anthropic + input-word TTS in one `Promise.all`; a second `Promise.all` batches lemma deck-check + lemma TTS. TTS failure non-fatal everywhere.
- **`serverExternalPackages` for Google Cloud SDKs (expensive lesson)**: Turbopack breaks the bundled gRPC client (`Error: undefined undefined: undefined`). Fix: top-level `serverExternalPackages` (NOT `experimental.serverComponentsExternalPackages`). **General rule: any `@google-cloud/*` SDK in a Next route → mark external first.**
- **Service-account JSON env transport**: `.env.local` needs single quotes (dotenv strips them); Vercel does NOT strip — paste bare JSON. Base64-encoding is the durable fix if it recurs.
- **IAM Editor trade-off**: `roles/texttospeech.user` doesn't reliably appear in the picker; granted broad Editor to `paco-tts`. Acceptable (server-only key, single-user project). Tighten via gcloud CLI — backlog.
- **AudioButton dual-branch**: `audioUrl` present → HTMLAudioElement; absent → SpeechSynthesis fallback.
- **Backfill**: `scripts/backfill-audio.ts`, concurrency 3, idempotent. 51 words backfilled.

### From M5.0a (v0.5.0)

- **Canonical status primitive** (`lib/word-status.ts`): pure derivation module, no I/O, no Next.js, no Supabase, no `server-only` (safe for Server and future Client consumers). Single source of truth — the old inline `statusPill` in `/words/[id]` is removed.
- **Two-derivation model** (load-bearing): `getWordStatus(card|null)` is action-priority — a due-now card reads "À réviser" even at high stability. `isMemorized(card|null)` is stability-based and ignores transient due-ness — a stability≥30 card that is currently due is still "memorized" for counting/filtering. M5.0c counts/filters consume `isMemorized`, not the pill label. A regression test locks this divergence.
- **6-state stability partition**, first-match precedence (every card resolves to exactly one): (1) no card OR `State.New` → "Nouveau"; (2) `State.Relearning` → "À rappeler"; (3) `due <= now` (instantaneous timestamp) → "À réviser"; (4) `stability >= 30` → "Mémorisé"; (5) `stability >= 7` → "En cours"; (6) else → "En apprentissage".
- **Nouveau-before-due** is deliberate: new cards are due immediately and would otherwise read "À réviser".
- **Tone discipline**: only `stability >= 30` (Mémorisé) gets the sage/success token. Nouveau / En cours / En apprentissage are neutral. Chip color carries only act / done / not-done. No gamified progress bar.
- **State.Learning has no special branch** — falls through to the stability bands. A test locks the fall-through.
- **`isMemorized` excludes `State.Relearning`** regardless of stored stability.
- Constants: `MEMORIZED_STABILITY_DAYS = 30`, `LEARNING_STABILITY_DAYS = 7`.
- `stability` added to the `/words/[id]` Supabase select + `CardRow` type (no schema change).

### From M5.0b (v0.5.1)

- **¡Listo! is a hold, not auto-advance.** The post-enrichment ¡Listo! screen reuses the pre-existing ready-phase hold (handleReveal tap → revealed) — zero new state-machine surface. Driven by the "no auto-flip" watch-item.
- **Existing-word routing keys on `data.status` directly**, NOT the derived DeckStatus. The existing-word guard early-returns to `revealed` before any `ready` is set, so ¡Listo! is unreachable for a deck hit BY CONSTRUCTION. Replaced a buggy `else → {tag:'new'}` fallback that misclassified deck hits when `data.dueDate` was absent.
- **dueDate hygiene.** `const dueDate = data.dueDate ?? new Date().toISOString()`. No non-null assertion on dueDate (a `!` would surface "prochaine révision dans NaN jour(s)").
- **The orphan misdiagnosis (institutional lesson — keep this).** The proposed root cause for the dueDate-absent flash — "a save-route double-failure orphans a `words` row with no `review_cards` row" — was plausible, code-cited, survived several review rounds, and was FALSE. The diagnostic `SELECT ... LEFT JOIN review_cards ... WHERE rc.id IS NULL` returned zero rows. The apparatus built on the phantom was ripped. The real protection is routing-on-`data.status`. Lesson: a confident, code-cited root cause is still a hypothesis until a diagnostic confirms it.
- **Loading-shell delay gate.** The entire loading branch is gated behind `LOADING_SHELL_DELAY_MS = 400` (renders null until the gate fires). Fast deck-only lookups resolve before the gate, so the loader never paints. Phase offsets `[1100, 1700, 2100]` relative to shell appearance; timer clears on unmount and on `status` leaving 'loading'.
- **PhaseChecklist.** Phase 4 (Phonétique) holds and never auto-completes; the loading → ready/revealed flip IS the data-wins transition. Label is **"Exemples"**, NOT "Trois exemples" (enrich schema is `examples: z.array().min(2).max(3)`).
- **¡Listo! preview card is `bg-card` (light), NOT `bg-surface-alt`.** Separation via subtle border + soft shadow; outer card low-weight (flattened nesting).
- **Ink CTA pilot.** The ¡Listo! "Voir la fiche" button is `bg-ink text-page` (existing tokens). Scoped as a pilot; system-wide dark-button propagation is a separate, deferred decision.
- **Extension-hydration.** `suppressHydrationWarning` on both `<html>` and `<body>` (Grammarly + Scribe land on different elements).

### From M5.0c (v0.5.2)

- **word-status.ts extended**: `getFamiliarity(card) → 0|1|2|3` (New → 0; stability ≥30 → 3, ≥7 → 2, else → 1; no state special-casing beyond New) and `isDue(card)` (instantaneous `due ≤ now` — NOT the calendar-day normalization the stats line uses). 15 new tests incl. the divergence lock: a due, stability-≥30 card is `getWordStatus` "À réviser" AND `getFamiliarity` 3 at once.
- **Shared renderers extracted** to `app/(app)/`: `StatusPill` (the SOLE `getWordStatus` renderer — `/words/[id]`, the list, and the Home preview all consume it; finally resolves the M5.0a→M5.0c list/detail inconsistency), `FamiliarityMeter`, `WordRow`. Pill = `getWordStatus`; dots = `getFamiliarity` — independent, may diverge.
- **`/words` list route.** Entry point: tapping the Home "TA COLLECTION" block (superseded by M5.2 — `/words` is now also a first-class "Mes mots" pill; the block stays as a redundant entry). Filters Tous / À revoir (`isDue`) / Mémorisés (`isMemorized`). Sorts Alphabétique (`localeCompare 'es'`), Date (desc, default), Familiarité (least familiar first: `getFamiliarity` asc, stability asc within a level).
- **Home editorial top.** State A: amber review CTA — "N mots à revoir", "≈ X min" + (i) infotip, "Commencer la révision". Effort estimate = median of the most recent 200 `review_logs.time_ms` (>0); flat 12000 ms/card fallback under 20 logs; `minutes = max(1, round(dueCount × perCardMs / 60000))`. State B: "Tout est à jour", no CTA/estimate. Removed the hand-rolled `statusLabel(reps, due)` and the pinned bottom action bar. Last-10 preview kept below the collection line.
- **Collection line spec**: "TA COLLECTION" eyebrow (Lora 700 / 10px / 0.16em / uppercase) + "N mots enregistrés" value (Lora 400 / 14px) on one row, separated from content above by a 1px hairline. Designer hexes mapped to existing tokens — no raw hex. The bare "→" see-all cue was intentionally dropped (felt crowded); the whole collection block is the tap target to `/words`.
- **End-of-session recap**: in-memory outcomes accumulated during the session (NOT re-queried from `review_logs`). Per-word ✓ (sage) / ✗ (terracotta) — a bounded correct/incorrect signal, NOT the status-pill taxonomy (the sage-only-for-Mémorisé rule does not reach here). Stats Révisés / Réussite (NaN-guarded) / Temps ("< 1 min" under 60 s). Conditional "Encore N mots à revoir" via a browser-client count (`review_cards` due ≤ now) taken after the final reschedule; hidden at 0. Session deck is fixed (single due≤now query, limit 20, index-only advance — no intra-session re-show), so append-per-rate yields no duplicate rows — confirmed and smoke-tested with an Again rating.
- **List-row redesign** (post-tag polish, folded into v0.5.2): `FamiliarityMeter` → left, vertical 3-dot column, filled bottom-up (ink filled / line empty). Word Lora 18px/700; definition italic, one line. Rows bordered (no shadow); due rows (`isDue`) get a tint background + accent border. A "N révision(s)" line under the pill (`reps`; singular 0–1, plural 2+). **`reps` is DISPLAY-ONLY metadata — it does not feed status** (status stays `getWordStatus`/stability; the révision count and the pill can diverge by design).
- **CTA-weight convention**: primary CTA buttons use bold text ("Commencer la révision", "Encore N mots à revoir").

### From M5.1 (v0.5.3)

- **Reuse `words` + flag (Model A).** Discovery words persist in `words`; a flag filters them out of every full-word consumer. Canonical filter: `origin='manual' OR discovery_status='promoted'`. Applied to: `/words` list, Home count + last-10 preview, `/words/[id]` (guard → `notFound()` for non-promoted), both `/api/words/enrich` duplicate checks (deck + lemma-collision), both backfill scripts. `/review` + Home dueCount read `review_cards` directly → safe by construction.
- **Lightweight generation, not full.** `getDiscoveryBatch(topic, exclude, signal)` → one Anthropic call returning compact `{word, fr, pos, gender, example:{es,fr}}`, zod-validated. `/api/discovery/generate`: resume-before-API (return existing `pending` rows, no call) → else generate, dedup against ALL seen words (any status), persist as `pending`. Empty-deck safe; × aborts through to the SDK.
- **`gender` is explicit.** `gender:'m'|'f'|null` stored inside `definition` JSONB → drives the el/la display article + the NOM·MASCULIN/FÉMININ eyebrow. The **bare** word is stored (`"mercado"`, not `"el mercado"`) so dedup matches manual adds; the article is display-only. (Flipped the original "derive from `pos`" plan — `pos` is too inconsistent to teach el/la from, and a wrong article is real miseducation.)
- **Enrich-on-keep, deferred + concurrency-safe.** `/api/discovery/decide` flips `pending`→`kept`/`known` per swipe (fire-and-forget; a failed decide leaves it `pending` → self-heals on resume). `/api/discovery/enrich` (fired on bilan-mount + grid-mount): per-chunk **atomic claim** via `discovery_claimed_at` (2-min stale recovery), reuses `getWordData` + `getAudioForWord` + `createInitialCard`, flips `kept`→`promoted` **only on success** (a half-enriched word never shows broken in `/words`); `UNIQUE(review_cards.word_id)` backstops duplicate cards; TTS failure non-fatal.
- **Reusable `SwipeCard` primitive** (`app/(app)/SwipeCard.tsx`): pointer + touch, drag threshold, tilt, stamp-opacity; buttons trigger the same outcome as swipes. Also unblocks the backlog swipe-to-delete/archive.
- **`DiscoverClient` phase machine** (grid → generating → deck → bilan / exhausted) rendered as a focused overlay; × exits and preserves `pending` rows for resume. (M5.2: overlay is `z-[60]`, above the new top nav `z-30`.)
- **Design:** discovery card is a portrait card filling the focused column (eyebrow top-left, word + gloss centered, example pinned to bottom; mid-swipe stamps sage "À APPRENDRE" right / terracotta "JE CONNAIS" left; bottom buttons amber "À apprendre" + terracotta-outline "Je connais"). Bilan: "Réviser maintenant" (primary) + "Retour à l'accueil" + tertiary "Découvrir un autre thème"; enrichment caption only when ≥1 word kept. Découvrir grid: portrait-ish tiles, rounded-square icon badge, "N MOTS" uppercase, Paco mascot beside the flush-left title.

### From M5.2 (v0.5.4)

- Top sticky pill-tab nav (`app/(app)/TopNav.tsx`) replaces the deleted bottom NavBar. `sticky top-0`, inside the `max-w-[430px]` column so nav + content share edges by construction. z below toast (40) and discover overlay (60).
- Layout: `(app)/layout.tsx` is `flex-col`, TopNav first + `flex-1` children (six wrappers switched `min-h-screen` → `flex-1`).
- Corner buttons vs pills: home circle + account avatar never show active state; the 5 pills carry it. Accueil is a pill AND the circle (both → `/`) so a tab is always selected — signals the row is nav.
- Active match: `path === href || path.startsWith(href + '/')` — exact for leaves, section-active for `/words/[id]`, safe for Accueil (`'//'` never false-matches; no bare `startsWith('/')`).
- StickyActions ripple: `bottom-16` → `bottom-0`; `z-[39]` → `z-30`; `/add` `pb-36` → `pb-20` (drop 64px NavBar height, keep bar clearance); toast `bottom-36` → `bottom-24`.
- Top-only nav (decision): no second bottom bar — two navs for the same destinations is an anti-pattern, and the bottom thumb-zone is reserved for in-task review actions. Don't relitigate.
- Pill styling: Lora serif bold labels (deliberate brand-chrome departure from Inter-for-UI); inactive = card fill + subtle accent-tinted border with the icon in that same tone; active = amber fill + white. No divider under the nav (tried, removed).

### From M5.2b (v0.5.5)

- **Personal dictionary** (`/dictionary`): an A–Z reference of the user's **memorized words only** — `isMemorized(card)`, the strict memorized SUBSET of `/words` (reuses the exact `origin.eq.manual,discovery_status.eq.promoted` filter, so no word is in one but not the other). Deliberately bare vs `/words`: NO status pills, familiarity meter, due-tint, reps, filters, or sorts — just word + gloss + audio. Content is LIVE (words enter/leave as mastery shifts). **Row gloss = Spanish (`definition.es`) — recorded decision (corrected in M5.5g):** the spec/board mock drew a French gloss; the row shows the Spanish sense because the dictionary is an index of words the user has **already memorized** — the Spanish reinforces the learning frame, and the French stays available at the word fiche. (NOT the earlier "deliberate Spanish-first override / standing rule" framing — that attribution was wrong; this is the honest reason.)
- **A–Z bucketing** (`lib/dictionary.ts`, pure + unit-tested): Ñ is its own bucket AFTER N (checked before accent-folding, else NFD would fold it into N); accented initials fold to base letter for BUCKETING ONLY (display keeps accents); non-letter → `'#'`. iOS-contacts jump rail (right edge) with tap + pointer-drag scrubbing that snaps to the nearest present letter; empty letters greyed/disabled.
- **`profiles` table — sticky unlock.** `DICTIONARY_UNLOCK_THRESHOLD = 10` memorized words. ACCESS is governed SOLELY by `dictionary_unlocked` — once true it **never resets**, even if the memorized count later drops below 10 (locked↔unlocked is driven by the flag, not the live count).
- **Unlock flip is a Server Action** (`app/(app)/dictionary/actions.ts` `syncDictionaryUnlock`), triggered from a mounted `<UnlockSync/>` on Home + `/dictionary` — **NOT an RSC-render side effect**, so a route prefetch can't flip the flag. `getDictionaryState` (`lib/dictionary.ts`) is **read-only**. On the false→true transition the action `redirect`s to the one-time interstitial (`/dictionary/unlocked`).
- **Dictionary IS a pill destination** (6th pill, last): unlocked = normal pill → `/dictionary`; locked = dashed border + lock glyph → `/dictionary` (which renders the locked screen). The locked pill never shows active styling. **Supersedes the M5.2b roadmap open question ("its entry point — not one of the five pills").** Layout does a cheap single-row flag read for the pill state.
- **Accepted v1 limits** (single-user, harmless): (1) the interstitial re-shows on a direct visit / browser-back — the page renders whenever the flag is set; `router.replace` on the CTA mitigates the normal-flow back; (2) a double-show race if two loads cross the threshold simultaneously. Revisit with an `unlock_celebrated` column only if it ever matters.
- **Dependency-free confetti** (`Confetti.tsx` + `confetti-fall` keyframe in globals.css): deterministic (SSR-safe, no `Math.random`), reduced-motion-aware. No new dep.

### Review focus-mode (review rework, slice 1)

- **`FocusModeContext`** (`app/(app)/FocusMode.tsx`): a `FocusModeProvider` wraps `TopNav` + children in `(app)/layout.tsx`; `TopNav` returns `null` when `focus` is set. An active `/review` session sets `setFocus(true)` for the whole session lifetime — suppressing the app nav from the first card **through the bilan** (slice 3 extended this from the original answering-only `setFocus(!done)`), restored on unmount. Only the 0-due "Tout est à jour" empty-state (a separate component in `page.tsx`, not `ReviewSession`) keeps the nav. Chosen over a route group (auth-layout duplication) and over a discovery-style `fixed inset-0` overlay (an overlay blocks the soft keyboard from scrolling the answer field into view). The active session adds `paddingTop: max(1.25rem, env(safe-area-inset-top))` since the nav no longer supplies the notch inset.
- **Answer-field keyboard visibility** (`FillInBlank.tsx`): autofocus on mount (no programmatic keyboard-open — iOS needs the gesture) + `scrollIntoView({block:'center'})` on `onFocus` and on `visualViewport` `resize`, so when the keyboard opens the question + field stay visible without a manual scroll.

### Review result card + write-in mechanic (slice 2)

- **Answering state** (`AnswerBlank.tsx`): the answer is typed **inline into the blank** in the sentence. Mechanic = a real `<input>` overlaid transparently on a visible inline blank (`color`/`caret-color` transparent) — it captures keystrokes + raises the native keyboard **on tap** (still gesture-gated; no auto-open), while the span renders the typed value + a faux `caretBlink` caret. Self-contained so it can be reverted/iterated alone if iOS fights it. No active-field chrome (so the blinking caret reads as "tap to type", not keyboard-up). **Accents not handled** here (plain input) — near-miss grading cushions a missing accent; real fix is the custom-keyboard milestone.
- **Result card** (`FillInBlank.tsx`, three verdicts ¡Eso es! / ¡Casi! / ¡Uy!): Paco + exclamation (¡Listo! type convention at 38px; success/`warm`/danger) + per-verdict body (filled sentence / diff line / correct word + your answer) with letter-level highlighting from **`lib/worddiff.ts`** (`wordDiff` backtrace). Verdict = the **existing grading** via `classifyBlankAnswer` extracted from `lib/rating.ts` (shared by `computeRating` — no grading change). Quiet `fadeUp` cascade, reduced-motion-gated.
- **Kept FSRS / dropped design framing**: rating stays the 4 `RatingButtons` labels + suggestion ring (NOT the design's `1j/3j/7j/21j` interval chips). Hint slot built but filled with the card's example sentence ("Exemple"), not a generated conjugation hint (deferred to M5.3).
- **Rating button restyle** (slice-2 polish): dropped the amber per-button gradation (`bg-again/hard/good/easy`) for **uniform pills** — one consistent treatment across all verdicts: the suggested rating is a solid `accent` pill, the rest are `border-line` outline. **Enter advances with the suggestion** (window keydown in `RatingButtons`, mounted only on the result screen — the answering form is unmounted by then). Styling/affordance only; suggestion logic + `hintUsed` penalty unchanged.
- **Shared `ResultReveal`** (`app/(app)/review/ResultReveal.tsx`): the Paco + ¡Eso es!/¡Casi!/¡Uy! reveal extracted so **MultipleChoice now uses the same treatment** as FillInBlank (replaced its old "¡Correcto! Sigue así" light-green card — it read disconnected). MCQ is binary → correct/wrong only (no ¡Casi!); the correct option stays `ok`-tinted in the list. FillInBlank refactored onto the shared component (no visual change) so the two can't drift.
- **`--color-warm` (#E8A24A)** added to `globals.css` `@theme` for the close/¡Casi! accent (no inline hex). Header label + progress bar flip to `ok` on a correct écriture answer (`ReviewSession` tracks the verdict via `FillInBlank`'s `onResult`).
- **QCM result-state aligned to the écriture card** (post-slice-3 polish): (1) option tints swapped from the math-derived `bg-ok/10` · `bg-err/10` alphas to the **canonical tint tokens** `bg-ok-bg` · `bg-err-bg` (same hue + intensity as the écriture surfaces and the bilan ✓/✗ circles); correct keeps `border-ok text-ok`, wrong-picked `border-err text-err`, unselected stays `border-line text-muted opacity-50`. (2) The Paco `ResultReveal` verdict moved to the **TOP** (verdict → prompt+options → rating, matching écriture); the already-on-screen options don't remount, so only the verdict + rating fade in (rating wrapped in `fade-up` `0.18s` like écriture). (3) **Shared `RatingButtons` restyle (changes écriture too, by design):** the suggested rating is now an accent **OUTLINE** (`bg-card border-accent text-accent`), not a solid fill — a fill read as an active selection and a tap gave no feedback. The actual **selection** fills solid (`bg-accent text-white`); precedence is selection→fill, else suggested→outline, else neutral. A new local `selected` state + a `~220ms` (`ADVANCE_BEAT_MS`) `setTimeout` lets the fill paint before the card advances; Enter routes through the same `choose()` so it shows the beat too. Timer cleared on unmount; `aria-pressed` tracks the real selection. Helper text + Enter-validates unchanged; **no grading/scheduling change** — the `onRate` payload is identical, only delayed by the visual beat.

### Review bilan — end-of-session recap (slice 3)

- **Built to the high-fidelity Claude Design handoff** (the first prose mockup was a computed-style dump — useless; the second was a clean reference HTML + README with token/type specs). **Fixed vertical frame**, `done` branch root `flex-1 min-h-0 flex flex-col` (fills the full column — focus mode hides TopNav through the bilan, so the header carries its own `max(0.875rem, env(safe-area-inset-top))` notch padding): header / stats / list / footer; **only the word list scrolls** (`flex-1 min-h-0 overflow-y-auto`), the other three are `shrink-0`.
- **Header** — left-aligned, 56px `paco-feliz.png` + italic "¡Buen trabajo!" (Lora 27/700, `-0.01em`) + 13.5px "Session terminée" subline.
- **Stats card kept ABOVE the list — deliberate override of the handoff** (which puts it below). Omar's reasoning stands: summarize before per-word detail / reveal reads top-down. Rounded-2xl `bg-card border-line` card, 3 columns split by `border-hair`; numbers Lora 23/700, labels 10.5 uppercase muted. Computation unchanged (display-only).
- **Word rows** — continuous list, **hairline `divide-hair` between rows** (no per-row card chrome); 26px status circle with an inline **SVG check / cross** (`bg-ok-bg text-ok` / `bg-err-bg text-err` tint tokens) + word (Lora 17/700) + gloss (`truncate`, single-line ellipsis — the handoff mechanic, same visual outcome as the kept truncation, full gloss text preserved).
- **New token `--color-hair` (#E7DBC2)** added to `@theme` — the handoff's softer inner divider, lighter than `--color-line` (#D9C9A8, kept for the stats card's outer border). From the provided PT palette, not a fork (same pattern as slice-2's `--color-warm`). **Rendered at `divide-hair/60` · `border-hair/60`** (60% via `color-mix`, verified generated): a full-opacity 1px hair line antialiases faint in the downscaled desktop design preview but renders crisp/heavier on a retina phone, so the dividers were softened to read as subtly on device as they do in the reference.
- **Row stagger reveal** (Omar's add, handoff is silent — kept): each row `className="fade-up"` + inline `animationDelay: i*35ms`. Reuses the **proven** `.fade-up` keyframe (globals.css, reduced-motion-gated, shipped in `ResultReveal`) — NOT a Tailwind `motion-safe:*` utility, deliberately, because the PhaseChecklist `motion-safe:animate-pulse` was specced that way and never rendered. Inline `animation-delay` longhand overrides the stylesheet `animation` shorthand's delay; under `prefers-reduced-motion` the keyframe rule is inert, so rows are simply visible. Header + stats don't stagger.
- **"Encore N" now continues the session in place** (was broken). The old `<Link href="/review">` did nothing — navigating to the route you're already on served a stale Router-Cache page, so the rescheduled deck never came back. Replaced with a `<button>` → `handleContinue`, which client-fetches the next due batch (`fetchDueCards`, same query/order/limit as the server `page.tsx`), swaps `cards`, resets `index`/`verdict`, and clears `done` (re-entering focus mode via the existing done-effect). **Outcomes accumulate** across batches so the recap is cumulative for the whole session. `cards` is now `useState` (init from props). Double-tap guarded by a `continuing` flag (button → "Chargement…", disabled). CTA = arrow-in-button accent pill. **"← Accueil" kept below — deliberate override of the handoff** (which drops it, relying on the nav); Omar's two-state logic stands: cards remain → "Encore N" + Accueil; fully done (`dueRemaining === 0`) → Accueil only. Given the nav is now hidden through the bilan, Accueil is the primary in-content way home, so it's a more prominent secondary: `bg-card` fill + `border-line`, `text-base font-semibold` (was a thin `text-sm` outline).

### From M5.3a (v0.6.1) — conjugation substrate + fill-in-blank fix + hint

- **`lib/conjugator.ts` — deterministic, pure, client-safe.** No I/O, no Next/Supabase, no `server-only` (same posture as `word-status.ts` — runs in the client `FillInBlank`). Rule engine + irregular DATA tables: `IRREGULAR_FORMS` (full per-tense overrides for ser/ir/haber/estar/dar/ver/hacer/decir/tener/venir/poner/salir/saber/traer/extraer/conducir), `STRONG_PRET`, `FUT_STEM`, `IRREG_PART`, `IRREG_IMP_TU`, `PRES_STEM` (stressed stem, incl. hiatus accent), `WEAK_STEM` (-ir e→i/o→u), `IRREG_YO` (consonantal -go/-zco/-jo). A2/B1 tense union: presente, pretérito indefinido, imperfecto, futuro, condicional, presente de subjuntivo, imperativo (afirmativo + negativo), gerundio, participio (pretérito perfecto = the participle token). API: `conjugate`, `paradigm`, `analyze`, `isInParadigm`, `unambiguousPerson`, `isConjugable`, `normalize`.
- **Library was rejected with evidence, not asserted.** `spanish-verbs` (RosaeNLG, Apache-2.0) failed the plan-time battery 7/27 — no stem changes (`pedir→"pedo"`, a vulgar word — the exact "wrong conjugation = real harm" failure), no `-j-` preterites, no imperative/gerund/participle. `verbos` is CC-BY-NC. Hand-rolled passes 67 conjugator + 23 mask/rating tests.
- **Paradigm-aware masking** (`maskVerbSentence` in `lib/mask.ts`): blanks the first sentence token that is a member of `lemma`'s paradigm — so it masks the **contextually-correct conjugated form** (estudiamos, creció) and captures its `{tense, person}`. Fixes the **dio/fue MC-gap** too (irregular stems the old 4-char stem heuristic couldn't match → forced MC). `maskSentence` unchanged, kept as the non-verb + no-paradigm-match fallback (then MC). **Coordinate recovery prefers an accent-preserving match** over the accent-folded one, so "logró" (pret. él) is not conflated with "logro" (pres. yo) — membership stays accent-tolerant (folded) but the hint's tense/person is accurate.
- **`lemma ?? word` is the verb to conjugate against.** `words.lemma` is **NULL** for verbs added in infinitive form (Anthropic returns lemma==input → stored null); the stored `word` is then the infinitive. (Added `lemma` to the `/review` server select + the client `fetchDueCards` select + `ReviewCard`.)
- **5-branch verb grading** (`classifyVerbBlank` in `lib/rating.ts`, POS-gated `v.`/`v.pron.`): exact target → `exact`; lemma typed → `near` (reason `lemma`); typo near-miss of target → `near` (reason `typo`, the existing path); other valid inflection (`isInParadigm`) → `near` (reason `inflection`); else `wrong`. Reuses the existing ¡Casi! verdict + its FSRS rating (no new mapping). `computeRating` takes an optional `verb` context; **non-verb grading and MC are untouched** (`classifyVerbBlank` injects `inParadigm` so `rating.ts` stays I/O-free — it does not import the conjugator). `FillInBlank` has ONE `classify()` path feeding both rating + verdict copy so they can't diverge.
- **In-question hint** (`FillInBlank`): no default indication; the Indice affordance reveals `(lemma, person)` for verbs — tense withheld (context is the cue), **`unambiguousPerson` → lemma-only `(estudiar)` on ambiguity** (e.g. imperfecto yo/él), never a guess. Non-verb cards keep the first-letter cue. Derived on the fly (no stored coords).
- **Storage of `{lemma,tense,person}` DEFERRED to M5.3b** (Omar's call): grading never reads stored coords, derive is deterministic + self-healing across the existing deck with no backfill, and M5.3b can derive the same way. The enrich/zod write was **not** touched.
- **Safety property — M5.3a never DISPLAYS a generated conjugation.** The answer shown is always the real blanked token from the Anthropic-authored sentence; the hint shows facts (lemma, person). So an untabled-irregular verb falling through to regular rules degrades **verdict generosity**, it cannot **miseducate**. Regular-rule fallback for untabled verbs is therefore accepted in M5.3a. **HARD REQUIREMENT recorded for M5.3b** (see roadmap): once a generated form is displayed (Astuce / table), it must refuse to show a paradigm for an untabled/uncertain verb.
- **Edge cases handled:** infinitive-in-sentence ("me gusta estudiar") → infinitive is in the paradigm → plain ¡Eso es!; clitic-attached infinitive ("probarme") not in the paradigm → verb path declines → `maskSentence` fallback; non-infinitive lemma data ("saltando"/"tocando") → `isConjugable` false → no crash, degrades to exact-match; mis-tagged/absent `pos` → verb gate fails closed → non-verb matching. Diagnostic confirmed `pos` is reliable for the verb/non-verb split (closed enum; only `v.`/`v.pron.` are verbs) — unlike the el/la gender case.

### From v0.6.2 — post-M5.3a review-format coherence

Three independent fixes after M5.3a, each diagnosed against the real deck (124 collection words / 45 verb-pos) before coding (demonstrate-per-item).

- **Token-aware spellcheck gate** (`lib/wordlist.ts` `checkSpelling`, consumed by `/api/words/enrich`). The enrich gate did a single `WORD_SET.has()` of the *whole* input; the Hunspell set holds only single tokens, so ANY multi-word input 422'd **before the Anthropic call** — not reflexive- or `v.pron.`-specific, purely the space (the Zod schema was never reached). `"te acuestas"`/`"te acostaste"` → `SPELLCHECK_UNKNOWN`; a fresh `"a menudo"` → `SPELLCHECK_CANDIDATES`. Now tokenizes on whitespace: a phrase passes iff **every token** is `contains()`. On exactly one missing token, fuzzy candidates are **reconstructed into the full phrase** (corrected token swapped in, rest kept) so the user never sees a bare token; 2+ missing tokens → `SPELLCHECK_UNKNOWN`. Single-word inputs unchanged. Anthropic path + Zod untouched.
- **Verb cloze-MCQ gated on stored-in-form coherence** (`lib/review-cloze.ts` `chooseQcmCue` — pure, unit-tested; consumed by `MultipleChoice`). QCM cue selection was a content-independent per-UUID coin flip (`seed % 2`), so a verb could draw a **bare-definition** QCM that can't test the form (duermes) while another drew a cloze (veníamos). An intermediate fix (commit `2e858f0`) over-corrected — forcing the cloze for **all** maskable verbs — but `maskVerbSentence` blanks the contextual **conjugated** form while the MCQ options stay the stored word + semantic distractors, so **infinitive-stored** verbs got an incoherent cloze (blank wants `Conozco`/`creció`; options are infinitives `[conocer, saber, …]`) — 16/45 of the real verb deck. The shipped rule: a verb gets the cloze **only when `normalize(picked.target.surface) === normalize(word)`** (stored in the example's form → blank and options are the same form); infinitive-stored verbs and `target===null` gerund/participle-stored verbs fall to the **lemma-level definition-MCQ**, coherent until conjugated distractors exist (M5.3c). Non-verbs keep the `seed % 2` split. Real-deck split: 45 verbs → **26 cloze / 19 definition**. The decision was **lifted out of the inline initializer into `chooseQcmCue` with locking tests precisely because the inline+untested version regressed** (the `2e858f0` over-correction). **Known limit (→ v0.6.3):** clitic-stored reflexives (`word = "te duermes"`) route to definition-MCQ because `maskVerbSentence` masks only the finite verb token (`duermes` ≠ `te duermes`); a coherent cloze there needs clitic-aware masking.
- **Écriture answer-revealing word header removed** (`FillInBlank.tsx`). The fill-in-blank answering screen opened with the headword itself (`"volvíamos · v."`) above the sentence — printing the answer the card tests. Removed the word + `pos` header; the sentence + French cue (cloze) or the definition (definition-fallback) remain the prompt. `word` still used by the non-verb first-letter hint + grading.
- **Cue selection is FIXED per card, not rotating** (`pickClozeExample`): `seed` derives from the card UUID, so an infinitive-stored verb shows the **same** form every review. Deliberate (deterministic SSR/hydration); per-review rotation would need a review-varying seed input — not done. **(Superseded in v0.6.3 #3 — `reps` now rotates the example; format still fixed.)**

### From v0.6.3 — écriture-polish

Five commits, each diagnosed against the real deck first (124 words / 45 verb-pos). Suite 161 → 181.

- **#2 — accent-homograph denylist** (`maskVerbSentence`, `lib/mask.ts`). The masker could blank a function word instead of the verb (confirmed: `dar` masks the preposition `"de"` in *"…película de terror me dio…"*). **Two compounding causes:** the accent-fold map (`dé→de`) AND — the deeper root — the **conjugator emitting UNACCENTED usted-imperatives** (`"de"` for dar, `"esta"` for estar) that sit in the exact map. Fix (mask.ts only, conjugator untouched): a curated denylist `{de,se,el,te,mi,tu,si,mas,esta,estas,aun,solo}` of UNACCENTED function words is never selected as the verb form — so genuine accented tokens (`dé`, `está`, `estás`) still mask. Guards écriture too (no cue-gate there).
- **#1 — clitic-aware reflexive masking** (`maskProcliticReflexive`). Proclitic-reflexive stored words (`"te levantas"`) routed to definition-MCQ because the paradigm yields the BARE verb (`levantas`) → `target.surface ≠ word` → `chooseQcmCue` failed. Now masks the full `clitic + verb` unit, `surface == word` → coherent cloze-MCQ; distractors are stored as full reflexive forms (`te levantas → [te sientas, te acuestas, te quedas]`), so options match the full-unit blank. **`lemma` passed RAW** — `paradigm`/`isConjugable` strip `-se` internally (verified, not asserted: `maskVerbSentence(s,"levantarse")` → `"levantas"`); conjugator + `classifyVerbBlank` untouched. **Grading (verified):** `"te levantas"` → exact ¡Eso es!; `"levantas"` (dropped clitic) → **`wrongForm`/¡Uy! with the "right verb, wrong form" copy** (NOT ¡Casi! — `isInParadigm` is true but the M5.3a `wrongForm` quality routes valid-other-inflections to ¡Uy! chrome) — the accepted masking-only degradation.
- **#1 × #2 composition:** `maskProcliticReflexive` calls the #2-hardened `maskVerbSentence` to locate the verb; the clitic (`te`/`se`) is denylisted there so it's never chosen AS the verb, and #1 extends the matched verb's blank leftward over it — different stages, no collision.
- **Backfill** (`scripts/backfill-reflexive-lemmas.ts`, lemma-IS-NULL guarded, applied to live DB): `te levantas→levantarse`, `te sientas→sentarse`, `te duermes→dormirse`. Graceful before `--write` (no lemma → masker declines → stays definition-MCQ; no broken window).
- **#3 — `reps` example rotation** (`pickClozeExample`). Was first-maskable always (a card fixed on one form forever). Now collects maskable examples in UUID-stable order and returns `pool[reps % pool.length]`, cycling forms across reviews. **Format stays per-card stable:** `chooseMode` untouched; `MultipleChoice` decides the cue from a **reps-independent** pick (only the displayed example rotates, never cloze↔definition). `reps` threaded from `FillInBlank` + `MultipleChoice`.
- **#4 — desktop accent-row** (`AccentBar.tsx`). `á é í ó ú ñ ü` inserts at the caret in the écriture answer field (`onMouseDown`+preventDefault keeps focus; caret restored after the controlled update). **Desktop-only** via `(pointer: fine) and (hover: hover)` using `useSyncExternalStore` (SSR-safe + reactive, same pattern as `AudioButton`); hidden on touch (long-press menu + grading cushion cover mobile). Card-fill/line-border/ink-glyph, pulled up (`-mt-2`) to read as tied to the blank.
- **#5 — prefer the coherent (stored-in-form) example** (`pickClozeExample`). The #3 selection returned the first *maskable* example, not the first *coherent* one: `te duermes` (id-seed start=1) landed on a `me dormiría` sentence → masked `"dormiría"` ≠ `"te duermes"` → definition, never reaching its coherent examples. Now examples whose masked form == the stored word are **preferred** (reps rotates within that pool); infinitive-stored verbs with no coherent example are unchanged (→ definition); empty-maskable still returns null (no `pool[reps % 0]`). **Deck delta: 6 verb cards definition→cloze, 0 reverse (monotonic):** `te duermes` (reflexive unit) + `destacar/dar/andar/circular/conocer` (each has an example with the bare infinitive → a coherent infinitive-level cloze, blank == option).
- **Known limit (→ M5.3c conjugator gate):** `te sientas` stays definition-MCQ — the conjugator lacks `sentar`'s e→ie present stem (`sientas`). Test-locked; auto-upgrades when fixed.
- **Two conjugator bugs surfaced + logged** (`backlog.md` → Conjugator audit; gate on M5.3c in `roadmap.md`): unaccented usted-imperatives (`dar→de`, `estar→esta`) and the missing `sentar` e→ie stem. Out of scope here (conjugator correctness is non-load-bearing until M5.3c displays generated forms). **— Both resolved in v0.6.4 below.**

### From v0.6.4 — conjugator hardening (standalone slice ahead of M5.3b)

Audit-FIRST, per SESSION_PROTOCOL (diagnose-before-building). A throwaway harness ran the conjugator over (a) the live deck (51 verbs) + (b) the conjugator's own tabled set, producing a concrete `(verb, tense, person, got, expected)` list **before** any fix was architected. Suite 181 → 298.

- **Finding 1 — the systemic catch (the whole reason this was bigger than "the known three").** `imperativeAffirmative()` and the negative-imperative branch in `conjugate()` built on the *rule-computed* `present()`/`subjunctive()` helpers, which **do not consult `IRREGULAR_FORMS`**. `formsFor()` (used by `paradigm()`'s present/subj cells) *does* — so the subjunctive cells were right but the **imperative cells silently bypassed the table** for **all 16** fully-irregular verbs (`ser`→`"sa"`, `ir`→`"a"`, `estar`→`"este"`, `dar`→`"de"`, `hacer`→`"haca"`…). This was the true root of the v0.6.3 `dar→"de"` masking misfire (the #2 denylist worked around it, never fixed it). **Fix:** reroute both imperative paths through `formsFor(...,'presente')`/`formsFor(...,'subjPresente')` (~2 lines). Affirmative-tú stays table-first (`IRREG_IMP_TU[inf] ?? pres[2]`) so the 8 short forms (`ten/ven/pon/sal/haz/di/sé/ve`) survive the reroute — proven by a tú-invariant test (the reroute changed the present/subj SOURCE, not the tú branch). **`ir` hortative special-case:** affirmative nosotros is the indicative `"vamos"` (1-liner), NOT the subjunctive `"vayamos"`; the negative stays `"no vayamos"` (separate assertions lock the no-leak).
- **Display-guard primitive — `canDisplayParadigm(lemma)`** (the primitive M5.3b/c consume to refuse a guessed paradigm). **Conservative allowlist** (`TRUSTED_LEMMAS`), not a heuristic: "is this untabled verb secretly irregular?" is undecidable from the lemma (`forzar`/`aprobar`/`contar` all *look* regular), so a rule-class guess would falsely green-light a guess. Returns true ONLY for full-paradigm reference-verified verbs (the fixture + harness below), false for everything else (graceful no-table). Matches the canonical lowercase infinitive **without accent-folding**, so `soñar` (dream) ≠ `sonar` (sound). Reflexive variants inherit via `stripReflexive` (`sentarse`→`sentar`). **No UI consumer in this slice** (table/drill are M5.3b/c). The guard does NOT catch a tabled-but-WRONG form — that's the fixture/harness's job (a verb leaves the set the moment a cell regresses).
- **Stem-change expansion** (`PRES_STEM` + `WEAK_STEM`): sentar (the `te sientas` deck gap), despertar, encender, mentir (e→ie); acostar, mostrar, recordar, costar, soñar, almorzar, mover, doler (o→ue); vestir, seguir, conseguir (e→i). `seguir`/`conseguir` added a narrow **`-guir` gu→g** orthographic rule (`sigo`/`siga`/`siguió`), gated on `inf.endsWith('guir')` so it can't touch `-uir` (construir). The existing ortho machinery (z→c, c→qu) already composed with the new stems (`almorzar`→`almorcé`/`almuerce`), so **no orthographic split to v0.6.5 was needed** — confirmed by the fixture, not asserted.
- **Self-verifying golden-fixture harness** (committed, replacing the throwaway audit script). `lib/conjugator.expected.ts` holds **reference-authored** full paradigms for the 15 irregulars + every stem-changer; `conjugator.test.ts` runs a data-driven loop asserting **every cell == `conjugate()`**, plus a clean-room **`regularReference()`** (independent reimplementation of the textbook regular rules) for the deck regulars, plus the admission invariant (`TRUSTED_LEMMAS` is exactly the fixture keys ∪ the regular-verified set — can't add a trusted verb without a passing full-paradigm entry). **Admission rule:** a verb enters `TRUSTED_LEMMAS` only if its FULL paradigm is reference-verified; a single wrong cell → excluded.
- **Excluded-and-logged blind spots** (each one wrong cell → `canDisplayParadigm` false → never displayed; `backlog.md`): **`poder`** gerund `"podiendo"`→should `"pudiendo"` (rare -er weak gerund; `WEAK_STEM` would break its subjunctive `"podamos"`); **`creer`** preterite hiatus accents `"creíste/creímos/creísteis"`; **`haber`** (auxiliary, no learner imperative); **`llover`** (impersonal). The independent fixture review is what *caught* these — proof the verification has teeth, not a rubber-stamp.
- **`te sientas` auto-upgrade** (v0.6.3 known limit): the two mask tests that locked the gap were flipped — `maskProcliticReflexive` now locates `sientas` and masks the full `te sientas` unit → coherent cloze-MCQ.
- **#2 denylist re-verified, KEPT** (not removed). Post-fix dar's only present/imperative homograph is the **accented** `"dé"`, so unaccented `"de"`/`"esta"`/`"este"` leave the paradigm entirely and the denylist now guards *only* the genuine function words. Re-verified: `dar` still masks `"dio"` (not the preposition `"de"`), and the now-correct accented `"dé"` IS maskable (the denylist holds unaccented entries only, so it doesn't over-skip).
- **Trusted-set coverage = M5.3b table-appearance frequency.** v0.6.4 seeded the deck regulars + the audited stem-changers/irregulars; comprehensive A2/B1 seeding (with a vetted fixture) is M5.3b-prep. The more verbs in `TRUSTED_LEMMAS`, the more often M5.3b's conjugation table can show — M5.3b inherits this scoping.

### From v0.6.5 — two review/add hygiene bugs (found during v0.6.4 smoke-testing)

Small patch, each diagnosed against the code before fixing. Suite 298 → 316.

- **Item 1 — result screen showed a different example than the exercise.** `FillInBlank` masked `picked` from `pickClozeExample` (reps-rotation #3 + prefer-coherent #5 → any index), but the result "Exemple" hint block independently re-read `card.examples[0]`, so once selection landed on a non-first example the two diverged (repro: masked `"logré"` vs shown `"…logró aprobar…"`). **Fix:** lifted the decision into a pure, unit-tested **`resultHintExample(picked, card)`** (`lib/review-cloze.ts`) — `picked ? picked.example : card.examples[0]` — so the result renders the SAME sentence the exercise masked, un-blanked. NOT inlined at the call site, per the `chooseQcmCue` lesson (an inline decision silently regresses). `MultipleChoice` was checked and has no divergence (its `prompt` is one `useState` shown identically before/after). Picked-null (definition-fallback écriture) keeps `examples[0]` — it masked no example, so nothing to contradict.
- **Item 2 — add flow stored an invalid reflexive clitic as headword.** `"ti acuestas"` passed the v0.6.2 **per-token** spellcheck (both `"ti"` and `"acuestas"` are real words — the malformation is grammatical, not lexical), and `enrich/route.ts` returned `word` = the **raw input**, so every display surface (interstitial subheading, `"Garder «…»"`, card header, TTS, `/words` · `/dictionary` titles) + the stored headword showed the typo. **Blast radius was display-only** (confirmed): review masks off the *example* via the clean `lemma`, never the headword. **Fix:** deterministic, verb-agnostic, **conjugator-free** `correctProcliticReflexive(word, lemma, form_annotation)` (`lib/reflexive.ts`). **Detection split from correction:** the valid proclitic set `{me,te,se,nos,os}` flags malformation without needing the person (`"ti"`/`"mi"` are definitively wrong); the person (parsed from `form_annotation`, hardened against `ª`/`a`, `pers.`/`persona`/`person`, `sing.`/`singular`, `pl.`/`plural`, usted/ustedes→se) picks the replacement. **Degradation ladder:** valid+match or valid+ambiguous → unchanged; valid+mismatch or invalid+parseable → correct the clitic; **invalid+unparseable → fall back to the LEMMA** (never store the typo). Gated on the reflexive flag so `"para ti"` is never touched. Wired in `enrich/route.ts`: `corrected` drives the lemma-diff check + the response `word`; TTS regenerated only when `corrected !== word` (rare path; common adds keep the parallel audio).
- **Brittleness logged, not solved:** the `form_annotation` regex parse depends on Anthropic's free-text format. Durable fix (a structured `person` field on the enrichment response) is logged to `backlog.md`; the lemma-fallback degradation means drift stores the lemma, never the malformed surface.
- **Corrupted test row** (`"ti acuestas"`): delete + re-add (a corrective `UPDATE word=` would leave stale `audio_urls` pointing at the typo's TTS object — re-adding through the fixed flow gets word + audio right). No script.

### From v0.6.6 — M5.3b conjugation-table half (frame-5 grid)

Additive to the existing `/add` lemma interstitial (the `estudié`→`estudiar` screen), not a rebuild. Coverage was measured against the live deck BEFORE any UI (diagnose-before-building). Suite 316 → 327.

- **Coverage check first.** `canDisplayParadigm(lemma ?? word)` over the 48 verb-pos deck words: **42 get a grid (87.5%)**, 6 fall to the no-grid path — `andar`/`conocer`/`crecer` (untabled), `creer`/`haber`/`acordar` (excluded-and-logged single-cell defects). Every miss is a verb whose paradigm we can't vouch for, not a guard misfire. The real hit rate was confirmed before wrapping UI around it.
- **Display guard is the load-bearing gate (HARD REQUIREMENT met).** The grid renders ONLY when `canDisplayParadigm(lemma)` is true AND the typed form's tense resolves to a finite six-person tense that contains it. Any untabled/uncertain verb, unresolvable tense, or incoherent surface → `null` → the interstitial renders **exactly as before** (heading + `form_annotation` line + CTAs, no grid). Never displays a guessed paradigm — the M5.3a "regular-rule fallback is OK because nothing is shown" property no longer holds now that a generated paradigm is on screen.
- **All logic in a tested pure helper** `lib/conjugation-grid.ts` (client-safe, same posture as `conjugator.ts`/`word-status.ts`; consumes `canDisplayParadigm` + `conjugate`, does NOT modify the conjugator) — never inline in the component (the `chooseQcmCue`/`resultHintExample` lesson). `resolveGridTense(form_annotation)` parses the pedagogical Spanish tense accent-folded, priority-ordered so `"pretérito imperfecto de indicativo"` → imperfecto (the `creías` shape, checked before the preterite branch) and `"perfecto compuesto"` → null (compound). `buildConjugationGrid(lemma, surface, form_annotation)` builds the 6 cells via `conjugate` (the conjugator's **accented** forms — the logró/logro lesson) + the tense-label map (Spanish name + French gloss). 11 unit tests.
- **Highlight rule (Omar's refinement, not "exactly one").** Fold-match the typed surface against the cells (retry once after stripping a leading reflexive clitic `me/te/se/nos/os`, so `"me acuesto"` highlights the bare `acuesto` cell). **0 matches → null (no grid)** — a grid for a word it doesn't contain is incoherent (same coherence principle as the cloze-only-when-masked-token==stored gate). **≥1 match → highlight ALL matching cells**, required because imperfecto / condicional / subjPresente are **syncretic** (yo == él/ella: `estudiaba`, `estudiaría`, `estudie`) — the old "exactly one" rule would have rendered a large share of real cards with no highlight at all; highlighting both is honest about the genuine person-ambiguity.
- **Decisions:** imperative-labeled + non-finite (gerundio/participio) inflected adds → **no grid** (no clean six-person table — imperative lacks `yo`); confirmed with Omar. The in-deck "« lemma » est déjà dans ta collection" tint box was **replaced** by the quiet italic review-status line (design-driven, mockups A/B both drop the banner).
- **Reusable component** `app/(app)/ConjugationGrid.tsx` (presentation only — takes the computed `ConjugationGrid` object, renders mockup A's titled 2-col/3-row grille with the amber-highlighted cell; existing tokens only: `bg-card`/`border-line`/`bg-surface-alt`/`bg-tint`/`text-accent`). Mounted by `/add` in this slice; the later Astuce-de-Paco /review slice mounts the same component — **NOT wired into /review here**.
- **Review-status line** ("Tu as revu **lemma** N fois · prochaine révision dans X j") — pure `review_cards` data, shown ONLY when `lemma_status === 'already_in_deck'`. Threaded via **optional** `lemma_reps`/`lemma_due` on the enrich response (the lemma-collision lookup now selects `review_cards(reps, due)`, read through `oneEmbed` for the UNIQUE(word_id) to-one embed — M5.1 cardinality lesson). Optional so no other enrich path/consumer is touched. The calendar-delta is computed in the event handler (`handleSubmit`), not render (`Date.now()` in render is impure — caught by the `react-hooks/purity` lint). `reps === 0` drops the "revu" clause; `dueDays <= 0` → "aujourd'hui".
- **Untouched:** conjugator, grading, masking, enrich/zod schema, no migration, no persisted coordinates. Everything computed live.

### From v0.6.7 — M5.3c verb-conjugation drill (Paco's first game mode)

The substrate's first *production* consumer (M5.3b *displayed* generated forms; M5.3c asks the user to *produce* them). **Wiring, not conjugator correctness** — the conjugator is untouched. Built to the `(ii)` trigger-frame mockups. Suite 327 → **361** (drill helpers + the polish/lock tests below).

- **New files (v0.6.7):** `lib/drill.ts` (+ `lib/drill.test.ts`), `buildConjugationGridForTense` added to `lib/conjugation-grid.ts`, `app/(app)/drill/*` (`page.tsx`, `DrillClient`, `DrillSetup`, `DrillPrompt`, `DrillResult`, `DrillRecap`, `DrillHeader`), `app/(app)/DrillCard.tsx` (Home entry), `app/api/drill/prefs/route.ts`, `lib/display-name.ts` (+ test), and the migration `supabase/migrations/20260606000000_drill_prefs.sql`.
- **Self-contained flow at `/drill`** (`app/(app)/drill/`): `page.tsx` (server) builds the pool + reads prefs and gates; `DrillClient.tsx` is a `'setup' | 'playing' | 'recap'` phase machine that holds **focus mode for its whole lifetime** (the `ReviewSession` pattern — `useFocusMode().setFocus(true)`, restored on unmount; TopNav suppressed across all three phases, matching the mockups). `DrillSetup` / `DrillPrompt` / `DrillResult` / `DrillRecap` / a shared `DrillHeader`.
- **All logic in the pure, tested `lib/drill.ts`** (client-safe, same posture as `conjugator.ts`; the `chooseQcmCue`/`resultHintExample` lesson — never inline): `isDrillableVerb` + `buildDrillPool` (verb POS, **not** `v.pron.` / not `-se`, `canDisplayParadigm` true; deduped by infinitive — drives both the unlock count and the prompt source), `buildDrillPrompts` (cartesian verb×tense×person, shuffle via an **injected rng** so tests are deterministic, **prefer unique combos then top up with repeats to reach 10**), `gradeDrillAnswer` (**strict** — wraps the already-strict `classifyVerbBlank` and collapses its 4 qualities to 3 verdicts: exact→¡Eso es!, accent/typo→¡Casi!, lemma OR a different valid form→¡Uy!; deliberately NOT the old lenient "any inflection = ¡Casi!"), `drillTeachingLine` (the deterministic "tu as donné le [X], on attendait le [Y]"), `triggerFrame`.
- **The single swappable seam.** `triggerFrame(tense)` returns a fixed, verb-agnostic, object-less Spanish lead-in per tense (`presente`→"Ahora mismo, ", `preterito`→"Ayer, ", `imperfecto`→"Antes, ", `futuro`→"Mañana, ", `condicional`→"Con gusto, ", `subjPresente`→"Ojalá "). (iii) full generated sentences later replace ONLY this function — the loop is untouched.
- **Teaching line precision — accent-EXACT preferred over folded** (the logró/logro lesson). `drillTeachingLine` matches the wrong answer against `paradigm(lemma)` preferring an accent-exact surface match, folding only when nothing matches exactly: so accented "hablé" is named unambiguously preterite, while a bare ambiguous fold degrades to "Ce n'est pas le [Y]" (or omits, when the answer isn't a recognizable form at all — pure typo path → just struck answer + correct form). Names the infinitive ("Tu as donné l'infinitif…") and the right-tense-wrong-person case ("C'est bien le [X], mais pas la bonne personne.") too. Never guesses.
- **Reuse, not rebuild:** the review écriture input `AnswerBlank` (transparent input over the inline blank) + the **desktop-only** `AccentBar` verbatim (Omar's call — mobile keeps iOS long-press; a dropped accent on mobile lands as ¡Casi!, consistent with the strict grader); the `ResultReveal` Paco-mood family (paco-feliz/pensando/sad, ¡Eso es!/¡Casi!/¡Uy!); the v0.6.6 `ConjugationGrid` for the "Voir la conjugaison" bottom sheet via a new **`buildConjugationGridForTense(lemma, tense, person)`** in `lib/conjugation-grid.ts` (the drill already knows tense+person, so it skips the `form_annotation` parse; highlights the asked person's cell; same `canDisplayParadigm` guard). `PERSON_LABELS` exported from `conjugation-grid.ts` for the cue chip / recap instead of duplicating.
- **Schema:** `profiles.drill_tenses` (JSONB) + `drill_person_scope` (TEXT, CHECK singular|all), migration `20260606000000_drill_prefs.sql` ending `NOTIFY pgrst`. Setup pre-fills the saved selection; "Commencer" persists it fire-and-forget via `POST /api/drill/prefs` (upsert, RLS own-row) and the drill starts from the in-memory selection regardless. **No FSRS/words/review_cards changes.**
- **Pure practice — no FSRS impact** (verified by construction): the drill never calls `/api/review`, never writes `review_logs`, never touches `review_cards` scheduling.
- **Edge cases:** <5 trusted verbs → soft-locked `DrillCard` (also rendered on a direct `/drill` deep-link, not just Home); fewer than 10 unique (verb,tense,person) combos → repeats to fill 10; `analyze` ambiguous on a wrong answer → the teaching line degrades or omits, never guesses.
- **Deferrals logged** (`roadmap.md` / `backlog.md`): drill "new verbs" (outside the deck) ⇐ A2/B1 coverage seeding; (iii) full generated sentences = the shared generation/cache layer (crossword shares it; only `triggerFrame` swaps); richer ¡Casi! morphology = the parked Astuce tip redesign. No game-mode selector (Home-card entry only).
- **Asset hygiene:** `paco-feliz`/`paco-pensando`/`paco-sad` PNGs compressed in place (`sips -Z 440`, ~1.5 MB → ~110 KB each; they render at ≤110 px) — benefits every existing consumer (the image-weights lesson).
- **Follow-up polish (same milestone, pre-tag):**
  - **Tense-selection "leak" — diagnosed as a DISPLAY defect, not a filter bug** (the M5.0b "confident-but-false root cause" discipline). `buildDrillPrompts` enumerates tenses ONLY from its `tenses` arg and is called once with the setup selection (`DrillClient` → `sel.tenses`); the chips' state and the emitted array read the same Set — it provably cannot emit an unselected tense. The real defects that made a served (pre-filled) `condicional` *read* as a leak: `DrillResult` showed the **raw tense key** (`prompt.tense` → "CONDICIONAL") while the prompt showed the label, and the cue chip omitted the tense entirely. Locked with a sweep test (every emitted `prompt.tense` ∈ the selected set, across seeds + subsets incl. one excluding condicional).
  - **Grammar terms now SPANISH (accented), consistently** — `DRILL_TENSES` labels → `Presente · Pretérito · Imperfecto · Futuro · Condicional · Subjuntivo` (propagates via `drillTenseLabel` to setup chips, BOTH focus headers, the cue, the recap); `DrillResult` header switched from the raw key to `drillTenseLabel` (fixes the prompt-vs-result inconsistency in one line); person-toggle titles → `Singular` / `Singular + plural` (persons were already Spanish). French stays for instructional chrome + the verdict/teaching prose (`TENSE_FR` unchanged).
  - **Cue shows all three coordinates** — `verb · person · tense` (e.g. `ir · nosotros · pretérito`); it's a conjugation exercise, so the tense is part of the spec, not just context.
  - **Conditional trigger frame** `"Con gusto, "` → `"En ese caso, "` (verb-neutral — "Con gusto" implied a willingness that doesn't fit verbs like *vaciar*).
  - **Conjugation table on all three result screens** (¡Eso es!/¡Casi!/¡Uy!) inline below the answer card, asked person highlighted — reuses `buildConjugationGridForTense` + `ConjugationGrid` (same as the prompt's hint sheet): reinforces on a hit, shows the correct paradigm on a miss.
  - **Placeholder display name** — `displayNameFromEmail` (`lib/display-name.ts`, pure + tested): title-cased first token of the email local-part ("camille.r@…" → "Camille"), used in the drill recap ("¡Muy bien, {name}!"; falls back to "¡Muy bien!" when absent). Threaded server-side via `drill/page.tsx` (`auth.getUser`) → `DrillClient` → `DrillRecap`. **Explicit placeholder** — M6 onboarding collects a real name on `profiles` and swaps ONLY the helper's source (logged in `backlog.md` → Onboarding). The real Home has no greeting line today, so there's no Home call site yet.
  - **Imperative-leak report ("drill served `bebed`") — diagnosed as NOT reproducible in the current code** (the diagnose-before-fixing / "hypothesis-until-checked" discipline). Reproduced exhaustively: `conjugate('beber', <any of the six finite tenses>, person)` never yields `bebed` (`subjPresente · vosotros` = `bebáis`); a whole-`trustedLemmas()` sweep emitted **zero** tenses outside the six (the only form-overlaps were correct syncretism — the affirmative imperative usted/nosotros/ustedes *is* the subjunctive, plus `vamos`/`ve` are also present forms). Both proposed root causes are structurally excluded: the pool only ever iterates the passed `tenses` (never `paradigm()`'s full union), `coercePrefs` filters the DB through the six, and `imperativoAfirmativo` isn't assignable to `DrillTense` (a mislabel can only misdisplay, never re-key). Likely a stale dev bundle. **Hardened regardless:** `DrillTense` now derives from the single canonical `DRILL_TENSES_ORDER` constant (type + runtime set can't drift), `buildDrillPrompts` defensively `filter(isDrillTense)`s its input (a cast/future-widening can't reach `conjugate` with a non-finite tense), and two new locks — pool-is-exactly-the-six (incl. a polluted-input rejection test) + a chip label↔key round-trip (the gap the existing sweep test misses). See `roadmap.md` → M5.3d for the committed compound-tense follow-up.

### From M5.4a (v0.7.0) — /words list scale: search + load-on-scroll

The `/words` page fetched all of the user's words and rendered every row (deck ~150 and growing). This slice adds forgiving search + progressive rendering, **all client-side over the already-fetched list** — NO schema, NO new API route, NO server-side pagination. Suite 361 → **373**.

- **Pure tested predicate** `lib/word-search.ts` (+ `lib/word-search.test.ts`) — `normalizeSearch` (lowercase + NFD diacritic-strip; the plain strip **folds ñ→n**, which is WANTED here — "nino" matches "niño" — deliberately NOT the Ñ-preserving guard from `lib/dictionary.ts`'s bucketing, which does the opposite for a different purpose) and `matchesWordSearch(item, query)` (empty/whitespace query → matches all; else the normalized query is a substring of the normalized word OR `defEs` OR `defFr`). The match predicate lives in the helper, not inlined in the component (the `chooseQcmCue`/`resultHintExample` inline-regression lesson).
- **French gloss threaded for search only.** `page.tsx` now also extracts `defFr` onto `WordListItem` (the `definition` JSONB was already selected — no query change); it feeds the search predicate and is **never rendered** (Spanish-first stays intact).
- **Search input** above the filter pills in `WordList.tsx` — `text-base` (16px, the iOS zoom-lock rule) with a clear (×) affordance. **Pipeline order: filter pill → search → sort → render.** Search narrows the currently ACTIVE pill (it does not reset the pill) and runs over the FULL deck, not the rendered rows.
- **Load-on-scroll (progressive append, NOT virtualization).** Render `INITIAL_CHUNK = 40`, reveal `CHUNK_INCREMENT = 30` more when a bottom sentinel enters the viewport via **IntersectionObserver** (not scroll-event listeners). Rows accumulate in the DOM (`visible.slice(0, shown)`); no new fetch, no react-window dependency. The sentinel only renders while `shown < visible.length`, so the observer naturally stops; the observer captures the current length in its closure and the cap makes extra fires harmless.
- **Cap resets to the initial chunk** whenever the visible set changes (filter / search / sort) — via the React-blessed *adjust-state-during-render* pattern (a `viewSig` string compared against its previous value), NOT a `setState`-in-effect (which the lint rule rejects). Default date-desc sort → the initial chunk is the newest N.
- **Empty state** branches: `"Aucun résultat"` when a search query is active, else the existing `"Aucun mot dans cette vue."`
- **Reduced motion:** rows simply appear — no reveal animation added, so there's nothing to gate (sidesteps the PhaseChecklist "specced an animation that never fired" trap).
- **Out of scope (later slices):** delete, archive, Relearn, bulk/multi-select, swipe, schema columns, server pagination.

### From M5.4b (v0.7.1) — /words per-word actions: delete + undo + Relearn

Per-word management on `/words` + the detail page. NO schema change, NO soft-delete; bulk/multi-select stays M5.4c. Suite 373 → **383**.

- **`reset-schedule` semantics confirmed first** (diagnose-before-building). `POST /api/words/reset-schedule` does ONLY `review_cards.update({ due: now })` — it does NOT touch state/stability/difficulty/reps/lapses and NEVER touches `review_logs`. So Relearn is a pure reschedule (no history wipe) → per the rule, **no confirm modal**, a single button with consequence-stating copy. A New/already-due card is a graceful server-side no-op.
- **FK cascade verified against the live schema** (not assumed — the base tables predate the migrations dir, and account-delete only proves the `user_id` cascade). `information_schema` showed `review_cards.word_id → words` = CASCADE and `review_logs.card_id → review_cards` = CASCADE (the full chain cascades on a word delete); `add_events` has no `word_id` FK (user-keyed, correctly untouched). So the delete route is a plain `words.delete()` — no manual child cleanup, no migration.
- **Single-word delete route** `app/api/words/[id]/route.ts` (`export async function DELETE`, id from the URL param — the first `[id]` route under `words`; REST resource-delete shape). `getUser()` → 401; `.delete().eq('id', id).eq('user_id', user.id)` (explicit user scope + RLS); `console.error`s the Supabase error (M3.3 logging convention). No batch route (M5.4c's call).
- **Deferred-delete + undo primitive** `app/(app)/DeferredDelete.tsx` — a context provider mounted in `(app)/layout.tsx` as a sibling of `FocusModeProvider`. **Layout-level by requirement:** the detail-page delete routes back to `/words`, and the undo toast/timer must survive that navigation (detail→list doesn't unmount the layout). `scheduleDelete({ ids, labels })` is **set-shaped** (1 id here; M5.4c passes N) — but NO batch route/UI built. State model: the provider owns `hiddenIds: Set<string>` (undo-window + committed ids); `/words` renders from its server `items` minus `hiddenIds`, so optimistic removal, undo (un-hide → the deterministic sort re-places the row), and commit all fall out of one Set with no list↔provider callbacks and no local list copy. ~5s timer → on expiry fire `DELETE` per id + `router.refresh()`; on undo cancel + un-hide; a failed DELETE un-hides (word reappears = accepted safe failure); **provider unmount clears the timer WITHOUT committing** (word survives). Scheduling a new delete flushes any in-flight one (commit immediately) so none is lost.
- **Undo toast** reuses the add-page toast styling (fixed `bottom-24`, `max-w-[430px]`, `rounded-card shadow-card border-line`, `font-serif`) + an "Annuler" action; copy via the pure tested `lib/delete-toast.ts` `deleteToastMessage(labels)` ("« mot » supprimé" / "N mots supprimés"). Folded in the backlog **`select-none`** on the toast text.
- **Swipe-to-reveal row** `app/(app)/words/SwipeRow.tsx` — reveal-and-tap (drag left → red "Supprimer" drawer, snap open/closed past a threshold, **tap to commit**), deliberately NOT the discovery `SwipeCard`'s fling-past-threshold-to-commit gesture (no persistent open state, no drawer, tilt/stamps we don't want — a fresh component sharing only the pointer-drag pattern). Pure snap math in tested `lib/swipe-row.ts` (`clampOffset`, `resolveSnap`, `exceedsTapSlop`, `DRAWER_WIDTH`/`OPEN_THRESHOLD`/`TAP_SLOP_PX`). **Wraps the existing `WordRow` unchanged** — WordRow gained an `asListItem` prop (default true; SwipeRow passes false so it supplies the `<li>` and avoids nested `<li>`); Home preview unaffected. Tap-vs-drag via a slop threshold + a capture-phase click guard (`preventDefault` → Next's Link bails); while open, a tap closes instead of navigating. **Pointer capture is acquired LAZILY — only once the drag crosses the slop in `handleMove`, never on `pointerdown`** (the desktop-click-through fix, below): a clean click takes the exact pre-4b plain-`<Link>` path (no capture, no `preventDefault`), so it navigates every time and cmd/ctrl/middle-click new-tab is preserved; a genuine drag captures (so it survives the pointer leaving the row) and `movedRef` suppresses the post-drag click. **Desktop click-through regression (caught pre-tag, fixed in this same amended commit):** the original `setPointerCapture` on `pointerdown` retargeted the desktop mouse `click` to the capturing `<div>` (the `<a>`'s ancestor), so most row clicks never reached the Link and didn't navigate (touch was unaffected — touch clicks hit-test to the anchor regardless of capture). Capture-on-drag-start removes the cause without an always-`preventDefault`+programmatic-nav workaround (which would have killed new-tab clicks).
- **Single-open-row** owned by `WordList` (`openRowId` state): opening one closes the rest; an effect (gated on an open row) closes it on `scroll` or on a `pointerdown` outside (the open row marks itself `data-swipe-open` so its drawer button/drag don't self-close). Composes with 4a load-on-scroll: optimistic removal changes `hiddenIds`, which is NOT in `viewSig` (`filter|sort|search`), so the scroll cap does **not** reset.
- **Detail-page actions** `app/(app)/words/[id]/WordDetailActions.tsx` in a `StickyActions` bar (detail container gained `pb-28`): **Delete** (terracotta/`err` outline) → `scheduleDelete` + `router.push('/words')` (toast carries over); **Relearn** (`RotateCcw`, neutral) → `reset-schedule`, no modal, a local "réapparaîtra dans ta prochaine session" caption on success.
- **Out of scope (M5.4c+):** bulk/multi-select/select-all, a batch delete endpoint, archive/soft-delete schema, swipe on the detail page (it uses buttons).

### From M5.5a (v0.8.0) — design-system foundation (first slice of the PRE-BETA design pass)

The cross-cutting visual substrate locked in the v2 system board (`Paco — Système visuel v2`, the canonical source for every hex/rule). **Substrate ONLY — NOT a screen redesign.** Per-screen reworks are later cluster slices; this builds the shared layer they inherit. No tests added (presentation layer; pure-logic suite stays **383**). Build green; smoke-tested before the tag.

- **Color tokens — additive, board §01.** Added `--color-amber-deep #9A5A1C` (pressed/hover), `--color-amber-light #F2DCB8` (tinted fill / disabled-primary bg), `--color-ivory #FFFBF3` (text-on-amber — a distinct role from the `--color-card` surface that shares the hex), `--color-faint #A88F6B` (tertiary/captions/disabled text) + `--color-border-soft #E7D8B8` (inner dividers / disabled borders) — both real §01 roles, tokenized so the canonical Button consumes tokens rather than inlining §01 hexes — and the two action shadows `--shadow-amber` / `--shadow-amber-sm`. **Value-aligned** `--color-surface-alt` `#EDE0C4`→`#EFE2C6` (canonical "crème+"; a ~2-pt drift correction, imperceptible). **Confirmed already board-exact, no edit:** `--color-line #D9C9A8` = bordure, `--color-muted #7A5A3A` = sépia (already wired as the secondary-text token via `text-muted`), `--color-accent #C27A2C` = amber. **Deliberately NOT touched** (out of scope — belong to the §06 status/cluster work): `--color-tint`, `--color-ok-bg`, `--color-err-bg`, and the OKLCH `--color-again/hard/good/easy` rating ramp (the /review rating buttons already comply — board says do not touch).
- **Fraunces — structurally-enforced allowlist (board §02).** Loaded in `app/layout.tsx` as `Fraunces({ weight:['700'], style:['italic'], variable:'--font-fraunces' })` and applied to `<html>` as a **raw CSS variable only** — deliberately **NOT registered in globals.css `@theme`**, so no `font-fraunces` Tailwind utility is ever generated. The SOLE code that names `var(--font-fraunces)` is the new **`app/(app)/Display.tsx`** primitive; its required `kind` prop is a **closed union `'listo' | 'casi' | 'count'`** = the literal board allowlist (¡Listo! / ¡Casi! / big review-count numbers), so the allowlist is enforced by the type system + the absent utility, not by convention. Display sets the **face only** (italic 700); size/color come from the caller, so `casi` can be rendered in **ink** per color rule 6. **No usages ship in this slice** — they land with their screen clusters (¡Listo!→add, ¡Casi!→review, counts→Home).
- **Canonical `Button` (board §03)** — `app/(app)/Button.tsx`, 4 variants (primary/secondary/text/destructive) × 3 states (default / `:active` pressed / `disabled`). Polymorphic: renders `<button>` or, when given `href`, a `next/link` (the discriminant is href presence, since `<button>` has no href). **Radius rule:** `full` (block CTA) → 14px; compact → 999 (pill); never inverted. **Family Inter** (font-sans) per the type system, even where the old bespoke button used serif — adopting the component is what unifies that. **No §01 palette role is inlined** — the button consumes globals.css tokens for every §01 role it needs, including the two added in this same slice for it: `--color-faint #A88F6B` (tertiary/captions/disabled text) and `--color-border-soft #E7D8B8` (inner dividers / disabled borders). The only inline hexes are board §03 button-table-specific values that have no §01 equivalent — `#C2A877` (disabled-primary text, an amber-on-amber-light desaturation, deliberately distinct from faint/muted), `#DDA994` (destructive resting border), `#8E3A1F` (destructive pressed text) — plus the pressed-bg tints `#F7E8D0`/`#F4E1D8` (board amber-tint/terra-tint, reconciliation deferred to the §06 cluster); all documented in-file as tracked button-state constants.
- **Button adoption — now vs deferred.** **Now (the board's brown-primary unification):** the two brown-`bg-ink` primaries → amber `<Button variant="primary" full>` — `DrillResult` ("Suivant →"/"Voir le bilan", the board's literal "Suivant →" fix) and `LoadingIdiom` ("Voir la fiche →", **retiring the v0.5.1 dark "ink CTA" pilot** — the system has no dark-brown primary). Left as-is: `EstimateInfo`'s `bg-ink` (a tooltip surface, not a button) and `DrillPrompt`'s `bg-ink/30` (scrim). **Deferred to clusters:** every other already-compliant amber/outline/destructive CTA across `page`/`WordList`/`add`/`discover`/`dictionary`/`review`/`drill`/`login`/`WordDetailActions`/`SwipeRow`/etc. keeps bespoke markup until its cluster migrates it. The /review **rating buttons** (mono-amber gradation) are explicitly **untouched** (board says they already comply).
- **Selection treatments (board §04)** — `app/(app)/selection.ts` centralizes the ONE rule as two className constants: `SELECTION_ACTIVE` (momentary tap = amber fill + ivory text + `shadow-amber-sm`) and `SELECTION_PERSISTENT` (persistent in-progress/due = crème+ tint + 1.5px amber border + ink). **The one global instance wired now = the nav active tab** (`SELECTION_ACTIVE`). Per-screen instances (filter pills → Words, swipe choices → Discover, en-cours/à-réviser word rows → Words) defer to their clusters.
- **Nav coherence paint (board §05)** — `TopNav.tsx`, **visual only, IA/behavior unchanged** (same pills + flag-gated Dictionnaire, horizontal scroll, per-screen grouping, brand lockup, two corner circles; no `+`, no add/remove, no restructure). The nav already largely complied from M5.2 (amber active fill ✓, Lora-bold labels ✓, brand lockup ✓), so the real diff is small: the active pill (5 pills + active Dictionnaire) now routes through `SELECTION_ACTIVE`, **gaining `shadow-amber-sm`** and switching active text `text-white`→`text-ivory` (board specifies ivory #FFFBF3, not pure white). **Deliberately NOT changed:** the inactive pill's M5.2 accent-tinted border/icon (not among the board's three named changes; reconciling it would be a redesign beyond "visual coherence"). **Avatar swap point:** a one-line comment marks the lockup `<Image src="/paco.png">` as the 32px Animando head-shoulders-crop slot — `paco.png` is already Animando, so this is **asset-gated only** (the tighter @1×/2×/3× crop is a separate dependency, logged in `backlog.md`); not blocked on.
- **Out of scope (cluster-deferred, do NOT build here):** the ¡Casi! neutral result screen (review/drill cluster); StatusPill §06 recolor + En cours/En apprentissage presentation-merge + 3-dot gauge (Words cluster — **the 6-band `lib/word-status.ts` taxonomy must not change when that lands**); per-screen button/selection call-site migration; the nav IA restructure (its own later pass); all per-screen Paco placements (only the nav-avatar slot is foundation, and it's asset-gated). The board's later "vibe A/B" mockups show an earlier nav with a "+" — ignored; §05 is canonical.

### From M5.5b (v0.8.1) — Words cluster (design-pass, screen-by-screen)

The first per-screen cluster slice of the design pass: `/words`, `/words/[id]`, and the delete/undo/Relearn surfaces re-skinned onto the v2 board, plus §06 (status pills) built for the first time. **Presentation-only over the taxonomy** — `lib/word-status.ts` (the 6-band partition, `getWordStatus`/`isMemorized`/`getFamiliarity`) and all FSRS/SRS logic are UNCHANGED; the §06 pill set, the En cours/En apprentissage merge, and the 4-dot gauge are display projections. Suite **383 → 394** (11 new `getMasteryGauge` cases). Build green; smoke-tested before the tag.

- **§06 StatusPill — render-layer label-map collapse** (`StatusPill.tsx`). Maps the unchanged `getWordStatus().label` → one of 4 pill kinds: `À réviser` **and** `À rappeler` (Relearning **folded in**, by decision) → the ONE loud amber pill ("À réviser", `bg-accent text-ivory`); `En cours` **+** `En apprentissage` → merged neutral "EN COURS" (`bg-card text-muted border-line`); `Nouveau` → neutral; `Mémorisé` → sage (`bg-ok-bg text-sage-ink border-sage-border`). **No amber/terracotta status TEXT anywhere** — folding Relearning into À réviser removes the last terra status text, keeping terracotta destructive-only per the locked system. Both merged bands stay distinct underneath; the gauge carries the difference. Action-priority is already in `getWordStatus` (a mastered-but-due card reads À réviser).
- **4-dot mastery gauge** (`MasteryGauge.tsx`, replaces the 3-dot `FamiliarityMeter.tsx`). New pure `getMasteryGauge(card): 0|1|2|4` in `word-status.ts` = `getFamiliarity` rendered as quarters (band map New→0, <7→1, 7–30→2, ≥30→**4**). **The 3rd dot is never filled by design** — the board never draws 3/4; a word steps 2/4→4/4 exactly when it crosses the 30-day Mémorisé threshold. Neutral ink fill, empty = hairline border; **no sage gauge dot** (sage reserved for the Mémorisé pill). A vitest case locks the `{0,1,2,4}` image (3 unreachable) and the divergence (biblioteca stability 3 → EN COURS pill + 1/4; me acuerdo stability 12 → same pill + 2/4). It's a projection over `getFamiliarity` — does not touch the partition or bands.
- **WordRow re-anatomy** (shared; ripples to Home): `[7px leading dot] · [word + italic gloss] · [right col: StatusPill ▸ MasteryGauge]`. Leading dot amber on À réviser rows else `border-soft`. Crème+ tint **only** on À réviser rows, via the reconciled `SELECTION_PERSISTENT`. **Dropped the "N révisions" reps line** from the row (Home `WordRow` call + `reps` prop removed). A new `flush` prop drops the row's own border/radius so a swipe container can supply one clipped rectangle.
- **List (`/words`, `WordList.tsx`):** filter pills → `SELECTION_ACTIVE` (amber fill + ivory + `shadow-amber-sm`); sort → discreet text toggle (active = ink/bold + 2px amber underline, inactive faint); search field wrapper focus → amber border + `shadow-amber-sm`, leading search glyph, italic-faint placeholder, amber clear ✕. **Three mascot empty states:** empty deck (0 words) → **Animando** (`/paco.png`) "Ton premier mot t'attend" + primary `Button` "Ajouter un mot" (no search/filter chrome); no-results → **Pensando** (`/paco-pensando.png`) "Aucun résultat" + secondary `Button` "Ajouter « query »"; passive filter-empty → **Durmiendo** (`/paco-durmiendo.png`) with filter-adaptive copy ("Rien à réviser" / "Aucun mot mémorisé"). **No-results uses Pensando — an intentional divergence from the board mock (which drew Durmiendo there):** a puzzled dog fits a search-miss; the sleeping dog stays on the passive "nothing due" state.
- **Delete/undo re-skin.** `SwipeRow` is now **one continuous clipped rounded rectangle** — an inner flex track holds the borderless `WordRow` (`flush`) + a flush terracotta Supprimer panel (`bg-err-bg text-terra-ink`, `border-l border-err`), track translates left to reveal; the clipped `<li>` supplies the single border (tinted on À réviser rows). Kills the old absolute-drawer corner mismatch; snap math (`lib/swipe-row.ts`) + reveal-and-tap behavior unchanged. Undo toast (`DeferredDelete.tsx`) restyled **dark**: `bg-ink` surface + `shadow-menu`, `amber-light` trash, ivory Lora copy, **`amber-mid` underlined "Annuler"**, and a 3px `bg-accent` countdown bar (`toast-countdown` keyframe, reduced-motion → static full bar; keyed on the labels so a replacement toast restarts it). Timer/undo logic untouched.
- **Detail (`/words/[id]`).** Actions moved into a top-right **⋮ overflow menu** (`WordDetailActions.tsx` repurposed → `MoreVertical` button, active = `border-accent bg-amber-tint text-amber-deep`; popover `shadow-menu`, outside-pointer + Escape close): "Remettre à réviser" (normal, `RotateCcw`) + "Supprimer" (destructive, `text-err`). **Bottom `StickyActions` button pair removed.** Relearn still = `reset-schedule` (no confirm modal; the pill flipping to À réviser on `router.refresh()` IS the confirmation). Delete still routes back to `/words` through the layout-level deferred-delete primitive (toast carries over). Back arrow → **labeled "‹ Mes mots" → `/words`** (was a bare "←" → "/"). Status pill + 4-dot gauge carried at the top, consistent with the rows. FR/Traduction reveals were **already two-way** — re-skinned to the board TextLink (amber underline + chevron, "Voir en français" ↔ "Masquer le français", "Traduction" ↔ "Masquer la traduction"). Content cards → `rounded-[16px]` + board eyebrow. **No phonetics line:** verified there is no IPA field anywhere (the enrichment schema is `definition{es,fr,pos}`/`lemma`/`form_annotation`/`examples`/`distractors`; `audio_urls` is the MP3 URL) — the mock's `/me aˈkweɾ.ðo/` was placeholder. **"Mots similaires" label kept** — the `distractors` field is MCQ confusables, not true family words, so the board's "Mots de la même famille" would mislabel the data (no separate family field exists).
- **Token reconciliation** (resolves backlog item (b), deferred from M5.5a): `--color-ok-bg #D4EDE9 → #DCEAE5` (board sage-tint = MÉMORISÉ pill bg), `--color-err-bg #F5E0D8 → #F4E1D8` (terra-tint = swipe panel bg), `--color-tint #F5E0C0 → #F6E7CC` (board tint). **Added** (each labelled board-T-object vs new-to-repo in `globals.css`): `--color-sage-ink #2F5A4F`, `--color-sage-border #BBD6CF`, `--color-tinted-border #E3CFA6`, `--color-terra-ink #8E3A1F`, `--color-amber-tint #F7E8D0`, `--color-amber-mid #D6924A`, `--shadow-menu`. The reconciled ok-bg/err-bg ripple to the review MCQ/bilan/drill tints — intended global-system alignment (tiny hue shifts), verified.
- **§04 reconciliation:** `SELECTION_PERSISTENT` softened from `border-[1.5px] border-accent` to `border border-tinted-border` (board-exact) — an amber border on every due row reads too loud; the amber accent on an À réviser row is carried by its 7px leading dot. This is now the single canonical "persistent" treatment (no amber-border variant in use), and `WordRow`/`SwipeRow` consume it so the primitive matches what ships.
- **Shared-renderer ripple (intended):** `StatusPill` / `MasteryGauge` / `WordRow` are shared with the Home preview, so the §06 recolor + 4-dot gauge appear there too — the global system. Home's full composition is a later cluster; this slice only keeps it from breaking.
- **Asset:** `public/paco-durmiendo.png` (sleeping Paco) optimized via `sips -Z 440` → **96 KB** at 440px, matching the `paco-pensando.png` family (~110 KB). Mascots render via `<img>` (the established Paco convention — `<img>` lint warnings are the same ones the existing Paco surfaces carry).

**Pre-tag refinements — final, per the post-board-review brief (`BRIEF-words-adjustments.md`); folded into the same branch, still presentation-only.** (An interim pass had tried a vertical-left gauge + restored reps + a spelled-out POS eyebrow; the board review reverted all three to the specs below — only the final state ships.)
- **Gauge re-bucketed so all four dots fill smoothly** (#1, `getMasteryGauge` is now a direct pure function of `stability`, no longer `getFamiliarity`-derived): New→0, `0<s<7`→1, `7≤s<15`→2, `15≤s<30`→3, `≥30`→4. New constant `GAUGE_MID_STABILITY_DAYS = 15` (the 2/4↔3/4 line — the midpoint of the existing 7–30 "En cours" band; `7` reuses `LEARNING_STABILITY_DAYS`). The previously-empty 3rd dot is now reachable; the 15-day split is **gauge-display only** — it does NOT subdivide the status bands or the partition. Test image `{0,1,2,3,4}` (boundary cases at 7/15/30; the due+mastered → 4/4 + À réviser lock stays).
- **Gauge orientation: horizontal, in the RIGHT column, centered UNDER the status pill** (#2). The right cluster is `flex flex-col items-center gap-2` and stays right-aligned in the row; `items-center` centers the 4 dots beneath the wider pill (the brief's exact recipe). The 7px leading À réviser dot is **kept** (amber on due rows, else border-soft). `MasteryGauge` is horizontal-only again (the interim `vertical` prop removed).
- **"N révision(s)" removed from list rows — kept on the detail only** (#2): a scan row carries headword + gloss + pill + gauge; the raw count is detail-grain and overlaps the gauge's "how worked-in" signal. The detail keeps its stats line ("Révisé N fois — …"). The `reps` prop was dropped again from `WordRow`/`SwipeRow`/`WordList`/Home. (Shared with Home — applied there too.)
- **Grammar (POS + FORME) unified across the /words detail AND the /add enrichment card** (#3). **POS = inline, abbreviated, baseline-aligned right after the headword** (Inter 14.5/500 `text-muted`/sépia) — the spelled-out "VERBE"/"NOM · MASCULIN" eyebrow is **removed** from the detail. New shared `posAbbrev(pos)` in `lib/discovery.ts` is the ONE map both cards route through (stored pos → FR abbreviation: `prep.`→`prép.`, `v.pron.`→`v.`, rest identity); **gender rides in the `n.m.`/`n.f.` abbreviation** (why abbreviated beats the eyebrow, which dropped gender). `posEyebrow` reverts to discovery-deck-only (its M5.5b gender-from-pos extension removed). **FORME stays conditional on both cards** (shown only when `form_annotation` is present = an inflected form; omitted for base forms) — already true on `/words`, confirmed on `/add`. `deckArticle` (el/la) still not used on the detail (conjugated headwords; logged to `backlog.md`). 7 new `posAbbrev`/`posEyebrow` tests (`lib/discovery.test.ts`).
- **Bidirectional sort** (#4) — re-clicking the active sort flips its direction; a `ChevronUp`/`ChevronDown` amber caret renders on the active sort only. Defaults: Alphabétique A→Z, Date newest-first, Familiarité least-familiar-first. Implemented as an ascending base comparator per sort + a `dir` reversal (negation flips familiarité's stability tiebreak too); `dir` joins `viewSig` so the load-on-scroll cap resets on a direction flip. Suite 383→**402**.

### From M5.5c (v0.8.2) — Add-flow cluster (design-pass, screen-by-screen)

The `/add` journey re-skinned onto the v2 board per the six canonical add-flow boards (`~/Downloads/design_handoff_ajouter`). **Re-skin of already-shipped behavior** (two-step add, did-you-mean, lemma normalization, loading polish, conjugation interstitial, duplicate detection, bulk similaires-add) — **plus the one specced-but-never-firing loading animation, finally wired.** Two boards (④ duplicate, ⑥ success) had no current screen equivalent and were built (the behaviors existed, the screens didn't); confirmed with Omar before building. No taxonomy/FSRS/conjugator change. Suite unchanged (402). Tests + lint + `next build` green. **Tag held until the device smoke-test confirms the animation fires + degrades** (per the brief — do not assert from spec).

- **Loading choreography — FULLY WIRED to `MOTION-loading.md` (canonical)** (fixes the backlog "PhaseChecklist active-indicator animation not firing"). The old `motion-safe:animate-pulse` + `···` span never rendered. Keyframes moved into `globals.css`, values **verbatim from the spec** (an interim pass had stale values, corrected here): `pacoRing` (halo, **1.8s**) + **`pacoCore`** (center dot opacity .55↔1, 1.8s — synced so the indicator reads as one pulsing object, not a static dot in a moving ring); `pacoDot` (three rising dots, 1.35s, peak 36%, stagger 0/.15/.30); `pacoBreathe` (bust, 3.4s, now with micro-`scale(1.015)`, off-tempo vs the ring). **The transition that was the main gap:** `pacoRingPop` (0.28s overshoot) + `pacoCheckDraw` (0.30s stroke draw-on, `pathLength=1` inline SVG — the ✓ *draws*, not cuts in) fired together once per phase. **One-shot re-fire:** `PhaseRow`'s indicator is **keyed by state** so a pending→active→done flip remounts it (a CSS one-shot on a persistent node plays once for the whole screen, not per step). `pacoEnterUp` mount-once entrance: divider (`.paco-enter`) + idiom card (`.paco-enter-2`, +0.12s lag). All `.paco-*` animations declared ONLY in the `no-preference` block → **honest reduce fallback** (solid ring + full-opacity core + dots @ .6 + ✓ instant + content in place; the JS timeline still advances). All halos are `box-shadow` so the idiom never jitters.
- **Phase pacing — deterministic dwell + data-gated step 4 + min-visible floor** (replaces the old `[1100,1700,2100]` flash). `LoadingIdiom` now owns a single `doneCount` timeline (`MOTION-loading.md` §5): steps 1–3 dwell **`PHASE_DWELL = 850ms`** each (decoupled from data, so they read as deliberate beats; active/done overlap on the same frame — no dead gap), step 4 (Phonétique) **holds for the real enrichment promise**. The loading→¡Listo! reveal moved INTO `LoadingIdiom` and is gated on **`max(dataReady, MIN_VISIBLE_FLOOR_MS = 3050)`** so even an instant resolve shows the full ~3.0–3.2s choreography; honest hold if data is slow. `PHASE_DWELL` / `MIN_VISIBLE_FLOOR_MS` are tunable top-level constants (Omar tunes on device). `page.tsx` unchanged — it still flips `phase→ready` with `result` when enrich resolves; the component decides *when to visually reveal*. No auto-advance to the fiche (¡Listo!→fiche stays the user's tap).
- **¡Listo! CTA clipping — root-cause fix.** The ③ "Voir la fiche →" CTA was cut mid-button on mobile AND desktop. Root cause: the `ready` view was the only add screen that bottom-anchored its CTA inside a hard-coded `min-h-[calc(100svh-4rem)]` column + a `flex-1` spacer — but `TopNav` is two rows (taller than the assumed 64px), so the column ran `100svh + (navHeight−64)` and its bottom edge (the CTA) sat below the fold (clipped on both, since it's a wrong-height assumption, not a mobile safe-area issue). Fix: render the CTA via the shared **`StickyActions`** (`position:fixed; bottom-0` + `env(safe-area-inset-bottom)`) — the exact pattern the fiche uses and that renders fine — and drop the min-h column (ready view → normal flow `pb-24`). No height math; matches the board's "CTA pinned bottom".
- **¡Listo! → Fraunces, the FIRST real `Display` consumer.** The ③ headline now renders through `<Display kind="listo">` (M5.5a shipped `Display` unused; this is its first usage). Preview card re-skinned (radius 18, headword + inline `posAbbrev`, ES gloss, four sage ✓ lines). The only Fraunces in the flow — the closed-union allowlist is intact.
- **Input placeholder — rotating, curated** (`WordInput.tsx`). Replaces the fixed "mariposa" (the one Omar's sick of) with a random pick per visit from a 10-word curated array (ventana · biblioteca · manzana · estrella · montaña · camino · tortuga · paraguas · cuchara · naranja), advancing on focus. Input re-skinned: focus → amber border + `shadow-amber-sm`, terra ring on error. Autocomplete dropdown re-skinned **presentation-only** (search glyph, typed-prefix bolded, first row `amber-tint` + trailing amber →, `border-soft` dividers, `shadow-menu`) — the accent-tolerant **matching** bug stays a separate backlog item.
- **④ Duplicate — new compact screen** (board ④, replaces the full-fiche-for-duplicate). A typed word already in the deck now lands on a centered "« mot » est déjà dans ton deck" screen: `paco.png` 96px + the shared **`WordRow`** (links to `/words/[id]`) + primary "Ouvrir « mot » →" + "Chercher un autre mot". **Behavior change (intended, logged):** the old full enriched fiche + its "Réinitialiser le calendrier" action are retired from the add flow — **reset-schedule capability is preserved** via the word's detail ⋮ menu (M5.4b). `handleResetSchedule` removed from the add page (the `/api/words/reset-schedule` route stays — the detail page still uses it). New phase `exists`.
- **⑥ Success — single + multi** (board ⑥; both built, with honest copy). **Single** (`success_single`): after the **awaited** save → centered `paco-feliz.png` + "« mot » est dans ton deck" (a true "done" — save is confirmed) + shared `WordRow` (NOUVEAU/0-gauge, links via the new save id) + "+ Ajouter un autre mot" / "Voir mes mots". **Multi/similaires** (`success_multi`): the bulk-add stays **fire-and-forget background** (Omar: don't claim a success that hasn't completed) — the screen frames it honestly as in-progress: "**N mots en route** · Tes mots sont en train d'être ajoutés… ça peut prendre jusqu'à 20 secondes. Tu peux continuer à naviguer." N non-linking `WordRow`s (words not yet created → no id). **The old floating add toast is removed** (the screen supersedes it). **KNOWN LIMITATION (logged in backlog, revisit before soft-launch):** a failed background bulk-add is no longer surfaced inline — only console-logged.
- **`WordRow` extended (reuse, not fork):** `id` is now optional → renders a non-link `<div>` (same anatomy) when absent, so ⑥-multi can reuse the shared row for not-yet-created words. `asListItem`/`flush` unchanged.
- **Data plumbing (minimal, additive — no schema change):** `/api/words/save` returns the inserted `id` (`{ok,id}`) for the ⑥-single row link; `/api/words/enrich` deck-check embed `review_cards(id,due)` → `(id,due,state,stability)` and returns `cardState`/`cardStability` (via `oneEmbed`) so ④'s `WordRow` renders the real pill + gauge.
- **Re-skin via the canonical `Button`** across all add screens (entry, did-you-mean cards, lemma two-path, fiche bottom bar = icon-only 52px back + primary, both success screens). Fiche kept: inline `posAbbrev` (shared), conditional FORME, two-way FR/Traduction reveals, deterministic `ConjugationGrid` (re-skin only, never LLM), the lemma two-path logic + copy, ceremony-fatigue (no streak/confetti). All board tokens already existed (M5.5a/b) — **no new tokens.**

### From M5.5d (v0.8.3) — Discovery cluster (design-pass, screen-by-screen)

`/discover` re-skinned onto the v2 board per the Discovery boards (`~/Downloads/design_handoff_decouvrir`). **Re-skin of shipped M5.1 behavior** (topic generation + fling-commit swipe) — not a re-architecture. Two real fixes ride along: the swipe-color role inversion and the loading-choreography reuse. No FSRS/generation/decide logic change; suite 402; tests + lint + build green. **Tag held for the device smoke-test.**

- **Loading choreography — REUSED, not rebuilt** (brief ②). Extracted the `/add` choreography (M5.5c) into a shared **`app/(app)/LoadingChecklist.tsx`** (breathing-Paco checklist card + per-phase ring-pop + check stroke-draw with the state-keyed remount + "Pendant que tu attends" divider + idiom card + dwell timeline + min-floor + reveal gate). Parametrized by `title` / `phases` / `ready` / `onReveal` (+ tunable `phaseDwellMs` / `minFloorMs` / `shellDelayMs`); reuses the `.paco-*` classes already in `globals.css` (Discovery's `#paco-disc2-css` was byte-identical — pure reuse, **no new keyframes**). `/add` `LoadingIdiom` refactored to consume it (keeps its own ¡Listo! + error + StickyActions CTA; behavior identical to v0.8.2). `IdiomCard` moved `add/` → `app/(app)/` (shared). Discovery generation renders `LoadingChecklist` in the focused overlay with theme phases (**"Sélection des mots du thème · Définitions & exemples · Mots à ne pas confondre · Phonétique"**), longer pacing (`phaseDwellMs=950`, `minFloorMs=3800`, `shellDelayMs=0`); **data-flow change:** `startTopic` stores the batch in a ref + sets `genReady` instead of flipping to `deck` — `LoadingChecklist` reveals at `max(dataReady, floor)` → `onReveal` routes to deck (or exhausted if empty). Retires the old `motion-safe:animate-pulse` skeleton loader.
- **⑤ Swipe colors — the role fix (real correction).** M5.1 had them inverted (terracotta on "Je connais", sage on "À apprendre") — violating the locked roles (terracotta = destructive-only; sage = acquisition). Corrected: **"Je connais" = sage** (left swipe), **"À apprendre" = amber** (right swipe). **Directions + gesture unchanged — only the color layer.** Applied to the stamps + the two action buttons (Je connais = `bg-card` `sage-border` `sage-ink`; À apprendre = `amber` fill `ivory` `shadow-amber-sm`).
- **`SwipeCard` enhancements** (generic, opt-in; discovery is its only consumer today): board-correct **stamp placement** (right-swipe stamp top-right `-11°`, left-swipe top-left `+11°` — the old code had them on the wrong sides), optional **`rightWash`/`leftWash`** color overlays (amber/sage `.15`, opacity tracking drag), and a **`lift`** flag (`--shadow-lift`, new token) while dragging. Threshold/fling logic untouched.
- **④ Discovery card — inline `posAbbrev`** (brief / board): the spelled-out `posEyebrow` dropped from the deck face for the shared inline abbreviation, full coherence with /words + /add (`posEyebrow` now has no caller — kept exported + tested for the future). Card stays **minimal** (hero headword + el/la via `deckArticle` + posAbbrev · gloss + one example, target word bold amber). **Divergence (deliberate):** the board draws a speaker on the card; the **brief enumerates "word + POS + gloss + one example"** and the deck card has no audio URL (audio lives on the kept word's `/words/[id]`), so the speaker is **omitted** — also avoids an interactive child on the swipe surface (the M5.4b pointer-capture-eats-click trap).
- **① Theme grid re-skin** + §04 selected tint (crème+ + amber border + amber-fill icon tile + `→`, never amber-fill the card; on tap → generation). **"Pour toi" band — non-functional placeholder shell** (brief 1b): two horizontal featured cards (amber-filled icon tile, amber kicker) for **adjacency** + **A2–B1**, each marked with a neutral **"Bientôt"** chip (not the live `→`); tapping shows a warm Paco-voice "pas encore disponible" message in the **dark bottom toast — same treatment as the delete "« … » supprimé" toast** (`bg-ink` + ivory copy + `amber-light` Sparkles, auto-dismiss ~4 s, **no terracotta**). The features stay deferred — **adjacency = M5.1b**, **A2–B1 = the content-gate seeding** (logged); this is just the wired shell to activate when they land.
- **⑥ End states re-skin:** Thème terminé → **Feliz** (honest tally, count bold `amber-deep`, the "arrivent dans Mes mots — jusqu'à 30 s" note consistent with the /add background-add limitation; **no streak/score/confetti**); deck exhausted → **Durmiendo** ("Tu as fait le tour de « … »"). Deck header (✕ circle + theme eyebrow + tabular counter + amber progress) + ghost-card stack re-skinned. `Button` used for all CTAs. **No terracotta anywhere in the cluster.**

### From M5.5e (v0.8.4) — Review cluster (design-pass, screen-by-screen)

The core daily loop (`/review` — FSRS QCM + écriture) re-skinned onto the v2 system per the six Review boards. **Re-skin of shipped behavior + three real fixes + one signed-off new behavior (auto-advance countdown).** Drill (`/drill`) is a separate mode, out of scope (shares only `AnswerBlank`/`AccentBar`, unchanged; `RatingButtons`/`ResultReveal` are review-only). **`computeRating` + FSRS scheduling UNTOUCHED.** Suite 402→411. Tests + lint + build green. **Tag held for the device smoke-test** (countdown fires + degrades; auto-advance + stop both work).

- **Fix #1 — écriture lemma-reveal, conditional by card type** (new pure tested `lib/review-cue.ts` `verbCue`). Verb cards get a context chip `infinitif · <tense> · <person>` in **Spanish** (Omar's call — honors the M5.3c "grammar in accented Spanish everywhere" standing decision; the board's French mock is a deliberate divergence), derived deterministically via `analyze` + `unambiguousPerson` + `canDisplayParadigm` + `PERSON_LABELS` + the drill's short tense labels — **degrade-safe** (untrusted/ambiguous → drop tense/person, keep just the infinitive, never a guess; never LLM). Non-verb cards get `posAbbrev · <FR gloss>` — **never the lemma** (that's the answer). The Indice now reveals the first letter for ALL cards (the persistent chip subsumes the old lemma/person hint); `hintUsed` penalty unchanged.
- **Fix #2 — ¡Casi! chromatically NEUTRAL** (ink, color rule §6 — was amber). `ResultReveal.close` → `text-ink`; the near-miss crème+ teaching card. **The verb-form mismatch now also shows the neutral ¡Casi! face** (`QUALITY_TO_VERDICT.wrongForm: 'close'`, was `'wrong'`/¡Uy!) with "le bon verbe, l'autre forme" + "LA FORME ATTENDUE" + the deterministic conjugation descriptor. **Verdict layer only — `computeRating` untouched** (Omar's call): wrong-form still *suggests* À revoir, near still suggests Bien/Difficile per the existing logic; zero FSRS-input change (accepted minor verdict↔suggested-pill incoherence). ¡Uy! mascot → `paco-pensando` (board; the app surfaces the answer, doesn't punish).
- **Fix #3 — rating = single-hue amber gradation, NO dots** (`RatingButtons`). À revoir `amber-deep` → Difficile `accent` → Bien `amber-mid` → Facile **`amber-pale` (new token `#E3BC8A`)**, text ivory except Facile→ink. A pill is filled with its own tone when selected, or — before any pick — the pre-selected suggestion (`result.rating`, unchanged). **No per-button dot row** (the 4-dot atom is word-level mastery; dots here would invert it). Only `amber-pale` added; the other three reuse existing tokens.
- **NEW behavior — auto-advance countdown** (`RatingButtons`, signed-off; refined on device). After grading, a **neutral** "Prochaine question…" bar (fill `faint` — a clock, not a signal, never amber/verdict) fills `0→100%` over **`AUTO_ADVANCE_MS` (one tunable top-level const = 10000)**; on completion the **suggested** rating applies + advances. **The bar is JS-driven** (a ~120 ms interval computes `progress` + sets the inline width with a short `width` transition) — the earlier CSS-keyframe (`pacoCount`) fill rendered static on device, so it was removed. **Stop affordances:** tap the currently-effective pill (selected, else the suggestion) → **advances immediately** (one tap); tap a *different* pill → selects it + cancels the timer; **↵** advances now; **pause (▐▐) = STOP** for that card (terminal — no resume/▶). **No captions** (the "Sans rien faire…" / "Prends ton temps…" lines were removed — a "show only for a user's first few answers" hint is backlogged, needs a persisted count). **Honest reduced-motion fallback:** a discrete per-second numeric "<n> s" readout (same interval) instead of the bar. `timeMs` frozen at grade time so the countdown never inflates it; the auto-applied rating feeds FSRS via the unchanged `onRate` payload.
- **Crash fix (device, card-10): no raw bilingual object reaches React.** The new M5.5e écriture context chip + ¡Uy! reveal rendered `definition.fr` raw; a card with the historic pre-M2.5 nested `{es,fr}` weak shape threw *"Objects are not valid as a React child (keys {es, fr})"*. Fixed at the **data boundary**: new tested `lib/bilingual.ts` `flatBilingual(value, side)` (string → itself; `{es,fr}` → the side; one level of nesting; else `''`), applied in a single shared `review/mapCard.ts` `mapReviewRow` that both the server page and the client "Encore N" refetch use to build `ReviewCard` — normalizing `definition.es/fr` + every `examples[].es/fr` to plain strings, so every consumer (chip, reveal, MCQ "Voir en français", session-complete recap) is safe.
- **Display union extension (gate intact).** `Display` widened to `…|'esoEs'|'uy'|'buenTrabajo'`; ALL Fraunces faces now route through the allowlist — `ResultReveal` verdicts (¡Eso es!/¡Casi!/¡Uy!), session-complete "¡Buen trabajo!", and the entry count — never raw Fraunces. (These were Lora before; first time the verdicts get the Fraunces face.) Colour comes from the caller (esoEs→sage, casi→ink, uy→terra).
- **① Entry — the re-skinned card lives on HOME; `/review` auto-starts** (the separate `/review` landing was the wrong read and was removed — `ReviewEntry.tsx` deleted). The **Réviser pill goes straight into a session.** The Home review card (`app/(app)/page.tsx`) is now the re-skinned **crème+ hero** (a SURFACE, never amber-filled): eyebrow "Révision disponible" (`amber-deep`) with **"≈N min" (Clock) top-right**, the due count via **`Display kind="count"`** (Fraunces), "mots à revoir", and a primary `Button` "Commencer la révision →" → `/review`. **No "QCM & écriture mêlés"** line; CTA pulled up (thinner card). `/review` with 0 due → the restful **Durmiendo** passive ("Rien à réviser" + outline "Découvrir de nouveaux mots"). The "≈N min" estimate is the shared `lib/review-estimate.ts` (`estimateMinutes`), which **Home consumes** (one formula; `EstimateInfo` infotip dropped from the card).
- **② QCM / ③ écriture / ⑥ session-complete re-skin:** QCM options as controls (correct = sage + check badge; wrong-picked = gentle terra 1px + soft strike + × badge; others faded); verdict head on top; non-punitive. Écriture answering/grading re-skinned (crème+ ¡Casi! cards, surface ¡Uy! reveal, amber-left example callout). Session-complete: **"Sus du 1er coup"** stat (correct-without-hint) **replaces "Réussite %"** (non-punitive), word list sage-✓ / neutral-✗ rows, "¡Buen trabajo!" Fraunces, "Encore N" only when cards remain. In-session header (× / n/N / mode + amber progress) kept.

### From M5.5f (v0.8.5) — Écriture context/hint model + QCM instruction

Reworks the écriture card's help system (the M5.5e always-shown context chips were redundant with the FR sentence translation, which already gives the meaning — a second giveaway) into a leaner **penalized 3-tier Indice**, and adds the missing QCM instruction. Per the revised écriture board ③ + logic panel. **Countdown untouched** (v0.8.4 is final); FSRS scheduling math untouched (only the suggested-rating cap changes). Suite 416→424. Tests + lint + build green. **Tag held for the device smoke-test.**

- **Always-shown context — removed for both card types** (`FillInBlank.tsx`). The M5.5e `contextChip` (verb cue / non-verb posAbbrev·gloss) is gone; the answering state shows only the eyebrow, the Spanish sentence + amber blank, and the FR translation. The verb's form moves into Indice tier 1.
- **Tiered Indice — one button "Indice · n/3", 3 tiers, penalized, type-aware.** `hintLevel` (0–3) replaces the old `hintUsed`. Each press reveals the next tier (not pre-announced); disabled after 3. **Non-verb:** T1 first letter (a faint `ghost` in the blank, via a new optional `AnswerBlank` prop) · T2 the **ES monolingual definition** (`definition.es`, "DEFINICIÓN · ES" card, pre-authored, never LLM) · T3 scramble. **Verb:** T1 the **form** pill `v. · ‹tense› · ‹person›` (Spanish, via `verbCue`) · T2 the **`ConjugationGrid` with the target cell blanked** · T3 scramble. **Degrade-safe:** verb T1 → first-letter if the form can't be determined; verb T2 → ES definition if grid coords don't resolve (new `verbGridCoords` in `lib/review-cue.ts`, gated on a single finite tense + unambiguous person + `canDisplayParadigm`). Scramble = a new pure tested `lib/scramble.ts` `scrambleLetters(word, seed)` (deterministic, seeded by `card.id`, ≠ original). **Brief-over-README:** the brief's verb tiers (T1 form / T2 table) are canonical; the README §③ prose (verb T1 = table) was stale.
- **Rating cap by hints used — replaces the `hintUsed` penalty** (`lib/rating.ts`). `computeRating` param `hintUsed: boolean` → **`hintLevel: number`**; the base rating is then capped via `HINT_CAP = [4,3,2,1]` (0→Facile possible · 1→Bien max · 2→Difficile max · 3→À revoir). The old `if (hintUsed) rating = max(1, rating−1)` line is **deleted** — no double count. **The cap is écriture-only** (it lived in the blank branch before too); MCQ's FR-reveal still doesn't affect its rating (MCQ passes `hintLevel = frReveal ? 1 : 0`, ignored by the mc branch — verified by a test). A caption under the controls shows "N indice(s) utilisé(s) · note max suggérée : ‹label›". `onRate` now carries `hintLevel`; `ReviewSession` derives `hint_used = hintLevel > 0` (API + `firstTry`).
- **`ConjugationGrid` — three-way reuse, all deterministic** (`ConjugationGrid.tsx` + two opt-in props, omitted by /add + drill so those stay identical): **Indice 2 (verb)** = `blankTarget` (highlighted cell → amber underline, pattern-completion); **¡Casi! verb-form + ¡Uy! verb verdicts** = the filled+highlighted grid (replaces the M5.5e plain-text verb reveals); plus an optional **`infinitive`** header prop naming the verb (the tier-1 pill has no infinitive). Non-verb verdicts keep their surfaced-answer reveal. Falls back to a plain reveal when coords don't resolve.
- **QCM instruction eyebrow** (`MultipleChoice.tsx`): "Choisissez la bonne réponse", matching écriture's "Complétez la phrase" register — every exercise now carries an instruction.

### From M5.5g (v0.8.6) — Dictionnaire cluster (design-pass, screen-by-screen)

Re-skins the whole Dictionnaire surface (nav pill, Home card, locked screen, populated index, unlock celebration) onto the v2 board, **preserving all M5.2b logic** (collation, jump rail, sticky unlock flag, audio, `/words/[id]` routing), plus **two behavioural changes**. Presentation-only except the trigger relocation. Suite 424→428; lint + build green. **Tag held for the device smoke-test.**

- **Unlock reveal relocated to review-end** (the primary change). The flag-flip + celebration no longer fire on Home/app load; they're anchored to the END of the review session that crosses 10 memorized: the normal bilan shows first, then tapping **either** bilan exit (Encore / Accueil — confirmed: intercept both, since our shipped bilan has two exits, not the mock's single "Continuer") intercalates the takeover **once**; "Plus tard" resumes the tapped exit, "Ouvrir mon dictionnaire" → `/dictionary`. The Home/app-load `<UnlockSync/>` check stays as a **safety-net fallback** (fires only if the user bailed before the bilan).
- **Flip-on-show, not flip-on-evaluate** (the load-bearing decision). `actions.ts` split: **`evaluateDictionaryUnlock()`** is now **read-only** (`{ shouldCelebrate, memorizedCount }`, no DB write); the flag is flipped by **`markDictionaryUnlocked()`**, called from the **client mount of the celebration itself** (`UnlockTakeover` at review-end, or the safety-net interstitial). So `dictionary_unlocked` is an honest "ceremony has been shown" once-guard — a user who reaches the bilan then quits before tapping an exit does NOT lose the celebration (the flag is still false → the Home safety-net still fires). Both actions stay Server Actions triggered from client mounts, never RSC-render side effects (the M5.2b prefetch-flip lesson). No new `unlock_celebrated` column. `syncDictionaryUnlock()` now only `redirect`s (when `shouldCelebrate`); the interstitial page guard renders when `unlocked || memorizedCount ≥ threshold` (the flag is false on first arrival).
- **Takeover milestone number = live memorized count at the crossing** (≥10), read from `getDictionaryState` via the read-only evaluate (review path) / the interstitial page (safety-net) — the SAME source as the dictionary/home count, never hardcoded 10. Stable through the bilan (no reviews occur during it).
- **Shared `UnlockTakeover.tsx`** (one component, two mount points): re-skinned celebration — sparse warm confetti (the ONE sanctioned ceremony) + Paco Feliz over a radial halo, "DÉBLOQUÉ" eyebrow, headline, **Fraunces milestone count**, body, amber "Ouvrir mon dictionnaire" + ghost **"Plus tard"** (the board's "Continuer la révision" string is ignored per the brief). Entrance/idle motion (`lx-pop`/`lx-bob`/`lx-halo`/`lx-rise` in `globals.css`) gated under `prefers-reduced-motion: no-preference`, base = finished state. `Confetti.tsx` re-skinned to the board drift (~12 pieces, amber/amber-mid/amber-deep/sage, dots+squares; the old `/dictionary/unlocked` "¡Listo!" design is retired).
- **`Display` union +`'milestone'`** (genuinely-new allowlist entry) for the unlock count. Fraunces appears in this cluster in exactly two places: the Home review-hero count (`kind="count"`) and the milestone count.
- **Populated index re-skin** (`DictionaryIndex.tsx`, logic untouched): masthead 34px Lora title + eyebrow + count + hair rule; letter heads = italic amber + flex-1 hair rule; rows = Lora 20/700 headword + italic gloss + the new **`AudioButton variant="circle"`** (36px bordered speaker — reuse, not a fork); the **jump-rail held/magnify state** (transient `heldLetter`/`heldY` distinct from the passive scroll `activeLetter` → amber rail pill + a 62px amber magnify bubble that tracks the finger); a cosmetic bottom fade.
- **Row gloss = Spanish (`definition.es`) — a recorded decision, not a "Spanish-first override."** The dictionary row shows the Spanish sense because the dictionary is an index of words the user has **already memorized**: the Spanish reinforces the learning frame, and the French stays available at the word fiche. The board mock's French gloss + "Glose FR" anatomy plate are superseded for the row. (See the corrected M5.2b note above.)
- **Locked screen / Home card / nav pill** re-skinned to the board: Pensando + a calm progress card (locked screen); amber-light icon tile + chevron (unlocked Home card) / dashed + lock + progress bar (locked Home card); the **locked nav pill** refined to dashed `tinted-border` + `faint` + opacity-85 (the 5 always-on pills + the inactive-border reconciliation stay the deferred nav-IA pass — untouched). Dictionary icon: **lucide `BookA` (vector) everywhere** — the nav pill (active ivory / resting `accent/60`, like its five siblings) and the Home card tile (amber-deep on amber-light). (The brief deferred the icon to the Accueil pass; the handover raster book+A-Z+ribbon was tried — nav pill, then Home card — but the PNG read darker/softer and inconsistent next to the vector pill icons, so it was dropped entirely; no `dict-book-*.png` ships. A single tintable book glyph is an Accueil-pass call.)

### From M5.5h (v0.8.7) — Conjugaison drill repaint (design-pass, screen-by-screen)

Repaints the shipped /drill flow (M5.3c: setup → prompt → result → bilan) onto the v2 board. **Presentation-only** — grading verdict tiers, practice scheduling, deck-only sourcing, the <5-verb unlock, `profiles` prefs, and every query are untouched — **plus the one new label helper.** Suite green (428); tsc + lint clean. **Smoke-tested clean on device; tagged v0.8.7 on the close commit.**

- **`tenseLabel(key)` — one pure, tested helper, the single tense-name source for every drill surface.** Returns the FULL Spanish display names (`Presente`, `Pretérito indefinido`, `Imperfecto`, `Futuro`, `Condicional`, `Subjuntivo presente`). Every spot that renders a tense name now consumes it — the setup chips (were inline `DRILL_TENSES[].label`, now `tenseLabel`; `DRILL_TENSES` reduced to the ordered key list), both focus-header tags, the cue parenthetical, the result teaching line, and the bilan missed-list. Regression-locked in `drill.test.ts`. The **short** `drillTenseLabel` survives ONLY for `lib/review-cue.ts` (the review cue chip — a different surface, deliberately not changed).
- **DELIBERATE DECISION (Omar's, not a unilateral change) — drill tense names stay Spanish, routed through `tenseLabel`.** The chips / headers / cue / teaching line / bilan name tenses in Spanish *by choice*: (1) the `ConjugationGrid` headers shown on the same screens are inherently Spanish (`Pretérito indefinido · passé simple`), so French chips would split one screen's grammar vocabulary across two languages; (2) Spanish grammar terms reinforce the learning frame (think in the target language); (3) it's consistent with the M5.5g dictionary row-gloss call (Spanish reinforces, French stays at the fiche). Omar also chose the FULL names over the short ones, and **`Pretérito indefinido`** over `Pretérito perfecto simple` — the rename was applied app-wide to the shared `ConjugationGrid.labelEs` too (so /add + /review verb tables match; `resolveGridTense` still parses either annotation as input). The choice is structurally enforced by funnelling every surface through the one `tenseLabel` helper, so a future relabel is a one-line, test-locked edit. French stays for instructional prose (the teaching-line frame, verdict copy).
- **Teaching line: French frame, Spanish tense names.** Per that decision, `drillTeachingLine` keeps its French prose ("Tu as donné …, on attendait …") but pulls the tense names from `tenseLabel` (Spanish) instead of the deleted `TENSE_FR` map. The five teaching-line test assertions were updated to match.
- **`¡Casi!` is chromatically neutral (board §6).** The near-miss verdict word and the "ta réponse" word are ink (no semantic hue); the focus-header bar stays amber. `¡Eso es!`→sage, `¡Uy!`→terra. The old `warm` token no longer appears in the drill. Wrong/near answers dropped the strike-through (surface the right form, don't punish the wrong). The teaching-line callout became a calm crème+ box (`surface-alt` + `tinted-border`), dropping the left-accent rule + mini-Paco.
- **`Voir la conjugaison` blanks the asked cell** before answering — `ConjugationGrid blankTarget` (the same réviser Indice-2 pattern-completion reused by `FillInBlank`): the other five forms show, the target is an amber blank, caption reworded to "La case à compléter…". The result-screen grid stays fully revealed (the answer is in).
- **CTAs through the canonical `Button`** (Inter, amber, `shadow-amber`, 14px radius) on every screen (Commencer / Valider / Suivant / Terminer-secondary / Rejouer), replacing the hand-rolled serif buttons; selected tense chip gains `shadow-amber-sm` + ivory text; selected person row → crème+ (`surface-alt`); prompt card gains `shadow-card`. Focus-header tense tag `truncate`s so the long Spanish names never crowd the `n / 10` counter.
- **Shared écriture fix — `AnswerBlank` caret gated on focus.** The faux blink caret now renders only while the input actually holds focus (internal `onFocus`/`onBlur` state), so an idle, blurred blank no longer looks editable. Applies to /review écriture too (same correct behaviour).
- **Two interaction fixes (Omar-flagged on click-through), both shared with écriture.** (a) The blurred write-in blank was hard to re-tap — `AnswerBlank`'s transparent input now extends its hit area ~12px vertically / 8px horizontally past the thin underline, so a tap *near* the blank re-focuses it. (b) The result screen has no form to catch the key, so **Enter** now advances to the next prompt (window `keydown`, `!e.repeat`) — the keyboard echo of tapping "Suivant".
- **DECLINED — horizontal-scroll + edge-fade for the tense chips.** The setup *Temps* chips are a plain `flex-wrap` row, NOT a horizontally-scrolling row with the §05 edge-fade mask. Forward-compatible by wrapping: when the M5.3d compound tenses land they wrap to a new line — no scroll affordance, no fade, no fixed-count grid to rebuild. (The edge-fade pattern stays reserved for the genuinely-overflowing nav-pill row.)
- **DEFERRED — the Accueil « entraînement » card.** The Home entry card that launches the drill (orientation-only in the handoff) is NOT built here; it rides the Home cluster pass (roadmap → M5.5j+), where it composes against the review hero rather than in isolation.

### From M5.6 (v0.8.9) — Color theming (4 palettes)

A cross-cutting **theming foundation** (NOT a per-screen cluster): 4 user-selectable palettes — **Sépia (default, light)** · Ardoise (light, petrol) · Indigo (light, violet) · **Nuit (dark)** — driven entirely by CSS custom properties on a root `data-theme` attribute. Components were already token-disciplined, so this is a foundation + a handful of holdout fixes, not an app-wide refactor. Suite 446→451; tsc + lint clean; build green; device smoke-test confirmed live-switch + Nuit.

- **Phase 0 — the `@theme` split (the unlock).** `@theme inline` (create-next-app scaffold default) compiled the token VALUES *into* every utility (`.bg-accent{background-color:#c27a2c}`), so a `[data-theme]` override did nothing. Diagnosed that `inline` is **load-bearing only for the fonts** (`--font-sans: var(--font-inter)` chains to the next/font var on `<html>`), not the colors. Split into two blocks: fonts stay `@theme inline`; **colors/radius/shadow → non-inline `@theme`** so utilities emit `var(--color-*)`. Byte-identical Sépia (94 var-refs now resolve to the same `:root` hexes); the parity gate passed before any palette work.
- **Phase 1 — de-hardcode the holdouts.** `Button.tsx` §03 hexes → tokens (`amber-tint`/`terra-border`/`err-bg`/`terra-ink`); new **role token `--color-disabled-ink`** (was the bespoke `#C2A877`; `FillInBlank` too); `text-white`→`text-ivory` (`DrillCard`, `login`); Discovery swipe **washes + stamp bgs** → `color-mix` from `--color-accent`/`--color-ok`/`--color-card`; new **`--color-scrim`** (delete-sheet overlay). Reconciliation: 33 tokens, only baked literals left are the deferred shadow/gradient rgba; the 4 rating tokens (`again/hard/good/easy`) are **dead** (RatingButtons moved to amber-family tokens in M5.5e) — backlogged for removal.
- **Phase 2 — the `[data-theme]` blocks** (`globals.css`, `:root[data-theme="…"]`, specificity beats `:root`). Each sets the **13 mapped roles** (handover `PALETTES`; name map: `surface→card`, `border→line`, `sepia→muted`, `accentDeep→amber-deep`, `accentLight→amber-light`, `success→ok`, `danger→err`) + the **9 designer-specced legibility tokens** (`ok-bg`, `sage-ink/border`, `err-bg`, `terra-ink/border`, on-accent `ivory`, `disabled-ink`, `scrim`). The **6 accent-family tints** (`amber-mid/tint/pale`, `tint`, `tinted-border`, `hair`) are shared **`color-mix`** derivations so they track each theme's accent (Sépia keeps its literals = parity).
- **Frozen semantics + rating rungs.** success(sage)/danger(terra) keep their **role** in every theme (never re-themed to the accent) but take each palette's legibility values — Nuit **inverts** them to the bright dark-ground variants (`sage-ink #8ACCB6`, `terra-ink #EC8E6B`), never the light sage/terra. The **rating rungs DO track the accent** (they consume `amber-deep/accent/amber-mid/amber-pale`), per the handover's "accent replaces amber" — petrol on Ardoise, indigo on Indigo, amber on Nuit.
- **Phase 4 — Nuit.** On-accent flips to dark ink via `--color-ivory: #2A2218` (every `text-ivory`/knob inverts automatically); shadows darken via **`--tw-shadow-color`** on the Nuit root (the `.shadow-*` utilities read `--tw-shadow-color`, NOT `var(--shadow-card)`); scrim deepens.
- **Control + persistence (Phase 3).** New **`profiles.theme`** column (migration `20260621000000_theme_pref.sql`); `lib/theme.ts` (ids, `coerceTheme`, cookie name, `THEME_SWATCHES` picker-preview colors, tested). The ACTIVE **"Thème de couleur"** swatch picker in Préférences (`ThemePicker`) flips `<html data-theme>` instantly + mirrors a **theme cookie** + PATCHes `profiles`. **SSR is FOUC-free:** the root layout reads the cookie → `<html data-theme>` (`suppressHydrationWarning`); `SettingsProvider` reseeds the cookie from the profiles-canonical value on mount.
- **Accepted caveats (smoke-tested OK on the modern-mobile target):** the `color-mix` static fallback (legacy no-`color-mix` browsers get a slightly-off tint computed against the default scope); a one-frame Sépia→theme flash on first load on a *new device* (cookie absent until reseed); the derived tint percentages (78/15/50/17/38% accent, `hair` 64% line) are tunable.

### From M5.5i (v0.8.8) — Profil / Préférences / Compte cluster

The settings/account surface at `/account`, built from the `profil-screens.jsx` mockups onto the locked v2 board. **Not a re-skin of a shipped screen — a new surface.** Tokens all pre-existing; added only `--color-terra-border #DDA994` (a board T-object value already inlined in `Button`, mirrored into `globals.css`). Reusable form chrome lives in **`components/form/`** (Field/Btn/Stepper/Toggle/Segmented/rows) — the canonical auth foundation login/signup/onboarding will import. Suite 446; tsc + lint clean.

- **Three live controls on `profiles`** (migration `20260612000000_settings_prefs.sql`: `cards_per_session INT def 20`, `autoplay_audio BOOL def true`, `playback_speed TEXT def 'normal' CHECK lent|normal|rapide`; existing own-row RLS covers them). Each closes write→persist→read: **Cartes par session** (stepper 10–50, `clampCardsPerSession`) is read by BOTH `/review` cap consumers (the server `page.tsx` fetch + the in-session "Encore N" refetch in `ReviewSession.tsx`); **Lecture auto à la révélation** gates a NEW reveal-time autoplay in the shared `ResultReveal`; **Vitesse de lecture** maps Lent/Normal/Rapide → `playbackRate` via the tested `playbackRateFor`. Persisted fire-and-forget through `PATCH /api/profile` (partial upsert, mirrors `/api/drill/prefs`). Audio settings flow app-wide through a server-seeded `SettingsProvider` context (consumed by `AudioButton` + `ResultReveal`); `cards_per_session` stays server-read (prop-threaded to `ReviewSession`).
- **Playback speed over the baked-0.9 cache (no re-synthesis).** The cached MP3 is synthesized at GCP `speakingRate 0.9`, and the client plays it at the default `playbackRate 1.0` — so "today's default" already sounds 0.9×. The control is applied STRICTLY as `HTMLMediaElement.playbackRate` over that existing cache. Because the file is pre-slowed, `playbackRate = perceivedTarget ÷ 0.9`: **Lent 0.833 / Normal 1.0 / Rapide 1.111** (perceived 0.75 / 0.9 / 1.0; **Normal = today's default**, plays the file unchanged). `preservesPitch` left at its default (true) so the rate change doesn't pitch-shift. The Web-Speech fallback (no cached file) stays at its fixed 0.9 — out of this control's scope.
- **Stats strip — "révisions" sourced from `Σ review_cards.reps`, NOT `count(review_logs)`.** A deliberate decision: `review_logs` is empty (the insert bug below), while `reps` is deterministic (ts-fsrs increments it on every grade, via the card update that succeeds), monotonic (`forget()` is never called; `reset-schedule` only bumps `due`), already-populated, and independent of the broken logger. `totalReviews` is summed over every in-scope card on the SAME `getDictionaryState` fetch (one round-trip, collection scope `manual ∪ promoted` — consistent with `memorizedCount` and the delete-warning `totalWords`). "mots mémorisés" = `getDictionaryState.memorizedCount` (unchanged source). The delete-sheet copy uses the **TOTAL** word count, not memorized.
- **Compte:** email (display) · change-password flow (`/account/password`: reauth via `signInWithPassword` → `updateUser`; a wrong current password surfaces the field error, never silently changes it; validity from the tested `password-policy` so the button-gate and the on-screen policy copy can't drift) · type-to-confirm **"SUPPRIMER"** delete → the privileged `POST /api/account/delete` (`auth.admin.deleteUser`, full FK cascade) · logout. **Mentions légales kept LIVE** (`/legal/privacy` + `/legal/terms`, re-skinned as two NavRows — deliberately NOT demoted to the mockup's inert BIENTÔT, since the pages ship). BIENTÔT inert rows for the unbuilt features (Nouvelles cartes/jour, rétention, entraînement, mots difficiles, langue, variante, thèmes, rappel, export).
- **KNOWN ISSUE (separate commit) — the `review_logs` insert has been broken since inception.** `app/api/review/route.ts` inserts `word_id: row.word_id`, but `review_logs` has no `word_id` column and is keyed by `card_id` (uuid, NOT NULL) → every insert fails 42703 and is swallowed (`console.warn` + the route still returns `{ok:true}`). So `review_logs` is **empty for all users** while `review_cards` advances normally — which is exactly why révisions is sourced from `reps`. **Fix (planned, own commit):** payload `word_id` → `card_id: cardId`; elevate the warn to `console.error`. Detail in `backlog.md` → Known bugs.
- **NO-BACKFILL GAP (accepted, irreversible).** The ~532 historical reviews (confirmed via `Σ reps`) were never logged; their per-review `time_ms`/`rating`/`reviewed_at` are **unrecoverable**. Logging accumulates from the insert fix forward; the Home review-time estimate (median of recent `review_logs.time_ms`, which has silently run on the 12000 ms/card fallback the whole time) **self-heals** as new reviews land. Not data loss to chase — named so it isn't later mistaken for one.

## Lemma flow UX (M3.3)

When a user submits a Spanish word and Anthropic returns `lemma != input`, the /add page transitions to a `lemma_suggestion` phase before revealing the card:

- Heading: "Forme conjuguée"; subheading `"« <input> » est une forme de <lemma>"`; form annotation as centered subtitle.
- **`+ Ajouter «<lemma>»`** (primary burgundy CTA) — saves the lemma, skips the inflected form.
- **`Garder «<input>»`** (subtle text link) — proceeds to the inflected form's card, which has a FORME section above DÉFINITION.
- If the lemma is already in the deck, the primary CTA is disabled and an **`Ouvrir «<lemma>»`** button routes to `/words/[id]`.

Definitions for inflected forms = definitions of the lemma. Examples and distractors are form-matched. As of M4.3, both input and lemma receive their own TTS audio at enrich time.

## Branding (as of v0.3.3)

- App name: **Paco**; tagline: **APRENDE · RECUERDA · HABLA**
- Visual identity: illustrated Podenco Ibicenco named Paco. Appears throughout the UI (the nav brand lockup, empty states, review completion, discover page, /add helper text, loading screen). **(v0.5.4: the old large home-body hero was removed; the wordmark + tagline now live in the top-nav brand lockup.)**
- Favicon: real Paco illustration; Apple touch icon: rounded-square Paco; 404: sad Paco at `/paco-sad.png`
- Voice: informal "tu" across all touchpoints
- Celebration copy: French chrome + Spanish celebration phrases (¡Correcto!, ¡Casi!, ¡Buen trabajo!, ¡Inténtalo otra vez!)
- Vercel URL still spanish-vocab-lyart.vercel.app; GitHub repo still spanish-vocab; custom domain deferred

## Persistent design system (Chaleureuse + Paco palette)

- Hex palette: background cream `#F5EDDA`; card surface `#FFFBF3`; ink (dark brown text) `#3D2B1A`; accent (amber) `#C27A2C`; success (sage) `#4A7C6F`; danger (terracotta) `#B84C2A`
- OKLCH tokens in `app/globals.css`; radius 14px; warm brown RGBA shadow
- Lora serif for headlines and word names; Inter for UI (exception: the top-nav pill labels use Lora bold as deliberate brand chrome). **Fraunces (italic 700) as a tightly-scoped expression layer (v0.8.0)** — applied ONLY via the `Display` primitive on the literal allowlist ¡Listo! / ¡Casi! / big review counts (no `font-fraunces` utility exists)
- **Design-system foundation (v0.8.0, board §01–§05):** canonical `Button` (`app/(app)/Button.tsx`, 4 variants × 3 states, 14px-block/999-pill radius rule, primary always amber — no dark-brown primary), selection treatments (`selection.ts` — `SELECTION_ACTIVE`/`SELECTION_PERSISTENT`), formalized amber tokens (`--color-amber-deep`/`--color-amber-light`/`--color-ivory`, `--shadow-amber`/`--shadow-amber-sm`). Adopted at cross-cutting sites (nav active tab; the two brown→amber primary unifications); per-screen call-sites migrate with their clusters
- Rating buttons: amber single-hue gradation (darkest = Again, lightest = Easy)
- Status pills mapped to FSRS state via `lib/word-status.ts` (v0.5.0) with accent/sage/neutral palette
- Mobile-first 430px max-width centered layout; top sticky pill-tab nav (v0.5.4); bottom freed for in-task actions
- `<StickyActions>` primitive (v0.4.2, repositioned to `bottom-0` in v0.5.4) for surfaces with long-scrolling primary actions
- Shared row renderers (v0.5.2): `StatusPill` (canonical `getWordStatus` pill), `FamiliarityMeter` (3-dot stability strength), `WordRow`. Primary CTAs use bold text.
- iOS Safari zoom locked (16px input minimum, viewport scale locked); safe-area handled top (nav) + bottom (StickyActions)

## Current pain points

- ~~**Fill-in-blank verb conjugation mismatch.**~~ **RESOLVED in M5.3a (v0.6.1).** The grader compared the user's answer against the stored lemma ("estudiar") while example sentences required the conjugated form ("estudiamos"). Now: verb cards mask the contextually-correct conjugated form (paradigm-aware) and grade against it; the lemma and other valid inflections are accepted as ¡Casi!. (M5.0d `nspell` interim was dropped to avoid throwaway code, as planned.) — See *From M5.3a*.
- Definitions still feel inconsistent across words (Anthropic non-determinism). The pre-M2.5 weak data was fully resolved in M4.1.
- ~~Word list **pagination + free-text search** still deferred~~ **SHIPPED in M5.4a (v0.7.0)** — client-side forgiving search + load-on-scroll progressive append over the already-fetched list. See *From M5.4a*.
- ~~No way to delete a word once added.~~ **Delete SHIPPED in M5.4b (v0.7.1)** (swipe-to-reveal on `/words` + a Delete button on the detail page, both via the deferred-delete/undo primitive). **Archive** (soft-delete + `archived` column) is still deferred — see *From M5.4b*.
- No first-time onboarding for new users.
- **Nav-avatar asset pending (from M5.5a).** The 32px head-shoulders Animando crop (@1×/2×/3×) is not yet produced; the nav lockup keeps the current `paco.png` and a one-line swap-point comment marks where it goes. Asset-gated, non-blocking — logged in `backlog.md`.
- `.next/types/validator.ts` TypeScript error — likely benign build cache artifact; verify against a pre-M3.3 commit before fully trusting.
- Adjective annotations include POS prefix (`"Alguno — adj. indefinido, femenino plural"`) while verb annotations don't. Keeping the extra pedagogical context for now.
- Rating buttons in /review flow with content instead of using `<StickyActions>` — small follow-up. Bundle with the ④ near-miss/typo display whenever /review is next opened.
- `paco-tts` GCP service account has project-level Editor (workaround for the unfindable `roles/texttospeech.user`). Tighten via gcloud CLI — backlog, low urgency.
- **PhaseChecklist active-indicator animation not firing** (from M5.0b). `motion-safe:animate-pulse` + staggered "···" specced but not rendering. Non-blocking; static indicator still conveys state. Logged to backlog.
- **Duplicate-word entries** (e.g. "a menudo" ×2) are now more visible in the cleaner M5.0c list. Existing data-hygiene backlog item, not introduced by the redesign.
- ~~**Two review-format items scoped to v0.6.3**~~ **SHIPPED in v0.6.3** (clitic-aware masking + accent-homograph denylist — see *From v0.6.3*).
- ~~**Conjugator correctness is the M5.3c gate**~~ **RESOLVED in v0.6.4.** The systemic imperative-table-bypass (all 16 irregulars) + the stem-change set are fixed, and `canDisplayParadigm` is the display guard M5.3c wires in. Remaining M5.3c conjugator work is *wiring*, not correctness. Four single-cell follow-ups (poder/creer gerund-and-preterite, haber/llover) are excluded from the trusted set (graceful no-table) and logged in `backlog.md` → Conjugator follow-ups. See *From v0.6.4*.
