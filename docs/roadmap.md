# Roadmap

The next committed milestones. Items in `docs/backlog.md` are deferred until promoted here.

> Re-sequenced after the M5 design pass (7 design boards reviewed) + the pill-tab nav deferral.
> The original monolithic **M5.0** was split into **M5.0a–d** (M5.0d later **dropped** — its
> interim conjugation fix folded into M5.3 to avoid throwaway code). The original **M5.2 (game
> modes)** was renumbered to **M5.3**. Pill-tab nav was inserted as **M5.2** (deferred to after
> M5.1 by explicit call).
>
> Re-sequenced at v0.7.1 — List management complete (M5.4a/b shipped; M5.4c bulk-select **dropped**); **cost cap** added as the soft-launch gate; the **design pass** restructured to END in signup/login/onboarding (the standalone signup + M6 PRE-BETA rows folded in — design into the pass, build after); the **content gate** expanded to verb coverage + discovery depth + A2/B1 essentials.

## Working slate — re-sequenced at v0.7.1

The committed order from here (detail for each lives in its section below or in `backlog.md`):

- **NOW:** M5.3c verb drill ✅ (shipped) · **List management — COMPLETE:** M5.4a free-text search + load-on-scroll ✅ (v0.7.0), M5.4b per-word delete + undo + Relearn ✅ (v0.7.1); **M5.4c bulk / multi-select / bulk delete — DROPPED** (decided not to build); **archive** (soft-delete + `archived` column) stays deferred (`backlog.md`).
- **PRE-BETA:** **Cost cap** (soft-launch gate before any external user touches the app — budget / rate guards on the two paid paths: the Anthropic enrich call + the Google Cloud TTS call; synergy with the content gate below — pre-seeded discovery content reduces live generation calls, lowering the spend the cap guards) · **Design pass** (umbrella: holistic UX/design audit + more Paco mood variations & placements from Claude Design + the parked Astuce/tip redesign + **FINAL STEP — design-led rework of signup / login / onboarding**, Claude Design first) · **Language balance / immersion toggle** (FR/ES chrome; **English DEFERRED** — the co-students are Francophone; the layer the onboarding build rides) · **Onboarding + signup build** (follows the designed screens — onboarding build rides the FR/ES language layer (M6 is built on it); backend hardening = **re-enable Supabase email verification** + empty states) · **Content gate — A2/B1 coverage seeding**, three components: **(a) verb conjugation coverage** — expand `TRUSTED_LEMMAS` via the vetted full-paradigm fixture process (`conocer`/`crecer`/`andar` candidates); **(b) discovery depth** — ≥100 pre-seeded words per theme (not only on-demand generation); **(c) A2/B1 essentials** — a curated set of high-frequency NON-theme words (todavía/ya, más, menos, mejor, peor, comparatives, time/quantity adverbs); **open scoping Q:** (b)+(c) imply a shared/global seed pool vs today's per-user on-demand `pending` discovery rows — a data-model decision to resolve at scoping; pre-seeding also addresses the M5.1 cold-generation latency and lowers API/TTS cost · **M5.3d compound (perfect) tenses** for the drill + grid (*he comido* / *había comido* — **IMPORTANT, A2** core; trails the content gate — `haber` verification rides the same fixture process) · **Learning-curve review + blank reviews** (analysis-first) — tail.
- **STRATEGIC:** **Crossword** (scoping → build; shares the (iii) generation + cache layer with the drill) · **Native apps** Android + iOS (**approach decision FIRST — Capacitor / PWA / rewrite** — then build).
- **LATER:** **English UI** · **M7 companion** (data-gated on ~100+ words).

## M1 — Deploy to Vercel ✅ (v0.1.0)
## M2 — /add experience overhaul ✅ (v0.2.4)
## M2.5 — Spanish-first definitions ✅ (v0.2.5)
## M3.1 — Two-step add flow + duplicate handling ✅ (v0.3.1)
## M3.2 — Wordlist-driven spelling correction ✅ (v0.3.2)
## M3.3 — Lemma normalization ✅ (v0.3.3, three phases)
## M4 — Word audio + detail view ✅ (v0.4.0)
## M4.1 — Definition backfill + styled 404 + empty-DÉFINITION guard ✅ (v0.4.1)
## M4.2 — /add polish ✅ (v0.4.2)
## M4.3 — Audio quality (Google Cloud TTS) ✅ (v0.4.3)

*(Detail for shipped milestones lives in `PROJECT_STATE.md`.)*

---

## M5.0a — Honest status taxonomy ✅ (v0.5.0)
- Canonical `lib/word-status.ts` primitive — single source of truth for word status
- `getWordStatus(card|null)` — 6-state stability-based partition, first-match, uses ts-fsrs `State` enum
- `isMemorized(card|null)` — mastery predicate; excludes `Relearning` regardless of stored stability
- **Two-derivation model**: the pill is action-priority (a due-now card reads "À réviser" even at high stability); the mastery class is stability-based and ignores transient due-ness. M5.0c counts/filters consume `isMemorized`.
- Constants `MEMORIZED_STABILITY_DAYS = 30`, `LEARNING_STABILITY_DAYS = 7`
- `/words/[id]` pill rewired; inline `statusPill` removed; 21 vitest unit tests (incl. the decoupling invariant + `State.Learning` fall-through lock)
- Home list rows out of scope **by design** — rewired in M5.0c

## M5.0b — Add-flow loading polish ✅ (v0.5.1)
- ② Timed phase checklist (Définition / Exemples / Famille / Phonétique) replacing the single-line loader. Last phase stays an indeterminate pulse until real data lands. Label is "Exemples" (not "Trois exemples").
- ③ "¡Listo!" screen **variant A** — informative card preview, hold-plus-tap (no auto-advance), reads as the resolution of ②'s checklist.
- Existing-word routing keyed on `data.status` (early return before any `ready`); 400ms loading-shell delay gate so fast deck-only lookups never paint the loader.
- Extension-hydration one-liner: `suppressHydrationWarning` on `<html>` + `<body>` (Grammarly + Scribe).

## M5.0c — Home + word-list editorial ✅ (v0.5.2)
- ① Home top section **variant A** — editorial weight, "≈ N min de révision" effort estimate (median of recent `review_logs.time_ms`). **No streak. No phrase-du-jour hero.** Review CTA loudest; "Tout est à jour" State B.
- ⑥ Word-list at `/words` — rows on `getWordStatus` via shared `StatusPill`; Tous / À revoir / **Mémorisés** filter pills; familiarity dot meter (`getFamiliarity`); sorts alphabétique / date / familiarité.
- ⑤ End-of-session **variant A** — per-word ✓/✗ recap, honest stats (Révisés / Réussite / Temps), **no streak**, "Encore N mots" only if cards remain.
- Resolves the transient M5.0a→M5.0c list/detail status inconsistency.
- Post-tag polish folded into v0.5.2: list-row redesign (left vertical familiarity meter, bordered/tinted-due rows, italic definition preview, "N révision(s)" reps line), collection-line spec, CTA-bold convention.

## M5.0d — Interim inflection-tolerant matching — **DROPPED**
- Was: a cheap `nspell` stem-heuristic stopgap for the fill-in-blank conjugation mismatch. **Dropped** — it would be thrown away the moment M5.3's deterministic conjugator lands, so the whole fix lives in M5.3 instead. (Daily workaround until then: type the dictionary form.)

## M5.1 — Discovery mode ✅ (v0.5.3)
- Topic grid, lightweight generation + enrich-on-keep, reusable swipe deck, bilan; reuse `words` + discovery flag (**Model A**).
- Built the **reusable `SwipeCard` primitive** (also unblocks backlog swipe-to-delete/archive). Schema: `words` discovery columns + `UNIQUE(review_cards.word_id)` backstop. (Detail in `PROJECT_STATE.md` → From M5.1.)

### M5.1 follow-ons (designed-for in M5.1, not built)
- **M5.1b — Adjacency mode** ("plus de mots comme les tiens"): same swipe primitive + generation, seeded from a sample of the user's library instead of a topic; new entry tile on Découvrir.
- **Onboarding slice:** empty-state Home → the same discovery flow (pairs with the backlog first-run item).

## M5.2 — Top pill-tab nav ✅ (v0.5.4)
- Top sticky nav replaces the bottom NavBar entirely. Lives inside the `max-w-[430px]` column so nav + content share edges by construction (resolves the M5.1 wide-viewport/edge-alignment pain point).
- Row 1: home circle (house) far-left + centered Paco brand lockup (mascot + wordmark + APRENDE·RECUERDA·HABLA, → /) + account avatar far-right. Row 2: horizontally-scrollable pills — Accueil, Mes mots, Ajouter, Réviser, Découvrir.
- Brand de-duplicated: old large home-body hero removed; tagline now lives in the nav lockup.
- Mes mots is now a first-class destination (supersedes M5.0c's "sole entry = TA COLLECTION block"; the block stays as redundant entry). Accueil is both a pill and the corner circle so a tab is always selected.
- StickyActions `bottom-16` → `bottom-0`; z simplified; every `pb-*` consumer revised; iOS top+bottom safe-area.
- `/dictionary` + unlock gate split out → M5.2b.

## M5.2b — Dictionnaire personnel ✅ (v0.5.5)
- `/dictionary` — A–Z reference of the user's **memorized words only** (strict subset of `/words`: `isMemorized`); deliberately bare (word + gloss + audio; no pills/meter/filters/sorts), iOS-contacts jump rail, Ñ-after-N bucketing.
- 10-word **sticky** unlock (`profiles.dictionary_unlocked`, never resets); flip via Server Action (prefetch-safe) → one-time confetti interstitial. Locked pill / Home card / `/dictionary` all show the locked state until then.
- Resolved the open questions: `/dictionary` is the **A–Z of the user's own (memorized) deck**, and it **IS a 6th nav pill** (locked = dashed + lock glyph) — superseding "not one of the pills". (Detail in `PROJECT_STATE.md` → From M5.2b.)

## Review rework ✅ (v0.5.6 + v0.6.0)
- **v0.5.6 — slices 1–2:** review focus-mode (`FocusModeContext`; TopNav suppressed through the bilan); write-in mechanic (`AnswerBlank` — transparent input over an inline blank); three-verdict result card (¡Eso es! / ¡Casi! / ¡Uy!) with letter-level diff (`lib/worddiff.ts`); shared `ResultReveal` (MCQ reuses the Paco reveal); uniform rating-pill restyle; `--color-warm` token.
- **v0.6.0 — slice 3 close:** bilan recap redesign (fixed vertical frame, scrolling word list, stats-above-list override, `--color-hair` token, staggered row reveal); **"Encore N" continues the session in place** (client-fetches the next due batch — the old `<Link href="/review">` served a stale Router-Cache page); QCM result-state aligned to the écriture card (canonical tint tokens, verdict-on-top, shared `RatingButtons` selection-fill beat).
- Grading was extracted to `classifyBlankAnswer` in `lib/rating.ts` (shared by `computeRating` + the result card) — the seam M5.3a's verb grading extends. (Detail in `PROJECT_STATE.md` → review-rework sections.)

## M5.3 — Game modes + conjugation substrate
*Split into M5.3a/b/c. The conjugator is built once (M5.3a) and feeds four consumers across the three slices: the inflection-fix (a), the ④ Astuce hint + frame-5 table (b), and the verb drill (c).*

### M5.3a — Conjugation substrate + fill-in-blank matching fix + in-question hint ✅ (this milestone)
- **Deterministic hand-rolled conjugator** (`lib/conjugator.ts`, pure/unit-tested, client-safe). Library-vs-handrolled was demonstrated at plan time: `spanish-verbs` (RosaeNLG) **failed the battery** (no stem changes — `pedir→"pedo"`; no imperative/gerund/participle), `verbos` is CC-BY-NC. Hand-rolled passes 67 battery + regression tests. A2/B1 tense union; **not LLM-sourced** (M3.2 lesson, higher stakes).
- **Paradigm-aware masking** (`maskVerbSentence`) blanks the contextually-correct conjugated form — fixes the lemma-vs-conjugation mismatch AND the dio/fue MC-gap (irregular stems the 4-char heuristic missed). `maskSentence` kept as non-verb + no-match fallback.
- **5-branch verb grading** (`classifyVerbBlank`, POS-gated): target form → ¡Eso es!; lemma → ¡Casi!; other valid inflection → ¡Casi!; typo near-miss → ¡Casi!; else ¡Uy!. Reuses the existing ¡Casi! verdict + FSRS rating. Non-verb grading untouched.
- **In-question hint**: blank → tap → `(lemma, person)`, tense withheld, lemma-only on ambiguous person. Coordinates **derived on the fly** (no stored data).
- **Storage of {lemma,tense,person} DEFERRED to M5.3b** — derive is deterministic + self-healing and grading never reads stored coords; nothing to persist yet.
- **Safety property:** M5.3a never DISPLAYS a generated conjugation (the answer shown is the real blanked token; the hint shows facts). So an untabled-irregular verb degrades verdict generosity, never miseducates.

### v0.6.2 — post-M5.3a review-format coherence ✅
Three independent fixes after M5.3a, each diagnosed against the real deck before coding.
- **Token-aware spellcheck gate** (`lib/wordlist.ts` `checkSpelling`) — multi-word adds (`"te acuestas"`, `"a menudo"`) no longer 422 before the Anthropic call; per-token validity, one-miss reconstructs the full corrected phrase. Single-word unchanged.
- **Verb cloze-MCQ stored-in-form gate** (`lib/review-cloze.ts` `chooseQcmCue`, pure + unit-tested) — a verb gets the cloze only when `normalize(target.surface) === normalize(word)`; infinitive-stored verbs use the lemma-level definition-MCQ. Supersedes the intermediate "force cloze for all maskable verbs" over-correction (which made an infinitive-stored blank/option mismatch on 16/45 verbs). Decision lifted out of the inline initializer into a tested helper because the inline version regressed.
- **Écriture word-header removed** (`FillInBlank.tsx`) — the answering screen no longer prints the headword above the sentence.
- (Detail in `PROJECT_STATE.md` → From v0.6.2.)

### v0.6.3 — écriture-polish ✅
Five commits (each diagnosed against the real deck first). Suite 161 → 181.
- **#2 accent-homograph denylist** (`lib/mask.ts`) — the masker never selects a function-word homograph (`de/se/esta/…`) as the verb form, so `dar` masks `"dio"` not the preposition `"de"`. Root cause was deeper than accent-folding: the conjugator also emits **unaccented usted-imperatives** (`de`/`esta`) that sit in the exact map. Guards écriture too.
- **#1 clitic-aware masking** (`maskProcliticReflexive`) — proclitic-reflexive stored words (`"te levantas"`) mask the full clitic+verb unit, `target.surface == word` → coherent cloze-MCQ with full-reflexive options. Reuses the #2-hardened masker (the clitic is denylisted there, so it's never chosen as the verb; #1 extends the matched verb's blank leftward — the #1 × #2 composition). lemma passed raw. Backfill (`scripts/backfill-reflexive-lemmas.ts`, applied): `te levantas/te sientas/te duermes`.
- **#3 reps rotation** (`pickClozeExample`) — cycles the example among the maskable ones across reviews (was first-maskable always); format stays per-card stable (cue from a reps-independent pick).
- **#4 desktop accent-row** (`AccentBar.tsx`) — `á é í ó ú ñ ü` inserts at the caret in the écriture field; desktop-only (`(pointer:fine)` via `useSyncExternalStore`), hidden on touch.
- **#5 prefer the coherent example** (`pickClozeExample`) — among maskable examples, prefer those whose masked form == the stored word, so a mixed example set (`te duermes` vs a `me dormiría` sentence) masks its own form. Deck delta: **6 verb cards definition→cloze, 0 reverse** (monotonic).
- (Detail in `PROJECT_STATE.md` → From v0.6.3.)
- ~~**Known limit:** `te sientas` stays definition-MCQ~~ **RESOLVED in v0.6.4** (sentar e→ie stem added → auto-upgraded to cloze-MCQ).

### v0.6.4 — Conjugator hardening ✅ (standalone slice ahead of M5.3b)
Audit-first (live deck 51 verbs + the tabled set), then the fix. Suite 181 → 298.
- **Finding 1 (systemic):** `imperativeAffirmative` + the negative-imperative branch bypassed `IRREGULAR_FORMS` (built on rule-computed `present()`/`subjunctive()`), so **all 16** fully-irregular verbs had wrong imperatives (`ser`→`"sa"`, `dar`→`"de"`, `estar`→`"este"`…). Rerouted both through `formsFor` (table-aware); `IRREG_IMP_TU` affirmative-tú preserved; `ir` affirmative-nosotros special-cased to `"vamos"` (negative stays `"no vayamos"`).
- **Display-guard primitive** `canDisplayParadigm(lemma)` — conservative allowlist (`TRUSTED_LEMMAS`); true only for full-paradigm reference-verified verbs, false for untabled/uncertain (the guessed-regular-for-irregular trap) — the primitive M5.3b/c consume. **No UI consumer in this slice.**
- **Stem-change expansion:** sentar + despertar/encender/mentir (e→ie), acostar/mostrar/recordar/costar/soñar/almorzar/mover/doler (o→ue), vestir/seguir/conseguir (e→i, incl. `-guir` gu→g ortho).
- **Self-verifying harness:** golden fixture (`lib/conjugator.expected.ts`, reference-authored) + clean-room `regularReference()` for deck regulars; data-driven test asserts every cell == `conjugate()` and `TRUSTED_LEMMAS` is exactly the verified union. Excluded + logged: haber, poder, creer, llover. (Detail in `PROJECT_STATE.md` → From v0.6.4.)

### v0.6.5 — two review/add hygiene bugs ✅ (from v0.6.4 smoke-testing)
Small patch, single commit. Suite 298 → 316.
- **Result-screen example threading** — the écriture result "Exemple" re-read `card.examples[0]` while the exercise masked the reps-rotated / prefer-coherent `picked.example`; they diverged. Lifted into a tested pure helper `resultHintExample(picked, card)` (not inlined — the `chooseQcmCue` lesson). `MultipleChoice` checked: no divergence.
- **Reflexive-clitic correction** — `"ti acuestas"` passed the per-token spellcheck (grammatical, not lexical, malformation) and was stored as the headword. `correctProcliticReflexive` (`lib/reflexive.ts`, deterministic, conjugator-free) corrects the proclitic against the person→clitic map; detection (valid set `{me,te,se,nos,os}`) split from correction (person parse), degrading to the lemma — never the typo — on annotation drift. Wired in `enrich/route.ts` (response `word` + TTS regen). (Detail in `PROJECT_STATE.md` → From v0.6.5.)

### M5.3b — Astuce de Paco + conjugation table
**Conjugation-table half ✅ (v0.6.6)** · Astuce-de-Paco /review hint **PARKED PENDING DESIGN** (the ④ post-answer morphology tip — including the richer ¡Casi! "racine devient pud-" explanation — waits on the tip redesign in the pre-beta design pass; it will mount the same `ConjugationGrid`).
- **✅ frame-5 lemma-interstitial conjugation table** (the `estudié`→`estudiar` screen) — the 2-col/3-row paradigm of the typed form's tense, typed cell in amber, added to the existing `/add` lemma interstitial (additive, not a rebuild). Body blends mockup **B** (calm reference framing — no "Bonne nouvelle" celebration banner, per the ceremony-fatigue standing decision) with mockup **A**'s grid + review-status line. Kept the current top pill nav (mockups showed the pre-v0.5.4 bottom nav). **Frame 6 rejected** (reintroduces M3.3 duplicate-fiche confusion).
- **✅ HARD REQUIREMENT met (carried from M5.3a):** the grid renders ONLY when `canDisplayParadigm(lemma)` is true (v0.6.4 display guard) AND the typed form's tense resolves to a finite six-person tense that contains it; any untabled/uncertain verb → no grid (today's screen unchanged). Never shows a guessed paradigm. Live hit rate **42/48 verb-pos deck words (87.5%)** — the 6 misses are all known/logged exclusions (`andar`/`conocer`/`crecer` untabled, `creer`/`haber`/`acordar` excluded-and-logged), not guard misfires.
- **✅ tested pure helper** `lib/conjugation-grid.ts` (tense resolution from `form_annotation` + six-cell selection + multi-highlight for syncretic tenses + tense-label map) — never inline in the component (the `chooseQcmCue`/`resultHintExample` lesson). Reusable `app/(app)/ConjugationGrid.tsx` (the later Astuce slice mounts the same component; NOT wired into /review here). The review-status line ("Tu as revu N fois · prochaine révision dans X j") is pure `review_cards` data threaded via optional `lemma_reps`/`lemma_due` on enrich, shown only when the lemma is already in the deck.
- **Decisions:** imperative-labeled + non-finite (gerundio/participio) inflected adds → no grid (no clean six-person table); syncretic tenses (imperfecto/condicional/subjPresente, yo == él/ella) highlight BOTH cells (honest — the form is genuinely ambiguous); a typed surface that matches no cell of the resolved tense → no grid (coherence, same principle as the cloze-only-when-masked-token==stored gate). The in-deck "déjà dans ta collection" tint box was replaced by the quiet review-status line (design-driven).
- **Astuce-de-Paco /review hint** (the ④ post-answer morphology tip) — **PARKED PENDING DESIGN** (folded into the pre-beta tip redesign); mounts the same `ConjugationGrid` component. May add the persisted `examples[].target = {lemma,tense,person}` field at enrich if the Astuce actually needs it (deferred from M5.3a).
- **Pre-beta gate flagged:** comprehensive A2/B1 `TRUSTED_LEMMAS` seeding is now a **REQUIRED pre-beta-handover gate** (`backlog.md`) — coverage = how often the table/drill can appear; `conocer`/`crecer`/`andar` are the surfaced near-term candidates.

### M5.3c — Verb-conjugation drill ✅ (v0.6.7)
- **Shipped:** Paco's first game mode — a self-contained practice flow at `/drill` (Home `DrillCard` → Setup → 10 prompts, prompt→result manual-advance → Recap), gated on ≥5 trusted deck verbs. The `(ii)` trigger-frame format. **Pure practice — no FSRS** (never writes review_logs / touches review_cards). All logic in the pure tested `lib/drill.ts` (pool/unlock gate · prompt builder, prefer-unique-then-repeat-to-10 · the single swappable `triggerFrame` seam · strict grading via the existing `classifyVerbBlank`, 4 qualities → 3 verdicts · deterministic teaching line via accent-exact `paradigm` matching). Reuses `AnswerBlank` + desktop-only `AccentBar` + `ResultReveal` + the v0.6.6 `ConjugationGrid` (new `buildConjugationGridForTense` builder) for the hint sheet. Drill prefs (tenses + person scope) persisted to `profiles`. Reflexives + untrusted verbs excluded from the pool; the conjugator is untouched. **Follow-up polish (pre-tag):** grammar terms shown in accented Spanish everywhere (chips/headers/cue/recap — `Presente·Pretérito·Imperfecto·Futuro·Condicional·Subjuntivo`); cue shows `verb·person·tense`; conjugation table on all three result screens; conditional frame "En ese caso,"; the "unselected tense" report diagnosed as a display defect (raw key on the result header), filter proven correct + lock-tested. (Detail in `PROJECT_STATE.md` → From v0.6.7.)
- **Deferrals (logged below + in `backlog.md`):** drill "new verbs" (outside the deck) ⇐ A2/B1 coverage seeding; (iii) full generated sentences = the shared generation/cache layer (crossword shares it; only `triggerFrame` swaps); richer ¡Casi! morphology = the parked Astuce tip redesign. No game-mode selector (Home-card entry only — a selector waits for a 2nd mode).
- **Original framing (for reference):** verb-conjugation drill (natural first, given the substrate); multiple sessions / session infra.
- **Format-philosophy note (canonical).** M5.3c — review formats: cue must match the answer's level. Inflected-form cards → cloze; lemma/infinitive cards → definition for MCQ, or cloze for écriture (type the conjugation). Interim gate (shipped): verb cloze-MCQ only when masked-token == stored-word, else definition-MCQ. Cloze-MCQ for verbs uses conjugator-generated wrong-FORM distractors (conozco/conoces/conoció/conocer) so options match the blank — lets infinitive-stored verbs do a coherent form-test MCQ. Verb format leans écriture (production); cloze-MCQ is the gentler recognition rung for new/low-stability cards. Non-verbs keep definition-MCQ but stay eligible for cloze-écriture.
- **GATE — conjugator hardening: ✅ DONE in v0.6.4** (was ⛔ blocking). The two prerequisites are met: (1) the **display guard exists** — `canDisplayParadigm(lemma)` returns false for any untabled/uncertain verb; M5.3c wires it into the drill + distractor generation (refuse to prompt/distract on a verb the guard rejects). (2) The **tabled irregulars are fixed** — the systemic imperative-table-bypass (all 16 irregulars) + the stem-change set (sentar/seguir/… ). **Remaining M5.3c conjugator work is wiring, not correctness:** consume `canDisplayParadigm` at the drill/distractor seam, and source wrong-FORM distractors only from trusted paradigms. Expand `TRUSTED_LEMMAS` coverage (with a vetted fixture) as needed for drill breadth — coverage = how often the drill can offer a given verb.

### M5.3d — Compound (perfect) tenses for the drill + grid (committed; not built)
- **Compound (perfect) tenses for the drill + grid:** pretérito perfecto compuesto (*he comido*) + pluscuamperfecto (*había comido*). **IMPORTANT — A2, core for the beta cohort.**
- **Scope:** compose `conjugate(haber, <matching simple tense>, person)` + the participle (`conjugate(verb, 'participio')`). **PREREQUISITE:** verify `haber`'s auxiliary forms — `haber` is excluded from the v0.6.4 golden fixture (`canDisplayParadigm('haber')` is false), so its paradigm must be reference-verified + admitted before it can drive displayed/drilled compound forms.
- **Drill + grid ONLY** — these introduce **two-word cells** (grid) and **multi-word grading** (drill answer = `he comido`). Leave the `/review` masker untouched (single-token blanks). New `DrillTense`-adjacent keys would need the two-word answer/grid handling; keep them out of `buildDrillPrompts`' single-token finite set until that lands.
- Sequenced **after the PRE-BETA content gate** (A2/B1 coverage seeding) — `haber` verification rides the same vetted full-paradigm fixture process that gate uses. Not built now.

## M5.4 — List management

### M5.4a — /words list scale: free-text search + load-on-scroll ✅ (v0.7.0)
- **Forgiving free-text search** over the FULL already-fetched deck — a search input above the filter pills (`text-base` 16px, the iOS zoom-lock rule). Matches the Spanish word AND both glosses (`definition.es` + `definition.fr`); `normalizeSearch` = lowercase + NFD diacritic-strip so "bebi"→"bebí", "nino"→"niño" (the plain strip folds ñ→n — wanted here, the opposite of `lib/dictionary.ts`'s Ñ-preserving bucketing guard). Pure tested predicate `lib/word-search.ts` (not inlined — the `chooseQcmCue` lesson). Composition: **filter pill → search → sort → render** (search narrows the active pill, doesn't reset it). Empty results → "Aucun résultat".
- **Load-on-scroll (progressive append, NOT virtualization).** Initial 40 rows, +30 when a bottom sentinel hits the viewport via IntersectionObserver (not scroll events). `list.slice(0, shown)`; no new fetch, no react-window dep. Cap resets to the initial chunk on any visible-set change (filter / search / sort); default date-desc → initial chunk = newest N.
- **All client-side over the already-fetched list** — NO schema, NO new API route, NO server pagination. `page.tsx` threads `defFr` onto `WordListItem` for the predicate (never rendered — Spanish-first). (Detail in `PROJECT_STATE.md` → From M5.4a.)
- **Out of scope (later M5.4 slices):** swipe delete/archive, Relearn/reset-a-word, bulk/multi-select, `archived` schema column, server pagination.

### M5.4b — /words per-word actions: delete + undo + Relearn ✅ (v0.7.1)
- **Single-word `DELETE /api/words/[id]`** — RLS + explicit `user_id` scope; FK cascade verified against the live schema (`review_cards.word_id → words`, `review_logs.card_id → review_cards` both CASCADE), so no manual child cleanup / no migration. Error `console.error`d (M3.3 convention).
- **Deferred-delete + undo-toast primitive** (`app/(app)/DeferredDelete.tsx`) — a layout-level provider (sibling of `FocusModeProvider`) so the toast/timer survive a detail→list navigation. **Set-shaped** (`scheduleDelete({ ids, labels })`, 1 here / N in M5.4c) but NO batch route or bulk UI built. Optimistic-remove via a provider-owned `hiddenIds` Set (`/words` renders `items` minus it → undo + deterministic re-sort fall out for free); ~5s timer; commit fires the DELETE(s) + `router.refresh()`; unmount clears the timer WITHOUT committing (word survives). Folded in the backlog `select-none` toast item.
- **Swipe-to-reveal delete** (`SwipeRow.tsx`) — reveal-and-tap (NOT the discovery `SwipeCard` fling-commit; a fresh component, pure snap math in tested `lib/swipe-row.ts`), wraps the unchanged `WordRow` (new `asListItem` prop avoids nested `<li>`). Single-open-row, closes on scroll / outside tap; composes with 4a (optimistic removal doesn't reset the scroll cap).
- **Detail-page Delete + Relearn** — Delete → primitive → back to `/words` (toast carries over); Relearn → `reset-schedule` (= reschedule-only → **no confirm modal**, consequence copy; New-card graceful no-op).
- **Out of scope (was → M5.4c, now DROPPED):** bulk / multi-select / select-all + a batch delete endpoint — decided not to build; **archive/soft-delete schema** stays deferred (`backlog.md`); swipe on the detail page (it uses buttons). The deferred-delete primitive stays set-shaped, so a future bulk need can reuse it.

## M5.5 — Design pass (PRE-BETA umbrella)

> Implements the **v2 system board** (`Paco — Système visuel v2`, canonical for all hexes/rules). Sequenced as **foundation first** (the cross-cutting substrate), then per-screen **cluster** slices that consume it, ending in the signup/login/onboarding rework (M6 build follows). The board's later "vibe A/B" mockups show an earlier nav with a "+" — ignore; §05 is canonical (nav IA restructure is its own separate later pass).

### M5.5a — Design-system foundation ✅ (v0.8.0)
- **Substrate ONLY — NOT a screen redesign.** New-family minor bump.
- **Color tokens (additive, §01):** `--color-amber-deep #9A5A1C`, `--color-amber-light #F2DCB8`, `--color-ivory #FFFBF3`, `--shadow-amber`/`--shadow-amber-sm`; `--color-surface-alt` value-aligned to canonical crème+ `#EFE2C6`. Confirmed `--color-line`=bordure / `--color-muted`=sépia already board-exact. Out-of-scope tokens (tint, ok-bg, err-bg, rating ramp) untouched.
- **Fraunces (§02):** loaded italic-700 as an `<html>` var only (NOT in `@theme` → no `font-fraunces` utility); the sole apply path is `Display.tsx` with a closed `kind` union ('listo'|'casi'|'count') = the structurally-enforced allowlist. No usages ship (land with clusters).
- **Canonical `Button` (§03):** 4 variants × 3 states, 14px-block / 999-pill radius rule, primary always amber. **Now:** the two brown-`bg-ink` primaries unified to amber (`DrillResult` "Suivant →", `LoadingIdiom` "Voir la fiche" — retires the v0.5.1 ink-CTA pilot). **Deferred to clusters:** all other already-compliant CTAs. Rating buttons untouched.
- **Selection (§04):** `selection.ts` (`SELECTION_ACTIVE` / `SELECTION_PERSISTENT`); the one global instance wired = nav active tab. Filter pills / swipe choices / word rows defer to clusters.
- **Nav paint (§05):** visual coherence only, IA/behavior unchanged; active pill routed through `SELECTION_ACTIVE` (gains `shadow-amber-sm`, ivory text). One-line avatar swap-point comment for the asset-gated 32px Animando crop. (Detail in `PROJECT_STATE.md` → From M5.5a.)
- **Out of scope (later cluster slices):** ¡Casi! neutral screen (review/drill); StatusPill §06 recolor + En cours/En apprentissage merge + 3-dot gauge (Words — **the 6-band `lib/word-status.ts` taxonomy does NOT change**); per-screen button/selection migration; nav IA restructure; per-screen Paco placements.
- **Deferred-and-logged from M5.5a** (`backlog.md` → Design pass — deferred from M5.5a): (a) the inactive nav-pill **accent-tinted border** (M5.2) vs board §05's neutral border → revisit in the nav-IA pass; (b) **semantic-tint drift** (`--color-ok-bg` `#D4EDE9` vs board `#DCEAE5`, plus err-bg/tint) → reconcile with the §06 status/Words cluster, not the additive foundation; (c) the 32px Animando nav-avatar crop asset.

### M5.5b — Words cluster (design-pass, screen-by-screen) ✅ (v0.8.1)
- **Presentation-only re-skin over the locked taxonomy** — the 6-band `lib/word-status.ts`, `getWordStatus`/`isMemorized`/`getFamiliarity` and all FSRS/SRS logic are UNCHANGED; this slice projects them onto the board §06 look + restructures the detail page. Patch within the M5.5 arc.
- **§06 StatusPill (first build):** a render-layer label-map collapse over the unchanged 6-band output — `À réviser`/`À rappeler`(Relearning **folded in**, per decision) → the ONE loud amber pill; `En cours`+`En apprentissage` → merged neutral "EN COURS"; Nouveau neutral; Mémorisé sage (`bg-ok-bg text-sage-ink border-sage-border`). **No amber/terracotta status text anywhere** (folding Relearning in removes the last terra status text). Action-priority already in `getWordStatus` (mastered-but-due reads À réviser).
- **4-dot mastery gauge** (`MasteryGauge`, replaces the 3-dot `FamiliarityMeter`): pure `getMasteryGauge(card)` = `getFamiliarity` rendered as quarters `{0,1,2,4}` (band map: New→0, <7→1, 7–30→2, ≥30→4). The **3rd dot is never filled by design** (board never draws 3/4 — a word steps 2→4 at Mémorisé); a vitest case locks {0,1,2,4} + the "biblioteca 1 / me acuerdo 2 share EN COURS" divergence. Neutral ink fill — no sage gauge dot.
- **List (`/words`):** filter pills → `SELECTION_ACTIVE` (amber fill); sort → discreet text toggle (active = ink/bold + 2px amber underline); crème+ tint only on À réviser rows; search field focus → amber border + `shadow-amber-sm`. Three mascot empty states: first-word **Animando** + primary CTA / no-results **Pensando** (intentional divergence from the board mock, which drew Durmiendo) + secondary "Ajouter « query »" / passive filter-empty **Durmiendo** (copy adapts to filter).
- **Delete/undo re-skin:** `SwipeRow` is now ONE continuous clipped rounded rectangle — borderless row (`flush` prop on `WordRow`) + flush terracotta Supprimer panel in one track, no corner mismatch. Undo toast restyled dark (`bg-ink`, ivory copy, `amber-mid` "Annuler", 3px amber countdown bar, reduced-motion safe).
- **Detail (`/words/[id]`):** Relearn + Supprimer moved into a top-right **⋮ overflow menu** (Relearn normal / Supprimer destructive); bottom button pair removed; Relearn still reset-schedule (no modal — the pill flip is the confirmation); Delete still flows through the deferred-delete/undo primitive. Back arrow → labeled **"‹ Mes mots"** → `/words`. Status pill + 4-dot gauge carried at the top. FR/Traduction reveals confirmed two-way + re-skinned to the board TextLink ("Voir en français" ↔ "Masquer le français"). **No phonetics line** — verified there is no IPA field (enrichment never generated one; the mock's `/me aˈkweɾ.ðo/` was placeholder). "Mots similaires" label kept (the `distractors` field is MCQ confusables, not true family — board's "même famille" would mislabel).
- **Token reconciliation** (resolved backlog item, deferred from M5.5a): `--color-ok-bg → #DCEAE5`, `--color-err-bg → #F4E1D8`, `--color-tint → #F6E7CC`; added `--color-sage-ink`, `--color-sage-border`, `--color-tinted-border`, `--color-terra-ink`, `--color-amber-tint`, `--color-amber-mid`, `--shadow-menu` (each labelled board-T-object vs new in `globals.css`).
- **§04 reconciliation:** `SELECTION_PERSISTENT` softened from "1.5px amber border" to the board's `border-tinted-border` (canonical; amber on a due row is carried by the 7px leading dot, not the border) so the primitive matches what ships.
- **Shared-renderer ripple (intended):** `StatusPill` / `MasteryGauge` / `WordRow` are shared with the Home preview — the §06 recolor + 4-dot gauge show there too. Home's full composition is a later cluster; this slice only keeps it from breaking. (Detail in `PROJECT_STATE.md` → From M5.5b.)
- **Pre-tag refinements, final per `BRIEF-words-adjustments.md` (same branch):** (1) gauge re-bucketed so all four dots fill smoothly — `getMasteryGauge` is a direct fn of stability (New→0/<7→1/<15→2/<30→3/≥30→4, new `GAUGE_MID_STABILITY_DAYS=15`); (2) gauge stays **horizontal in the right column, centered under the pill** (`items-center`), the 7px leading À réviser dot **kept**, and **"N révision(s)" removed from rows — kept on the detail only**; (3) **POS + FORME unified across /words detail AND the /add card** — POS inline+abbreviated+gendered right after the headword via a shared `posAbbrev` (the spelled-out "VERBE"/"NOM" eyebrow removed; gender rides in `n.m.`/`n.f.`; `posEyebrow` reverts to discovery-deck-only), FORME conditional on both (only for inflected forms); (4) **bidirectional sort** — re-click flips direction + an amber caret (Date newest / Familiarité least-mastered / Alpha A→Z defaults). Suite 383→402. *(An interim pass tried a vertical-left gauge + restored reps + spelled-out eyebrow; the board review reverted all three.)*

### M5.5c — Add-flow cluster ✅ (v0.8.2)
- The whole `/add` journey re-skinned onto the v2 board per the six canonical add-flow boards. **Re-skin of shipped behavior + the loading choreography, now fully WIRED to `MOTION-loading.md`** (canonical): per-phase ring-pop + check stroke-draw (keyed remount so the one-shots re-fire per step), synced center dot, rising thinking dots, off-tempo breathing, mount-once idiom entrance; deterministic ~850ms dwell for steps 1–3 + data-gated step 4 + ~3.05s min-visible floor (tunable constants); honest reduced-motion fallback — fixes the backlog "PhaseChecklist active-indicator animation not firing". **Root-cause fix:** the ¡Listo! "Voir la fiche" CTA clipped (mobile + desktop) because the `ready` view bottom-anchored it in a hard-coded `100svh−4rem` column shorter than the real two-row nav; now pinned via the shared `StickyActions` (the fiche's working pattern).
- ① entry + autocomplete (presentation-only — accent-tolerant **matching** stays backlog) + did-you-mean re-skin; rotating curated input placeholder (drops "mariposa"). ② loading checklist + breathing Paco + the wired active-phase animation. ③ ¡Listo! → Fraunces via the **first real `Display` consumer**. ⑤ "Forme conjuguée" interstitial re-skin (deterministic grid, never LLM; two-path logic unchanged). ③a fiche re-skin via canonical `Button`.
- **Two screens built whose behaviors already shipped** (confirmed with Omar): **④ compact "déjà dans ton deck" duplicate** — replaces the full-fiche-for-duplicate; **reset-schedule moves to the detail ⋮** (capability preserved, `/api/words/reset-schedule` route kept). **⑥ save success — single** (awaited → confirmed "« mot » est dans ton deck") **+ multi** (similaires bulk-add stays fire-and-forget background; screen framed honestly as in-progress "N mots en route … jusqu'à 20 s, continue à naviguer" — don't claim an incomplete success).
- `WordRow` extended to **optional-link** (reuse, not a fork) for ⑥-multi's not-yet-created words. Additive plumbing (no schema): `save` returns `id`; `enrich` deck-check returns card `state`/`stability` for ④'s row. No taxonomy/FSRS/conjugator change; suite 402; tests + lint + build green. **Tag after the device animation smoke-test.**

### M5.5d — Discovery cluster ✅ (v0.8.3)
- `/discover` re-skinned onto the v2 board (Discovery boards). **Re-skin of shipped M5.1 behavior** (topic generation + fling-commit swipe) + two real fixes. No FSRS/generation/decide change.
- ① theme grid re-skin + §04 selected tint (crème+ + amber border + amber-fill icon tile + `→`; never amber-fill the card). **"Pour toi" non-functional placeholder shell:** two featured cards (adjacency + A2–B1) with a neutral "Bientôt" chip → tap shows a warm Paco "pas encore disponible" (no terracotta). The features stay deferred — see the M5.1b + content-gate rows below; this is just the wired shell.
- ② generation **reuses the /add loading choreography** (brief: don't build a different loader) via the new shared `app/(app)/LoadingChecklist.tsx` (extracted from `LoadingIdiom`; `IdiomCard` moved to shared `app/(app)/`); theme phase labels + longer dwell (950/3800); reveal gated on max(dataReady, floor) → deck/exhausted. ③④ deck re-skin (ghost stack, amber progress) with the unified inline **`posAbbrev`** (spelled-out eyebrow dropped; speaker omitted per the brief's card enumeration — audio lives on `/words/[id]`).
- ⑤ **swipe-color role fix** (real correction): "Je connais" = sage (acquisition), "À apprendre" = amber (action) — M5.1 had them inverted (terracotta on Je connais). Directions + gesture unchanged. `SwipeCard` gained board stamp placement + optional wash + `lift` (`--shadow-lift`). ⑥ end states → Feliz bilan (honest tally, no streak) + Durmiendo exhausted. Suite 402; green. **Tag after the device smoke-test.**
- **Deferred features the "Pour toi" shell will activate** (logged, NOT built this slice): **adjacency = M5.1b** ("plus de mots comme les tiens" — same swipe primitive + generation, seeded from the user's library; see the M5.1 follow-ons row); **A2–B1 essentials = the PRE-BETA content-gate seeding** (the curated reference collection; open Q from the board — 120 words may need a sub-pick by palier vs one deck).

### M5.5e — Review cluster ✅ (v0.8.4)
- `/review` (FSRS QCM + écriture) re-skinned onto the v2 board (six Review boards). **Re-skin of shipped behavior + three fixes + one signed-off new behavior.** Drill is a separate mode, out of scope (shares only `AnswerBlank`/`AccentBar`, unchanged). **`computeRating` + FSRS scheduling untouched.**
- **Fix #1 (lemma-reveal):** écriture context chip conditional by card type — verb keeps `infinitif · tense · person` (**Spanish**, per the M5.3c standing decision; deterministic + degrade-safe via the new tested `lib/review-cue.ts`, never LLM); non-verb gets `posAbbrev · FR gloss`, **never the lemma**. **Fix #2 (¡Casi! neutral):** near-miss + verb-form verdicts → chromatically neutral ink (color rule §6) — **verdict layer only, ratings untouched** (Omar's call). **Fix #3 (rating):** single-hue amber gradation, **no dots** (the 4-dot atom stays word-level mastery); new `--color-amber-pale` token.
- **New behavior — auto-advance countdown:** after a verdict, a neutral **JS-driven** faint bar fills 0→100% over the tunable `AUTO_ADVANCE_MS` (10 s); on completion the suggested rating applies + advances. **One tap on the effective (suggested/selected) pill advances**; a different pill selects + cancels; ↵ advances; **pause = stop** (no resume); no captions. Honest reduced-motion fallback (discrete numeric). The auto-applied rating feeds FSRS via the unchanged suggestion mapping.
- **① Entry card relocated to HOME; `/review` auto-starts** (the standalone landing was removed — `ReviewEntry` deleted). Home's review card is the re-skinned crème+ hero (`Display kind="count"`, ≈min top-right, no "QCM & écriture mêlés"); the Réviser pill starts a session directly; `/review` with 0 due → Durmiendo passive. "≈N min" extracted to the shared `lib/review-estimate.ts` (Home consumes it). **Display union extended** (`+esoEs/uy/buenTrabajo`) to route ALL verdict faces through the allowlist gate. ⑥ session-complete: "Sus du 1er coup" replaces "Réussite %", non-punitive word list.
- **Device-test fixes (folded into v0.8.4):** a **card-10 crash** — the new écriture chip/¡Uy! reveal rendered a malformed nested `{es,fr}` `definition.fr` raw — fixed at the data boundary (`lib/bilingual.ts` `flatBilingual` in a shared `review/mapCard.ts` `mapReviewRow`, used by the server page + client refetch). Suite 402→416; green. **Tag after the device re-test.**

### M5.5f — Écriture context/hint model + QCM instruction ✅ (v0.8.5)
- The deferred Review follow-up. The écriture card always shows the FR sentence translation (which gives the meaning), so M5.5e's always-shown context chips were a second giveaway. **Removed always-shown context for both card types**; all help moved into a **penalized 3-tier Indice** ("Indice · n/3"): non-verb → first-letter (in the blank) / ES monolingual definition / scramble; verb → form `v.·tense·person` / blanked `ConjugationGrid` / scramble. Degrade-safe (verb form→first-letter; verb table→ES definition via `verbGridCoords`). Scramble = the new pure tested `lib/scramble.ts` (deterministic, seeded, ≠ original).
- **Rating cap by hints used replaces the `hintUsed` penalty:** `computeRating` takes `hintLevel`; `HINT_CAP=[4,3,2,1]` caps the suggestion (0→Facile…3→À revoir), only lowering. Écriture-only (MCQ's FR-reveal still doesn't affect its rating); the old `−1` penalty deleted (no double-count); a caption shows the cap. **`ConjugationGrid` reused three ways** (Indice 2 blanked + the two verb verdicts highlighted) via opt-in `blankTarget`/`infinitive` props (/add + drill unchanged). **QCM instruction eyebrow** "Choisissez la bonne réponse". **Countdown untouched** (v0.8.4 final); FSRS math untouched (only the suggested-rating cap). Suite 416→424; green. **Tag after the device smoke-test.**

### M5.5g — Dictionnaire cluster ✅ (v0.8.6)
- The Dictionnaire surface re-skinned onto the v2 board (Dictionnaire boards). **Re-skin of shipped M5.2b behavior** (collation, jump rail, sticky unlock flag, audio, `/words/[id]` routing — all preserved) + **two behavioural changes**.
- **① Unlock reveal relocated to review-end** (was Home/app-load): normal bilan first → tapping **either** bilan exit (Encore / Accueil — both intercepted, since our bilan has two exits vs the mock's single "Continuer") intercalates the shared **`UnlockTakeover`** once → "Plus tard" resumes the tapped exit, "Ouvrir mon dictionnaire" → `/dictionary`. Home/app-load `<UnlockSync/>` stays a **safety-net fallback** (fires only if the user bailed pre-bilan).
- **Flip-on-show once-guard:** `evaluateDictionaryUnlock()` made **read-only**; the flag is flipped by **`markDictionaryUnlocked()`** on the celebration's client mount (review takeover OR safety-net interstitial), so the flag is an honest "ceremony shown" marker — quitting before tapping can't lose the celebration, the safety-net stays valid. Both stay client-mount Server Actions (not RSC render — the M5.2b lesson). No new column.
- **② Milestone number = live memorized count** at the crossing (≥10), same source as the dictionary/home count, never hardcoded 10.
- Presentation: shared `UnlockTakeover` (review-end + re-skinned interstitial); `Display` union **+`'milestone'`**; `Confetti` re-skinned to the sanctioned warm drift + `lx-*` entrance keyframes (reduced-motion-gated); populated index re-skin + jump-rail **magnify bubble** + **`AudioButton variant="circle"`** (reuse, no fork); **row gloss stays Spanish (`definition.es`)** as a recorded decision (index of already-memorized words; FR at the fiche); locked screen / Home card / locked nav pill re-skin (the 5 always-on pills + inactive-border = deferred nav-IA, untouched); dictionary icon = lucide `BookA` everywhere (nav pill + Home card; the handover raster book was tried on both then dropped — it read darker/softer and inconsistent beside the vector pill icons; no `dict-book-*.png` ships; a single tintable book glyph stays an Accueil-pass call). Suite 424→428; green. **Tag after the device smoke-test.**

### M5.5h — Conjugaison drill repaint ✅ (v0.8.7)
- **Visual repaint of the shipped /drill flow** (M5.3c) onto the v2 board — no grading/scheduling/sourcing/unlock/prefs/query change. Setup → prompt → result (3 verdicts) → bilan all re-inherit the now-current réviser/écriture + `ConjugationGrid` + bilan patterns: hand-rolled CTAs → canonical `Button` (Inter, amber, `shadow-amber`); selected tense chip gains `shadow-amber-sm` + ivory; selected person row → crème+ (`surface-alt`, was `tint`); prompt card gains `shadow-card`.
- **`¡Casi!` neutralized (board §6):** the near-miss verdict word + the "ta réponse" word are **ink** (no semantic hue), the focus-header bar stays **amber**; `¡Eso es!`→sage, `¡Uy!`→terra; the old `warm` hue is gone from the drill. Wrong answers no longer strike-through (surface the right form over punishing the wrong). Teaching-line callout → calm crème+ box (dropped the left-accent rule + mini-Paco).
- **`Voir la conjugaison` now blanks the asked cell** (`ConjugationGrid blankTarget`, the réviser Indice-2 pattern-completion) — the pre-answer sheet helps you *find* the form, never hands it over; the result-screen grid stays revealed.
- **AnswerBlank caret gated on focus** (shared écriture fix): the faux caret only blinks while the input holds focus, so an idle blank no longer reads as editable.
- **One new helper `tenseLabel(key)`** — pure, tested, the SINGLE tense-name source for every drill surface (setup chips, both focus headers, the cue parenthetical, the result teaching line, the bilan missed-list); returns the FULL Spanish names (`Pretérito indefinido`, `Subjuntivo presente`, …). Per decision, the teaching line keeps its French frame but takes its tense names from `tenseLabel` (Spanish); `TENSE_FR` deleted. The short `drillTenseLabel` is retained ONLY for `lib/review-cue.ts` (a different surface; untouched). Header tag ellipsizes the long names so they never crowd the counter.
- **Deliberate decision (Omar's, routed through `tenseLabel` — not a unilateral change):** drill tense names stay **Spanish**. Reasoning: the on-screen `ConjugationGrid` headers are inherently Spanish (`Pretérito indefinido · passé simple`), so French chips would split one screen's grammar vocabulary; Spanish reinforces the learning frame; consistent with the M5.5g dictionary Spanish-gloss call. Omar also chose the FULL names and `Pretérito perfecto simple` → **`Pretérito indefinido`**, applied app-wide incl. the shared `ConjugationGrid.labelEs` (so /add + /review match; `resolveGridTense` still accepts either as parser input).
- **Two interaction fixes (Omar-flagged on click-through, shared with écriture):** the blurred `AnswerBlank` gained an enlarged tap area past the thin underline (easy re-focus); the result screen advances on **Enter** (window `keydown`, no form to catch it).
- **DECLINED — chip horizontal-scroll + edge-fade.** The *Temps* chips are a plain `flex-wrap` row (not a scrolling row with the §05 edge-fade mask) — forward-compatible by wrapping when the M5.3d compound tenses land; no fixed-count grid to rebuild. Edge-fade stays reserved for the overflowing nav-pill row.
- **DEFERRED — the Accueil « entraînement » card.** Orientation-only in the handoff; it's built with the **Home cluster pass** (M5.5j+), composed against the review hero, not here.
- Suite green (428); tsc + lint clean. **Smoke-tested clean on device → tagged v0.8.7 on the close commit.**

### M5.5i — Profil / Préférences / Compte cluster ✅ (v0.8.8)
- **New surface, not a re-skin of a shipped screen** — the settings/account surface at `/account`, built from the `profil-screens.jsx` mockups onto the locked v2 board (tokens all pre-existing; added only `--color-terra-border #DDA994`, a board T-object value already inlined in `Button`). Reached via the existing `TopNav` account avatar (now amber-active on `/account`).
- **Three live controls, each closing write→persist→read** (persisted on `profiles`, migration `20260612000000_settings_prefs.sql`): **Cartes par session** (stepper 10–50/def 20 → `cards_per_session`, read by BOTH `/review` consumers — the server fetch + the in-session "Encore N" refetch); **Lecture auto à la révélation** (toggle → `autoplay_audio`, gates a NEW reveal-time autoplay in `ResultReveal`); **Vitesse de lecture** (Lent/Normal/Rapide → `playback_speed`, mapped by the tested `playbackRateFor` to `playbackRate` 0.833/1.0/1.111 over the baked-0.9 MP3 — Normal = today's default, `preservesPitch` left true). Audio settings flow through a server-seeded `SettingsProvider` context.
- **Stats strip:** "mots mémorisés" (sage, via `getDictionaryState.memorizedCount`) · "révisions" — **sourced from `Σ review_cards.reps`** (extended onto the same `getDictionaryState` fetch, collection-scoped), **NOT `count(review_logs)`**, because the logger is broken (see below); `reps` is deterministic, monotonic, already-populated, and log-independent.
- **Compte:** email (display) · **Changer le mot de passe** (`/account/password`, reauth via `signInWithPassword` → `updateUser`, wrong-current surfaces the field error) · **Supprimer mon compte** (type-to-confirm "SUPPRIMER" → privileged `auth.admin.deleteUser` cascade; warning copy uses the **TOTAL** word count, not memorized) · **Se déconnecter**. Mentions légales kept **live** (`/legal/privacy` + `/legal/terms`, re-skinned — deliberately NOT demoted to BIENTÔT). Reusable form chrome lives in `components/form/` (the canonical auth foundation login/signup will import).
- **BIENTÔT inert rows** (faint label + dashed ghost pill): Nouvelles cartes/jour, Objectif de rétention, Révisions d'entraînement, Mots difficiles, Langue, Variante d'espagnol, Thèmes Discovery, Rappel quotidien, Exporter mes données. Validation + speed mapping + session-cap bounds in tested pure helpers (`password-policy`, `playback-speed`, `session-cap`). Suite 446; tsc + lint clean.
- **Surfaced (separate commits):** the `review_logs` insert bug (`word_id`→`card_id`, table empty since inception — `backlog.md` → Known bugs) and `getDictionaryState`'s swallowed words-fetch error (`backlog.md` → Error handling). **Out of scope, parked:** new-cards-per-day budgeting (`backlog.md` → Scheduling).

### M5.6 — Color theming (4 palettes) ✅ (v0.8.9)
- **Cross-cutting foundation, NOT a per-screen cluster** (so it does not take the M5.5j slot). 4 user-selectable palettes — **Sépia (default, light)** · Ardoise (light, petrol) · Indigo (light, violet) · **Nuit (dark)** — on a root `data-theme` attribute, pure CSS custom properties.
- **Phase 0 — `@theme` split (the unlock):** `@theme inline` (scaffold default) baked token hexes into utilities so `[data-theme]` couldn't override them. Split — **fonts stay `inline`** (var-chain to next/font's `--font-inter`/`--font-lora` on `<html>`), **colors/radius/shadow → non-inline `@theme`** → utilities emit `var(--color-*)`. Sépia byte-identical (parity-gated before any palette work).
- **Phase 1 — holdouts de-hardcoded:** Button §03 hexes → tokens; new role tokens **`--color-disabled-ink`** (was `#C2A877`) + **`--color-scrim`**; `text-white`→`text-ivory`; Discovery washes/stamps → `color-mix`. The 4 dead rating tokens (`again/hard/good/easy`, orphaned since M5.5e) backlogged for removal.
- **Phase 2 — `[data-theme]` blocks:** 13 mapped roles + 9 **designer-specced legibility tokens** (`ok-bg`, `sage-ink/border`, `err-bg`, `terra-ink/border`, on-accent, `disabled-ink`, `scrim`) + 6 accent-family `color-mix` tints (track each theme's accent; Sépia keeps literals). **Frozen semantics:** success/danger keep their role, never re-themed to accent; rating rungs **do** track the accent (amber-family tokens).
- **Phase 4 — Nuit:** success/danger **invert** to bright dark-ground values (`#8ACCB6`/`#EC8E6B`); on-accent flips via `--color-ivory #2A2218`; shadows darken via `--tw-shadow-color`; deeper scrim.
- **Phase 3 — control + persistence:** ACTIVE "Thème de couleur" swatch picker in Préférences; `profiles.theme` (migration `20260621000000_theme_pref.sql`); `lib/theme.ts` (tested); live re-theme via `<html data-theme>` + **SSR cookie-mirror** (FOUC-free, profiles canonical). Suite 451; smoke-tested live-switch + Nuit. **Accepted caveats** (`backlog.md`): `color-mix` legacy fallback, new-device one-frame flash, tunable tint %.

### M5.5j — Home cluster ✅ (v0.8.10)
- **Presentation-only re-composition of `/` onto the v2 board** — FSRS/due, the `lib/drill` unlock gate, the dictionary unlock, the `lib/word-status` taxonomy and `getMasteryGauge` are all UNCHANGED. Patch within the M5.5 arc.
- `ReviewHero` (4 states, one family): due (crème+ `Display kind="count"` + amber CTA, auto-starts /review) · caught-up (Durmiendo, no CTA) · before-first-review (Animando, no CTA — covers first-launch 0-words + young). **Headlines in Lora, NOT Fraunces** — Omar reversed the prompt's Fraunces-routing instruction to keep the allowlist tight; the planned new Display kind was **dropped, not added** (only the count keeps `kind="count"`).
- NEW "Continuer avec Paco" 2×2 hub (`HubCard`/`HubCardLocked`): Ajouter (feature card — crème+ fill + 3px amber rail + amber icon tile, carte≠contrôle holds) · Découvrir · Conjugaison (`Rows3`, gated on the existing ≥5-trusted-verb unlock; locked = muted card + quiet **neutral** `bg-faint` gate meter) · Dictionnaire (`BookA`, gated on the existing dict unlock; `N / 10 mémorisés` meter). **Retires the M5.3c `DrillCard`** (deleted; the /drill deep-link soft-lock fallback re-points to `HubCardLocked`) and **absorbs the deferred M5.5h « entraînement » card**.
- `CollectionSection` (established / young / empty), reusing the M5.5b `WordRow` → `StatusPill` + `MasteryGauge` unchanged (the mockup "MotCard" == WordRow's anatomy). established = "Tout voir → /words" + last-10 preview + "Voir les N mots →"; young = "Tes premiers mots" + Découvrir pill; empty = dashed card + Ajouter/Découvrir CTAs.
- The hero/collection branching is extracted to the tested pure `lib/home-state.ts` (`resolveHomeState` — the two axes resolve **independently**). **Zero new reads** (`hasReviewedBefore = Σ review_cards.reps > 0`, already on the `getDictionaryState` fetch). New token `--shadow-card-sm` (board-T-object shCardSm). **TopNav untouched** (the mockup's 3-pill nav is illustration only — nav-IA is a separate later pass); `getDictionaryState`'s swallowed words-fetch error stays a SEPARATE commit. Suite 451→454; tsc + lint clean. Smoke-tested 4 hero/collection states + locked↔unlocked hub + all 4 palettes → tagged v0.8.10 on the close commit.

### M5.7 — Press / interaction-feedback states ✅ (v0.8.11)
- **Cross-cutting foundation, NOT a per-screen cluster** (guidance-driven, `GUIDANCE-press-states.md`). Previously only the review exercises reacted to a tap — nothing else signalled it was actionable. Additive only: no layout / rest-colour / copy change, just press/focus/reduced-motion feedback.
- **ONE reusable CSS primitive** — six `press-*` classes in `globals.css` (`press-card` / `press-card-feature` / `press-row` / `press-pill` / `press-pill-amber` / `press-icon`), all from the EXISTING palette (no new hue): a darkening **veil** (`::after` at `var(--color-ink)` 6%/10% — flips light on Nuit, so it reads as a contrast highlight on every theme), a **depress** (scale .985 cards / .94 small), a **shadow drop**; `press-pill-amber` darkens the amber fill to `amber-deep`. Themed keyboard **focus ring** (`color-mix` accent @35%) on `a/button/[role=button]`; `prefers-reduced-motion` keeps the veil, drops the motion.
- **Pure CSS `:active`/`:focus-visible`, deliberately not a JS `Pressable`** — keeps the Server Components (HubCard/WordRow/CollectionSection/dictionary index) server-side and handles pointercancel/scroll-cancel natively.
- **Wired across** Button (depress on top of the §03 press states), WordRow (non-swipe rows only — the SwipeRow drag owns the gesture), TopNav (icons + all pills incl. locked dict), Home HubCard/CollectionSection, dictionary rows, discover cards + topic cards (replaced ad-hoc `active:bg-tint`) + close, account ThemePicker/form Controls + LinkRow, word-detail ⋮ + items, AudioButton. **Locked cards + review exercises deliberately get none.**
- **Two gaps logged to `backlog.md`** (Mobile UX polish): the ≥44px hit-target expansion (a layout pass, out of a feedback-only change) and the sort-toggle / toast-X left focus-ring-only. Suite 454; tsc + lint clean. Smoke-tested all surfaces + all 4 palettes + reduce-motion → tagged v0.8.11.
- **Follow-up ✅ (v0.8.12):** the v0.8.11 values were too subtle to perceive on a real screen — strengthened (veil 6%→10%/14%, card scale .985→.97, small .94→.92) + `cursor: pointer` on every `press-*`; and since iOS Safari paints `:active` on tap only with a touch listener present (`cursor` fixed desktop, not iPhone), added the no-UI `TouchActiveFix` (empty passive document `touchstart` listener) in the root layout. Desktop + iPhone confirmed → tagged v0.8.12.

### M5.5k — Accueil v2 + global header swap ✅ (v0.9.1)
- **Presentation-only re-composition of `/` onto the `design_handoff_accueil_v2` board + the deferred nav-IA header swap** — FSRS/due, the drill/dict unlock gates, `lib/word-status`/`getMasteryGauge`, and `resolveHomeState` are all UNCHANGED (like M5.5j). The handoff's thesis was *decluttering* the Home header. Recreated from the Babel-in-browser references (`HeaderFinal`/`ReviewThinP`/`CaughtUpThinP`/`HubScrollP`/`FnCardP`/`CollectionFullP`), all hexes mapped to the `[data-theme]` tokens (all 4 palettes).
- **`TopNav` → "Header allégé" (GLOBAL, all routes — the nav-IA pass, done in the one shared component):** the old two-row home-circle + centered lockup + pill nav becomes **Row 1** = Paco lockup (circle-cropped `paco.png`, → `/`) on the left + profile avatar (→ `/account`) on the right (home-circle retired — the lockup is the `/` link), **Row 2** = the horizontally-scrollable menu. **Decision (Omar, after seeing both): the menu keeps the PILL format + today's items** (Accueil · Mes mots · Ajouter · Réviser · Découvrir + flag-gated Dictionnaire) — a text-tab variant was built and reverted; Conjugaison stays a Home rail card, NOT a nav item. Avatar keeps its tinted amber circle (`bg-tint text-accent`). All prior behaviors preserved (focus-mode hide, `aria-current`, active `scrollIntoView`, `press-*`, `SELECTION_ACTIVE` active pill).
- **Home-only greeting** `¡Hola, {prénom}!` (Lora 26, between the lockup row and the menu) — name via the shared `displayNameFromEmail` placeholder (`lib/display-name.ts`), so it becomes a real first name for free when M6 onboarding stores one. (The handoff had DROPPED the greeting; Omar re-added it.) Passed from the layout (`user.email`), gated to `path === '/'`.
- **`ReviewHero` → thin-card family** (same `HeroState` union + `Display kind="count"` **italic** — the handoff's upright count was NOT adopted, Omar's call): `due` = thinner card + a minutes **chip** (top-right pill) + 46px count + CTA; `caughtUp` = thin Durmiendo row; `firstReview` (handoff-unspecified) **preserved**, restyled to match.
- **Home hub 2×2 grid → horizontal rail** (`page.tsx`): full-bleed `-mx-5` scroller, equal-height `w-[182px] h-[132px]` cards + right-edge fade; `HubCard`/`HubCardLocked` gained a `className` passthrough for rail sizing (anatomy + gating props unchanged). `CollectionSection` reused **unchanged** (its established/young/empty states already match the handoff's amber-rail + "Tout voir" + `WordRow` anatomy).
- No lib logic touched → suite 458 unchanged; tsc + lint + build clean. **Smoke-test all 4 palettes (Sépia · Ardoise · Indigo · Nuit) + Home State A/B/firstReview + the nav on every route on device; tag v0.9.1 on the final post-polish commit.**

### M5.5k+ — remaining per-screen cluster reworks (not yet scoped)
- The Astuce + conjugation-table review interstitial (M5.3b, deferred — its own milestone). Consumes the M5.5a substrate. (The signup/login design slot shipped as **M6.0a**; the Accueil/nav-IA slot shipped as **M5.5k** above. The onboarding flow is the remaining M6 build.)

## M6 - App onboarding & login screen upgrade

> PRE-BETA sequencing: the login/onboarding **design** is the design pass's FINAL step (Claude Design first); this section is the **build** that follows it, riding the FR/ES language layer. Backend hardening (re-enable Supabase email verification + empty states) follows the designed screens. (No longer a standalone PRE-BETA slate row — folded into the design pass + build-after.)

### M6.0a — First-run auth slice ✅ (v0.9.0)
- **First shippable slice of the "Premier lancement" cluster** — the three screens a new user meets before any shipped surface: **Login · Inscription · `/review` vide**. First-run chrome over the locked v2 system (same `[data-theme]` tokens/type/roles), recreated from the `design_handoff_onboarding` Babel-in-browser references (source of truth for values/copy/states, NOT ported). **Explicitly NOT the onboarding flow** (welcome/tour/prénom/immersion/thème/niveau/starter/first-swipe/home-handoff) — that rides the FR/ES layer + content gate as a later slice.
- **Login (`app/login/page.tsx`, rebuilt off the bare pre-system form):** 84px Paco (Animando) bust, "Te revoilà !" (Lora), reusing `components/form/Field` + `RevealLink` and the canonical `Button`. Any `signInWithPassword` failure collapses to one non-committal terracotta field message ("E-mail ou mot de passe incorrect.", never reveals which field). Quiet links → `/signup` and the `/forgot-password` stub. Submit gated on `isValidEmail`.
- **Inscription (`app/signup/page.tsx`, new):** mirrors Login + a confirm field; helper copy rendered verbatim from `PASSWORD_POLICY_TEXT`; primary disabled until `isValidEmail && isValidPassword && confirm === password` (all via `lib/password-policy.ts` — no re-implemented rule). `signUp` → `router.push('/')` (email verification stays disabled → live session). **FORWARD SEAM (noted, no action this slice):** when the onboarding flow lands, first-run signup must route into onboarding, not straight to Home.
- **`/forgot-password` (new, minimal stub):** the reset flow itself is deferred (backend task); the route exists so the login link routes somewhere rather than churn in/out. Themed page + back-to-login link.
- **`/review` vide (`app/(app)/review/page.tsx`, enhanced):** Paco (Durmiendo), "Révision" eyebrow, "Rien à réviser pour l'instant" (Lora), calm subcopy, **Ajouter (primary) + Découvrir (secondary)**, and a **« prochaine révision » pill bound to `lib/review-estimate.ts`**. TopNav is inherited from the `(app)` layout (not rebuilt — the handoff `ReviewNavChrome` was mock-only).
- **`lib/review-estimate.ts` extended:** new pure+tested `nextReviewLabel(nextDueMs, nowMs)` — relative-day granularity only, **no time-of-day** (FSRS due-times inherit the review hour, so "matin/soir" would be an artifact; the mock's "demain matin" is illustrative). Day delta counted **local midnight-to-midnight** (not a rolling 24h window). `null` (MIN(due) null → nothing scheduled) → the pill is omitted. `estimateMinutes`/`median` untouched. The page adds one RLS-scoped `MIN(due) WHERE due > now` read only on the empty path.
- **`middleware.ts`:** the auth guard broadened from `/login`-only to a public-auth allowlist (`/login | /signup | /forgot-password`) — without it a logged-out visitor would be bounced back to `/login` and never reach signup.
- **Out of scope (unchanged):** re-enabling Supabase email verification, the password-reset backend, the onboarding flow, ThemePicker/`components/form/` internals.
- Suite 454→458 (4 new `nextReviewLabel` cases incl. the near-midnight boundary); tsc + lint clean. **Smoke-test all three screens across Sépia · Ardoise · Indigo · Nuit on device; tag v0.9.0 on the final post-polish commit.**

### M6.1a — FR/ES immersion layer (substrate + Review) ✅ (v0.9.2)
- **The reusable FR/ES immersion substrate** the onboarding immersion selector + modes ride on, **applied to Review only** (the richest surface — chrome-swap + all three gloss states + mode-c suppression prove the whole mechanism). Three per-user modes: **`fr_es`** (default = today's exact behavior), **`immersion`** (ES chrome, FR via tap-to-reveal), **`totale`** (ES chrome, no FR at all). NOT i18n — a two-language, mode-keyed, per-surface toggle (English deferred; onboarding scaffolding stays French → **no global `<html lang>` lock**).
- **Substrate:** `profiles.immersion_mode` migration (`20260715000000`, apply-on-deploy, mirrors `theme_pref`; default `fr_es` ⇒ zero change for existing users) · **`lib/immersion.ts`** pure+tested (`coerceImmersionMode`, `resolveChrome({fr,es?}, mode)` with **FR fallback when ES unauthored**, `glossVisibility(mode) → visible|tap|hidden`, `REVIEW_CHROME` authored pairs) · `SettingsProvider` gains `immersionMode`/`setImmersionMode` (seeded from the `(app)` layout profile fetch; **NO cookie / NO `<html>` attr** — immersion drives React not CSS, and is consumed only under `(app)` which already server-seeds → FOUC-free; deliberate deviation from the theme cookie-mirror) · `api/profile` Zod enum · **`app/(app)/TapReveal.tsx`** reusable tap-to-reveal (Review now, Discover later) · **`ImmersionModePicker`** in `/account` Préférences (replaces the inert "Langue de l'interface" SoonRow; French copy from the mock `MODE_IMM`) — makes the substrate testable now, before onboarding; the onboarding selector reuses `setImmersionMode`.
- **Review application — FULL chrome (client components read `useSettings().immersionMode`):** **all** Review chrome is mode-aware via `REVIEW_CHROME` + a shared four-rating lexicon `RATING_LABELS` (reused by `RatingButtons` pills + the FillInBlank hint-cap caption) — instructions, submit/hint labels, result eyebrows + teaching lines, rating question/countdown, and the session bilan (stats, "Aún n palabras por repasar", "← Inicio", "Escritura"/"Opción múltiple"). Pluralized strings build per-language at the render site. Plus **gloss gating** on the FR render sites (MCQ definition-hint + example gloss; écriture sentence gloss + result example callout + wrong-verdict FR meaning) via `glossVisibility` + `TapReveal`. `fr_es` is **byte-identical to today** (visible-branch markup unchanged). The exclamations (`¡Eso es!` etc.) + the verb cue (`review-cue.ts`) stay Spanish by design.
- **Copy:** the vetted ES (register tú / es-ES) was authored by Omar and folded in the same commit (v0.9.2) — Review ships immersion-complete, not mechanism-only. **One a11y gap deferred to M6.1d** (`backlog.md`): the shared `AnswerBlank` aria-label (French because `AnswerBlank` is shared with the still-French `/drill`). CC did not author any Spanish.
- Suite 458→466 (`lib/immersion` cases); tsc + lint + build clean. **Migration apply-on-deploy. Smoke-test all 3 modes on Review across Sépia · Ardoise · Indigo · Nuit (incl. tap-reveal + mode-c suppression); tag v0.9.2 on the final post-polish commit.**

### M6.1b — Discover immersion-awareness ✅ (v0.9.3)
- **Second surface onto the M6.1a substrate** (same two moves as Review): FR-gloss gating + chrome resolve on `/discover` (`DiscoverClient.tsx`), reusing `resolveChrome`/`glossVisibility`/`TapReveal`/`useSettings`. `fr_es` byte-identical.
- **`DISCOVER_CHROME`** (`lib/immersion.ts`) — full Discover chrome, French + vetted ES (Omar): grid (title/subtitle/Pour toi/Par thème/Bientôt/featured cards + toasts/close), generation (title + the 4 phase labels + error/retry), card (stamps + swipe hints), bilan, exhausted. Dynamic/plural lines (bilan add-line + "déjà connu", exhausted heading, topic-tile count) build per-language at the render site. **No French holes** — the one exception is the shared `LoadingChecklist` "Pendant que tu attends" (see backlog).
- **Card gloss gating** (2 sites — headword gloss + example FR) via `glossVisibility` + `TapReveal` labelled **"Toca para traducir"** (card register, ≠ Review's "Ver traducción"): `visible` unchanged · `tap` reveal · `hidden` suppressed (incl. the "·" separator). Spanish content (headword/article/pos/example.es) always shown.
- **`deck` → `collection` rename (narrow):** `DeckCard→CollectionCard`, `deckArticle→collectionArticle` (`lib/discovery.ts` + `DiscoverClient` + `api/discovery/generate`), phase `'deck'→'collection'`, the flow's internal comments + the 3 API error bodies; **user-facing `/add`** "dans ton deck" → "dans ta collection" (×2). Broad internal `deck` identifiers (drill `DeckVerbInput`, the `already_in_deck` wire value, home `deckVerbs`) **deferred** (`backlog.md`).
- Suite 466→467 (`DISCOVER_CHROME` case); tsc + lint + build clean. **Smoke-test all 3 modes on `/discover` across the 4 palettes (fr_es unchanged; immersion = ES chrome + tap-reveal card gloss; totale = FR suppressed); tag v0.9.3 on the final post-polish commit.**

### M6.1c — TopNav · Home · Status · Words immersion ✅ (v0.9.4)
- **Third surface group onto the M6.1a substrate** — mostly chrome-swap; ONE gloss-gating spot (word detail). New per-surface maps in `lib/immersion.ts`: `NAV_CHROME`, `HOME_CHROME`, `STATUS_CHROME`, `WORDS_CHROME`, `DETAIL_CHROME` (French + vetted ES, register tú/es-ES). `fr_es` byte-identical.
- **TWO mechanisms (the architecture):** the Home surface + shared rows are **server components** and `WordRow` is consumed by the French-only `/add` → so the mode is **prop-threaded with a `fr_es` default** (`StatusPill`/`MasteryGauge`/`WordRow` take `mode?='fr_es'`; `ReviewHero`/`CollectionSection` take `mode`; Home `page.tsx` + `words/[id]/page.tsx` read `immersion_mode` server-side and thread it) — **`/add` passes nothing → stays French, no leak.** Client surfaces (`TopNav`, `WordList`, `SwipeRow`, `DeferredDelete` via `scheduleDelete({mode})` since it sits above `SettingsProvider`, `WordDetailContent`, `WordDetailActions`) read `useSettings()` / the threaded mode.
- **Word-detail gloss gating** (definition + each example) reuses the page's **existing `TextLink` toggle** (no `TapReveal`): `visible` keep today's toggle byte-identical · `tap` same toggle, ES label (`resolveChrome`) · `hidden` suppress the FR **and** the toggle.
- **Fixes:** `MasteryGauge` aria "Maîtrise…" → progress-framed "Mémorisation : {n} / 4" / "Memorización : {n} / 4" (drops the Mémorisé-not-Maîtrisé violation). `deleteToastMessage` + `DeferredDelete` undo toast mode-aware (fem. agreement: "«w» eliminada" / "{n} palabras eliminadas").
- **`AudioButton` parked** (shared across dictionary/review/add/account/detail) → shared-components-pending list. All ES vetted by Omar; **CC authored no Spanish**. Suite 467→469; tsc + lint + build clean. **Smoke-test all 3 modes across TopNav/Home/Words/detail on the 4 palettes (`/add` stays French); tag v0.9.4 on the final post-polish commit.**

### M6.1d+ — remaining immersion surfaces (not yet built)
- **M6.1d — Drill immersion** (make `/drill` mode-aware; folds in the deferred `AnswerBlank` aria-label).
- **Shared-components-pending (make mode-aware when their last French consumer joins):** `AudioButton` ("Écouter la prononciation", shared 6 surfaces) · `LoadingChecklist` ("Pendant que tu attends", shared with `/add`) · `AnswerBlank` aria (shared with `/drill`).
- **`deck` anglicism — finish the app-wide drop** (deferred internal identifiers: `DeckVerbInput`/`deckVerbs`, the `already_in_deck` wire value + comments/tests).
- ~~Copy fill-in for Review~~ **DONE in M6.1a** (vetted ES folded in v0.9.2).
- **Never mode-aware:** onboarding scaffolding is always French (instructional; not built yet) — the reason the resolver is per-surface opt-in, not a global lang lock.

- App's first opening should trigger an onboarding with interactive steps showing how the app works.
- Login screen should be reworked to have Paco's branded content, to actually allow to login or to create a new account.
- **Collect a real display name → replace the placeholder.** The drill recap ("¡Muy bien, {name}!") currently derives the name from the email local-part via `displayNameFromEmail` (`lib/display-name.ts`) as a placeholder. Onboarding should capture a real name, store it on `profiles`, and swap **only that helper's source** (read the stored name, fall back to the email derivation) — every name surface already reads through the helper, so no call sites to chase. (Detail in `backlog.md` → Onboarding.)

## M7 — Companion character
- Visual/voice half shipped in v0.3.3. Remaining: non-pushy, opt-in personalized AI check-ins (small Anthropic call per check-in).
- **Data-gated** — needs ~100+ words before it's meaningful (currently ~72); naturally blocked on accumulation, not dev.

---

### Design-pass standing decisions (apply across M5.x)
- **No streaks** anywhere. Streak counters appeared in ①/⑤ mockups — do not reintroduce.
- ④ correction logic is **ours** — designs do not dictate how answers are graded. The near-miss/typo *display* (Levenshtein, already loaded) is decoupled and can ride whenever `/review` is next opened.
- Terminology unified on **"Mémorisé"** everywhere (filter = "Mémorisés", count = "N mémorisés"). "Maîtrisé" not used.
- Ceremony-fatigue rule: informative-over-celebratory on the per-use path; big editorial moments reserved for genuinely low-frequency events.
- The frame-5 "revu N fois · prochaine révision dans Xj" line is a **free, decoupled** near-term slice (pure `review_cards` data) — can land in the existing interstitial anytime, independent of the conjugator.
- **v2 system locked (from M5.5a):** amber is the ONE interactive/brand hue (sage = success/acquisition only; terracotta = destructive/error only, rare); a full-amber card is a SURFACE not a control (amber fill is reserved for the button — its card takes crème+); `¡Casi!` is chromatically NEUTRAL (ink headline + crème+ tint, never amber/sage/terra); Fraunces is allowlist-only (¡Listo!/¡Casi!/big counts); no dark-brown primary. These bind every cluster slice.
